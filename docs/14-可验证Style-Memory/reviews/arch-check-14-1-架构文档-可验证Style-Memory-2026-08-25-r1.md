# 架构文档检查报告：可验证 Style Memory

> 检查对象：`14-1-架构文档-可验证Style-Memory.md`
> 需求基准：`14-0-需求设计-可验证Style-Memory.md`（依据架构文档 frontmatter `input_documents` 唯一指向，路径存在，与文件名推断一致）
> 检查日期：2026-08-25
> 检查方法：arch-check skill（结构完整性 + 15 维需求满足度 + 代码事实验证）

---

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | `input_documents` 唯一指向本次使用的 PRD，路径存在，与文件名推断（14-0 / 14-1 同目录同编号）一致 | — |
| Frontmatter contract | ✅ | `workflow_type: arch-gen`、`status: review_ready`（合法取值）、`input_documents`、`open_questions` 四个必填字段齐全 | — |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 §5.9「无未决问题」一致；原问号项 Q1–Q5 均已替换为一行决策记录，未静默删除章节 | — |
| 实现影响的未决问题 | ✅ | 无未决问题。模块边界、Schema、API、状态机、回退算法、预填算法、排序口径均已收敛到 ADR / §6 / §5.9 | — |

> 本节全部通过，无前置阻塞。

---

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter（F.1–F.6） | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5 | 0 | 0 | 100% |
| 3. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 4. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 5. 系统上下文与模块职责 | ⚠️ | 2 | 1 | 0 | 67% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| 质量检查项（Q.1–Q.5） | ✅ | 5 | 0 | 0 | 100% |
| **合计** | — | **49** | **1** | **0** | **98%** |

注：模板章节顺序编号（ADR 为第 3/5 章、上下文为第 4/5 章）在本文档中互换了位置，但语义对应齐全，不计为问题。

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖（PRD §1.4 十三项） | 12 | 1 | 0 | 92% |
| 3. 用户故事满足（US-01–US-12） | 11 | 1 | 0 | 92% |
| 4. 业务规则执行（规则 1–30） | 28 | 2 | 0 | 93% |
| 5. 异常与边界覆盖（异常旅程 6 条） | 6 | 0 | 0 | 100% |
| 6. 成功标准可达性（AC-01–AC-11，关键维度） | 10 | 1 | 0 | 91% |
| 7. 风险与降级对齐 | ✅（PRD 无独立风险/降级清单，见 §四说明） | — | — | 100% |
| 8. UX 设计支撑 | ✅（含 1 处已记录的合理收敛） | — | — | 100% |
| 9. ADR 需求溯源（ADR-1–ADR-8） | 8 需求驱动 | 0 | 0 | 100% |
| 10. 范围合规性（不做十项） | 10 | 0 | 0 | 100% |
| 11. 一致性检查（关键维度） | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | ✅ 无存疑设计 | — | — | 100% |
| 14. 闭环完整性（关键维度） | 5/5 链路可走通 | 0 | 0 | 100% |
| 15. 安全底线（关键维度） | ✅（PRD 无合规底线章节，见 §五说明） | — | — | 100% |

> 部分满足项共 5 处，但根因为 2 个独立问题（见 §六），其中 AC-02 搜索来源问题同时影响维度 2/3/4/6 四个条目。

---

## 三、结构完整性详细检查

以下章节全部为 ✅，按压缩规则仅列关键证据：

- **Frontmatter**：F.1–F.6 全过。`status: review_ready` 合法；`open_questions: []` 与 §5.9 同步（F.6）。
- **1. 系统摘要**：核心能力 3 句概括；闭环锚点「保存 → 验证 → 复用（Memory → Verify → Reuse）」≤5 词；首版定位明确（资产语义升级、不新增外部服务与表）。
- **2. 范围**：P0 七组交付逐项可辨；「明确不做」继承 PRD 十条并加架构层约束；成功标准表中功能性指标全部定性（状态诚实性、删除边界、AC 全通过），仅性能类保留数值（p95 ≤500ms / ≤300ms），符合「不使用百分比量化」规则；AC 承接矩阵 11 条齐全，每条含 PRD 原文摘要 + 承接模块 + 关键链路/状态 + 风险/降级（2.5 全过）。（注：PRD 本身无 P1 清单，模板 2.2「P1 预留」由 §4.3 与 §10 演进方向承载，不计缺失。）
- **3. 用户流程与状态**：主流程（复用/保存/治理）+ 关键分支表（11 个分支，均含入口/触发条件与架构处理）+ mermaid 两态状态机及 3 条关键规则（不变式、读时防御、页面级状态归属）。
- **5. ADR**：8 条（符合 4–8 上限），每条含选择/理由/风险与对策（ADR-3 用演进余地替代，合规）；§5.9 保留并替换为决策记录，未删除。
- **6. 运行链路**：5 条链路均为编号步骤 + 实现原则段落；覆盖全部 P0 端到端流程；算法具体到 SQL 谓词、聚合 JOIN、规范化比较与 sessionStorage 握手细节。
- **7. 领域对象**：对象表（7 对象含 SSOT/Owner）、TS Schema（嵌套 ≤2 层）、API 表（8 端点 ≤10，认证与限流在说明列 + §8.3 承载）、写点矩阵、数据边界、命名与术语映射齐全。
- **8. NFR**：性能表、L1–L5 降级链、安全表、成本表、可观测性（新增 3 事件）、风险表（7 项含缓解）齐全。
- **9. 实施方案**：Phase A/B/C 均明确到文件路径与实现内容，每个 Phase 有独立验证目标；引用的文件（schema.ts、template-repository.ts、各 route.ts、render-readiness.ts、use-workspace-state.ts、style-memory-view-model.ts、left-sidebar.tsx、各 e2e spec）全部真实存在。
- **10. 架构结论**：核心判断（三条设计主线 + 一处必须修复的 FK 缺陷）与演进方向明确。
- **质量项**：Q.1 无 DDL/目录树/环境变量（SQL 片段属链路算法粒度，符合任务要求的算法具体性）；Q.2 无重复；Q.3 一致；Q.4 ADR 8 / 模块 6 / API 8 / 嵌套 ≤2 全部达标；Q.5 frontmatter 与正文一致。

唯一 ⚠️ 项：

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 4.1 | 系统上下文 ASCII 框图 | ⚠️ | 未使用 ASCII 框图，以文字列表描述四个外部系统（PostgreSQL/R2/Auth.js/AI Pipeline）与数据流向变化点。内容完整、边界清晰，仅形式偏离模板。见问题 📝-5。 |

---

## 四、需求满足度详细检查

### 维度 2：范围覆盖（PRD §1.4 十三项 → 架构 §2.1 七组交付）

13 项中 12 项有完整 ADR/链路/模块支撑（证据：§2.1-1↔ADR-1/ADR-8、§2.1-2↔§6.1、§2.1-3↔§6.2、§2.1-4↔§6.3、§2.1-5↔§6.5/ADR-7、§2.1-6↔ADR-6、§2.1-7↔§8.2 L1–L5）。

| PRD 需求 | 架构覆盖 | 状态 | 证据 | 差距说明 |
|----------|---------|------|------|---------|
| 范围 3：搜索覆盖名称、真实风格规则、变量和**可见来源信息** | §6.1 search 谓词 = `name OR description OR array_to_string(retained_rules) OR array_to_string(negative_constraints) OR 变量名/标签聚合` | ⚠️ | 架构 §6.1 步骤 2、§2.4 AC-02 行 | 谓词不含任何「来源」字段，也未显式说明取舍。变量维度已显式决策（仅 name+label），来源维度被静默省略。见问题 ⚠️-1。 |

### 维度 3：用户故事满足

US-01/02/04–US-12 共 11 条在 §3.1/§6.1–§6.5 均有完整操作路径（含前端状态、API 调用、数据变更，见 §4.2 UI 模块交互链路补充）。

| 用户故事 | 架构流程/链路 | 状态 | 差距说明 |
|----------|-------------|------|---------|
| US-03: 搜索页面实际展示的风格规则、变量**和来源** | §6.1 搜索谓词 | ⚠️ | 同 ⚠️-1：来源不可搜索，与「页面实际展示」口径存在缺口。 |

### 维度 4：业务规则执行

规则 1–30 中 28 条有具体执行机制（含架构层强制点）：

- 规则 2/12（状态定义、主动勾选）→ ADR-1 服务端派生 + §5.9 Q5 默认不勾选；
- 规则 4（每条最多一个代表结果）→ §7.2 单引用字段 + §6.4-2 原子更新；
- 规则 5（旧资产）→ §7.4 迁移默认回填 `pending_verification`；
- 规则 6（卡片事实）→ Phase B-3 删除 `NAME_TAG_RULES`（该代码确实存在于 `src/lib/style-memory-view-model.ts:31`）；
- 规则 7（卡片预览）→ Phase B-3 预览选择算法；
- 规则 9（筛选互斥/组合/返回保持）→ §6.1 URL 持久化 + status 单选；
- 规则 11（预填缺失标记）→ §6.3-A-3「缺失项显式标记，不推测补齐」；
- 规则 14（输入反馈时机）→ §6.3-A-4 中性帮助文案 + 失焦/提交才报错；
- 规则 15（编辑影响）→ §6.4-1 规范化集合比较算法（服务端 + 前端同口径）；
- 规则 20（工作区保护判定）→ §6.5-2 三分支判定算法；
- 规则 22（准备状态一致）→ ADR-7 单一结论对象；
- 规则 23（不自动生成）→ §6.5 实现原则；
- 规则 25（删除边界）→ §6.4-4 + ADR-2 FK `SET NULL`；
- 规则 27/28（弹层焦点/删除禁背景关闭）→ ADR-6 + §3.2 分支表「确认层禁用背景点击关闭」；
- 规则 3（状态文字+视觉标识）→ Phase B-3「状态徽标文案」（徽标含文字，非仅颜色）。

| 业务规则 | 执行机制 | 状态 | 差距说明 |
|----------|---------|------|---------|
| 规则 8: 可见的名称、保留规则、变量名称和**来源说明**均可被搜索 | §6.1 谓词 | ⚠️ | 同 ⚠️-1：来源说明未纳入搜索范围，且 §2.4 AC-02 行声称「search 谓词覆盖可见字段」，与 PRD 口径存在静默收窄。 |
| 规则 29: 图标按钮必须有可理解名称**和舒适的点击区域** | ADR-6 / Phase B-2 仅「图标按钮可访问名称」 | ⚠️ | 「舒适点击区域」（最小命中面积）未在原语规格或验收承接中出现。见问题 ⚠️-2。 |

### 维度 5：异常与边界覆盖

异常旅程 6 条全部三要素齐备：空列表（Phase B-4 空态 + 创建入口）、搜索无结果（§6.1 条件保留 + 清除动作）、旧资产/部分缺失（§3.2 分支 + §6.2 分区标注，AC-09）、保存冲突/暂不可用（§8.2 L3：保留全部步骤、409 改名、5xx 重试、服务端同名防重）、列表/详情不可用（L4：URL 条件 + 游标 + 工作区快照保留）、未登录（L5：登录后回原入口恢复查询）。✅ 全过。

### 维度 6：成功标准可达性（关键维度）

AC-01–AC-11 逐条承接（§2.4 矩阵），语义核对结果：

| AC | 状态 | 核对结论 |
|----|------|---------|
| AC-01 | ✅ | 卡片字段（状态/代表结果或来源图/规则摘要/变量数/最近使用）与加载期不显示虚假资产均在 §6.1 DTO 与 Phase B-3/4 中承接；名称派生标签代码删除有明确指向。 |
| AC-02 | ⚠️ | 搜索/筛选组合、URL 条件与浏览位置恢复均已承接；但 PRD 动作含「搜索一条卡片上可见的……来源」，架构谓词无来源字段且未显式决策（⚠️-1）。 |
| AC-03 | ✅ | §6.2 DTO 覆盖 PRD 列出的全部九类信息（参考图/代表结果/来源 Iteration/风格指纹=styleTokens/核心保留规则/增强方向=enhancementHints/排除约束/可替换变量/使用情况），完整提示仅在高级信息。 |
| AC-04 | ✅ | 双来源保存（§6.3-A/B）、服务端状态派生、提交锁防重复、中性帮助文案全部承接；既有来源迭代校验（本人+completed+resultAssetId）在代码中属实。 |
| AC-05 | ✅ | 五个连续动作（仅元数据修改→保持；规则修改→回退；复制→待验证；待验证选代表结果→已验证；替换：取消不变/确认替换）在 §6.4-1/2/3 与 §7.4 写点矩阵中逐一闭合。 |
| AC-06 | ✅ | 预检必填门（列出缺失项禁止进入）、取消零变更、身份条持续显示、ADR-7 就绪单一来源、不自动生成、sourceTemplateId 关联与 ADR-4 聚合更新全部承接。 |
| AC-07 | ✅ | ADR-2 将 `generation_tasks.source_template_id` FK 由 `NO ACTION` 改 `SET NULL`——已核实 `drizzle/0003_swift_nitro.sql:5` 确为 `ON DELETE no action`，「必须修复项」判断属实；删除只删 templates 行、不触碰 assets/analysis_tasks/generation_tasks。 |
| AC-08 | ✅ | 四弹层 + 更多菜单迁移共享原语（ADR-6），Playwright 键盘断言；「舒适点击区域」部分见 ⚠️-2。 |
| AC-09 | ✅ | 迁移默认 `pending_verification`、不回填伪造、分区缺失说明、仍可编辑/复用。 |
| AC-10 | ✅ | 条件持久化 URL（含游标）、401/503 既有口径、工作区快照不触碰；已核实既有 GET 契约（limit 默认 10 上限 50、search ≤100、cursor ISO 8601）与代码一致。 |
| AC-11 | ✅ | 409 名称冲突（POST 代码确为 `TEMPLATE_NAME_CONFLICT`）、向导步骤与内容保留、服务端同名检测防重复、成功进入新详情。 |

### 维度 7：风险与降级对齐

PRD 无独立「风险清单/降级策略」章节（第 5 部分为决策记录）。PRD 决策记录 6 条全部被架构承接且无改写（验证状态两态、保存流程次序、规则修改回退、预检+身份条、AC-08 键盘验收、自动审计不做）；PRD 异常旅程的降级语义由 §8.2 L1–L5 逐条对齐（含「保留什么、下一步做什么」）。✅

### 维度 8：UX 设计支撑

线框图六组交互（列表搜索/筛选/排序、详情四分区、三步保存向导、预检弹层、身份条、删除确认）均有前端状态与接口支撑（§4.2 交互链路补充逐个给出「触发 → 状态变化 → 回调 → 数据更新」）。一处收敛：PRD 线框出现「排序：[最近使用 ▾]」下拉控件，架构 Q2 决策首版固定「最近使用」不做切换器——PRD 正文与 AC 均未要求多排序，属有决策记录的合理收敛，不计问题。✅

### 维度 9：ADR 需求溯源

| ADR | 溯源需求 | 判定 | 说明 |
|-----|---------|------|------|
| ADR-1 验证状态服务端派生 | PRD 决策「验证状态只两态」+ 规则 12 | 需求驱动 | 三个写点 + 客户端不可写，直接兑现「不把系统推测冒充用户认可」。 |
| ADR-2 引用不复制 + FK SET NULL | AC-07 / 规则 25 | 需求驱动 | 兼修复既有 FK 缺陷（已核实 0003 迁移为 no action）。 |
| ADR-3 规则四元组 text[] 扁平快照 | PRD 详情字段要求 + 规则 15 | 需求驱动 | 可编辑/展示两组按「是否触发回退」分层，正对齐规则 15。 |
| ADR-4 使用情况读时聚合 | AC-06（最近使用与派生数量更新）+ 列表信息层级 | 需求驱动 | 避免生成链路第二写点。 |
| ADR-5 sessionStorage 一次性握手 | AC-06 / 规则 19–21 | 需求驱动 | 复用既有快照机制（`primeWorkspaceSnapshotFromTemplate` 与 `?templateId=` 路径均已在代码核实存在），失败回退路径明确。 |
| ADR-6 自建焦点原语 | AC-08 / 规则 27/28/30 | 需求驱动 | 已核实仓库确无 Radix 依赖。 |
| ADR-7 就绪结论单一来源 | 规则 22 / AC-06 | 需求驱动 | 扩展既有 `deriveRenderReadiness`（函数已在代码核实存在），向后兼容。 |
| ADR-8 保留路径与表名 | PRD 产品语言统一决策 | 需求驱动 | 避免 Pure 重构成本；引用的 `left-sidebar.tsx` 属实（`app-shell.tsx` 引用有小误，见 📝-2）。 |

无孤儿决策。✅

### 维度 10：范围合规性

PRD「明确不做」十条逐一对照：架构 §2.2 全部继承并加架构层约束（不建新表、不改名、不做全文索引、不回填旧数据）；全文未出现批量操作、版本树、多代表结果、标签体系、自动评分、插件、移动端重设计等设计。✅

### 维度 11：一致性检查（关键维度）

- ADR 间一致性 ✅：ADR-1（服务端派生）与 ADR-3（可编辑字段触发回退）、ADR-2（SET NULL）与 §6.4 删除链路互相印证。
- Schema 与 API 一致性 ✅：POST/PUT body 字段 ↔ §7.2 记录字段 ↔ §6.3/§6.4 算法三方对齐；DTO（列表/详情/候选）与 §6.1/§6.2 SQL 输出对齐。
- 状态机与流程一致性 ✅：§3.3 状态机 ↔ §7.4 写点矩阵 ↔ §6.3/§6.4 链路步骤一一对应；「三个服务端写点」与五写点矩阵不矛盾（duplicate/迁移只能写 pending，不可能违反不变式）。
- 范围与设计一致性 ✅：§2.1 七组交付均有 ADR/链路/Phase 支撑。限流覆盖口径存在表述与实施清单的精度问题，记为 📝-1。

### 维度 13：过度设计检测

未发现存疑设计。§4.3 显式列出 8 项刻意避免的过度设计（版本表、资产表、冗余计数、Redis/队列、Radix、全文检索、标签体系、更名），量化约束全部达标（模块 6、API 8、ADR 8、嵌套 ≤2）。超出 PRD 的部分（FK 修复、限流、输入上限）均有明确的需求或安全理由。✅

---

## 五、深度质量检查

### 维度 14：闭环完整性（关键维度）

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| §6.1 列表/搜索/筛选 | ✅ | 无 | 挂载→URL 参数→SQL（谓词/聚合/代表结果 JOIN）→DTO→游标编码→返回恢复，输入输出齐备。 |
| §6.2 详情读取 | ✅ | 无 | 401/404 分支、DTO 组装、读时防御降级、待验证引导标记均定义。 |
| §6.3 保存（双来源） | ✅ | 无 | 预填（V1/V2 分支算法）→三步向导/草稿说明→提交校验→状态派生→201→跳详情；失败分支保留步骤。 |
| §6.4 治理动作 | ✅ | 无 | 编辑回退算法、候选集定义、原子替换、复制不携带验证、删除 FK 解链，取消分支全部零写操作。 |
| §6.5 复用预检与工作区集成 | ✅ | 无 | 预检门→工作区影响判定→握手（含失败回退 `?templateId=`）→身份条→就绪统一→生成关联。 |

### 维度 15：安全底线（关键维度）

PRD 无「合规底线」章节。架构 §8.3 覆盖认证授权（逐请求 session + userId scope + 候选归属校验）、输入校验（长度/条数上限）、限流、AI 应用安全（本期零新增模型调用、无新注入面）、敏感数据（日志不输出规则全文），满足 PRD 隐含的信任与滥用防护要求。✅

---

## 六、代码事实验证（抽查结果）

架构文档声称的全部关键代码事实逐项核实：

| # | 声称 | 核实结果 |
|---|------|---------|
| 1 | `templates` 表既有列与 §7.2「既有」标注一致 | ✅ `src/lib/db/schema.ts:184-212`（id/name/content/variables/sourceAssetId/sourceImageUrl/userId/sourceGenerationTaskId） |
| 2 | `generation_tasks.source_template_id` FK 现为 `NO ACTION`，是 AC-07 硬前置 | ✅ `drizzle/0003_swift_nitro.sql:5` 确为 `ON DELETE no action` |
| 3 | 新迁移编号 `0005_*.sql` | ✅ 现最新为 0004，编号合理 |
| 4 | 既有游标契约（limit 默认 10/上限 50、search ≤100 trim 空等同不过滤） | ✅ `src/app/api/templates/route.ts:329-383` |
| 5 | POST 名称冲突 409 `TEMPLATE_NAME_CONFLICT` | ✅ route.ts:210（但 PUT 侧 code 为 `CONFLICT`，见 📝-4） |
| 6 | 既有来源迭代校验（本人+completed+resultAssetId） | ✅ route.ts:259-274 |
| 7 | `POST /api/templates/[id]/duplicate` 既有端点、"(copy)"/"(copy N)" 去重 | ✅ duplicate/route.ts + template-repository.ts:203-225（注意：现复制**不含** sourceGenerationTaskId，见 📝-6） |
| 8 | `render-readiness.ts` / `deriveRenderReadiness` 为既有单一派生函数 | ✅ src/lib/render-readiness.ts:49 |
| 9 | `use-workspace-state.ts` / `currentTemplateId` / `style-gen-workspace-state` | ✅ src/hooks/use-workspace-state.ts:15,59,120 |
| 10 | `primeWorkspaceSnapshotFromTemplate` 组装逻辑、`?templateId=` 拉取路径 | ✅ src/app/workspace/templates/page.tsx:60 / src/app/workspace/page.tsx:229 |
| 11 | `NAME_TAG_RULES` 名称派生逻辑待删除 | ✅ src/lib/style-memory-view-model.ts:31 |
| 12 | 仓库零 Radix 依赖 | ✅ package.json / pnpm-lock 无 radix |
| 13 | V2 配方字段（`styleInvariants[].value`、`kind`、`optionalModifiers[].defaultValue`、`styleFingerprint.tokens`、`negativeConstraints`）与 V1 字段（`mustKeep`/`styleTags`/`visualKeywords`、`negativePromptText`） | ✅ src/types/models.ts:38-41,80-92,101-130,217 |
| 14 | `IterationDetail` 预填来源 | ⚠️ 小偏差：无 `variablesSnapshot`/`sourceGenerationTaskId` 字段名（实际为 `variables`+`variablesSource`，来源迭代 id 即 `IterationDetail.id`），见 📝-3 |
| 15 | `promptSummary` 截断 120 字符（既有口径） | ✅ generation-task-repository.ts:436 |
| 16 | 既有 `sourceTemplateName` 解析、`POST /api/generation` 携带 `sourceTemplateId` | ✅ generation-task-repository.ts:490 / api/generation/route.ts:140 |
| 17 | Phase B/C 引用的前端文件与 e2e spec（template.spec.ts、ai-first-style-memory.spec.ts、template-default-values.spec.ts、workspace-ai-first-iteration-memory.spec.ts 等） | ✅ 全部存在 |
| 18 | `left-sidebar.tsx` / `app-shell.tsx` 的 "Library" 标签 | ⚠️ 小误："Library" 仅在 left-sidebar.tsx:24；app-shell.tsx 无该字符串（已是 "Style Memory" pageLabel），见 📝-2 |
| 19 | §8.3「既有内存级滑动窗口 30 次/小时/IP 覆盖全部写端点」 | ⚠️ 表述偏差：现仅 POST /api/templates 有限流（route 本地 Map），PUT/DELETE/duplicate 均无，Phase A 也只给两个新端点接限流，见 📝-1 |

---

## 七、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无 ❌ 项。

### 部分满足的需求（中优先级）

| 编号 | 维度 | 需求 | 差距 | 修复建议 |
|------|------|------|------|---------|
| ⚠️-1 | 维度 2/3/4/6 | PRD 范围 3、US-03、规则 8、AC-02：「可见来源信息 / 来源说明」可被搜索 | §6.1 search 谓词（name/description/retainedRules/negativeConstraints/变量名+label）不含任何来源字段，且 §2.4 AC-02 行声称「search 谓词覆盖可见字段」，属对 PRD 口径的静默收窄（变量维度已有显式决策，来源维度没有） | 在 §6.1 增加来源可搜索口径的显式决策并同步 §2.4 AC-02 风险列：要么声明「卡片可见来源仅为图像，无用户可输入的文本来源字段，故无文本可搜索项」并说明与 PRD「来源」一词的对齐结论，要么把来源相关文本（如来源 Iteration 信息）纳入谓词 |
| ⚠️-2 | 维度 4 | PRD 规则 29 / AC-08：图标按钮「舒适的点击区域」 | ADR-6 与 Phase B-2 仅承接「可访问名称」，未承接最小命中面积 | 在 Phase B-2（dropdown-menu 原语规格）与 AC-08 承接中补最小点击区域要求（如 ≥44×44px 或等效 padding），并纳入键盘/指针断言 |

### 存疑设计（需评估）

无。

### 备注级问题（低优先级，建议随手修）

| 编号 | 位置 | 问题 | 修复建议 |
|------|------|------|---------|
| 📝-1 | §7.3 / §8.3 vs Phase A | 「既有内存级滑动窗口…覆盖全部写端点（含两个新 POST）」与现状（仅 POST /api/templates 有限流，且为 route 本地 Map）及 Phase A 清单（仅两个新端点接限流）不一致 | 明确 PUT/DELETE/duplicate 是否也接入限流并列入 Phase A，或将 §8.3 改述为「新写端点接入既有限流模式」 |
| 📝-2 | ADR-8 | 「`left-sidebar.tsx` / `app-shell.tsx` 的 "Library" 标签改名」——`app-shell.tsx` 无 "Library" 字符串（memory 变量 pageLabel 已是 "Style Memory"） | 改为仅 `left-sidebar.tsx`（及以其为准的 ariaLabel "Style Memory Library" 一并统一） |
| 📝-3 | §6.3-A-1 | 字段引用不准：`IterationDetail` 无 `variablesSnapshot` 与 `sourceGenerationTaskId` 字段（实际为 `variables`+`variablesSource`（snapshot/fallback/missing），来源迭代 id 即 `IterationDetail.id`，现保存链路正是 `sourceGenerationTaskId={detail.id}`） | 校准为实际字段名，避免实现者找不到字段 |
| 📝-4 | §8.2 | 「沿用既有结构化口径：…409 `TEMPLATE_NAME_CONFLICT`」仅对 POST 成立；既有 PUT 名称冲突返回 code `CONFLICT`（[id]/route.ts:320），而详情编辑表单支持改名会命中该路径 | 注明两处 code 现状，或在 Phase A 中统一 PUT 错误码并写入迁移说明 |
| 📝-5 | §4.1 | 模板要求的 ASCII 系统上下文框图缺失（以文字列表替代，内容完整） | 可选：补一张简图；或保持现状（不阻塞） |
| 📝-6 | §6.4-3 | 复制设计为携带 `sourceGenerationTaskId`，但既有 `duplicateTemplate` 不复制该字段——属有意的新行为而非既有事实 | 在 §6.4-3 标注「新增复制字段（现状不复制）」，防止实现者按现状理解 |

---

## 八、总结

- **结构完整度**：98%（49/50 检查项通过；唯一 ⚠️ 为 §4.1 框图形式）
- **需求满足度**：PRD 13 项范围 / 12 个用户故事 / 30 条业务规则 / 6 条异常旅程 / 11 条 AC / 10 条不做项中，除 2 个根因问题（⚠️-1 来源搜索、⚠️-2 点击区域）外全部充分满足；5 条运行链路闭环、8 条 ADR 全部需求驱动、无过度设计与范围违规
- **代码事实准确性**：19 项抽查中 16 项完全属实（含 FK `NO ACTION` 缺陷、既有 API 契约、预填配方字段、全部引用文件存在），3 项为表述级小偏差（📝-1/2/3 + 相关）
- **整体评价**：⚠️ **需补充后可进入下一阶段**
  - 无前置阻塞、无 ❌ 项、关键维度（6/11/14/15）无 ❌；存在 2 个非关键 ⚠️（≤3），修复后即可进入 `create-dev-plan`
- **优先修复建议**：
  1. ⚠️-1：在 §6.1 显式定义「来源信息」可搜索口径，并同步 §2.4 AC-02 行，消除对 PRD 验收口径的静默收窄
  2. ⚠️-2：补「舒适点击区域」规格（Phase B-2 + AC-08 承接）
  3. 📝-1/3/4：修正限流覆盖表述、IterationDetail 字段名、409 错误码口径三处与代码事实的小偏差，避免实现阶段误读
