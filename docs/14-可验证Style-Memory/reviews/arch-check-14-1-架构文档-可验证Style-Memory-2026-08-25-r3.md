# 架构文档检查报告：可验证 Style Memory（第 3 轮收敛检查）

> 检查对象：`14-1-架构文档-可验证Style-Memory.md`
> 需求基准：`14-0-需求设计-可验证Style-Memory.md`（验收标准 SSOT）
> 检查日期：2026-08-25
> 检查轮次：r3（r1/r2 问题已历经两轮修复，本轮重点：修复回归核验 + 全文自洽性 + 代码事实抽查 + "能否进入 create-dev-plan"收敛判定）
> 检查方法：按 `.agents/skills/arch-check/SKILL.md` 全维度检查；对架构文档声称的全部代码事实逐项对照真实代码库核验

---

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | frontmatter `input_documents` 唯一指向 `docs/14-可验证Style-Memory/14-0-需求设计-可验证Style-Memory.md`，文件存在，与本次使用的 PRD 一致 | 无 |
| Frontmatter contract | ✅ | `workflow_type: arch-gen`、`status: review_ready`（合法取值）、`input_documents`、`open_questions` 四项 required 字段齐全 | 无 |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 §5.9「无未决问题」+ Q1–Q5 已决策记录一致 | 无 |
| 实现影响的未决问题 | ✅ | 无未决问题；Q1–Q5 均已给出决策且与正文链路（§6.1 排序、§6.3 预填、Q5 默认不勾选）一致 | 无 |

无前置阻塞。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 问题数 | 说明 |
|------|------|--------|------|
| Frontmatter（F.1–F.6） | ✅ | 0 | 配对、字段、状态、同步全部通过 |
| 1. 系统摘要 | ✅ | 0 | 核心能力 ≤200 字、闭环锚点「保存 → 验证 → 复用」、首版定位齐备 |
| 2. 范围、非目标与成功标准 | ✅ | 1 📝 | 2.4 承接矩阵 11 条齐备；§2.1 搜索范围摘要与 §6.1 谓词有清单级不一致（R3-N4，不阻塞） |
| 3. 用户流程与状态 | ✅ | 0 | 主流程/双来源保存/治理流程、11 条关键分支表、mermaid 状态机齐备 |
| 4. 系统上下文与模块职责 | ✅ | 0 | ASCII 框图 + 6 模块职责表 + UI 交互链路补充 + 8 条反过度设计清单 |
| 5. 关键架构决策（ADR） | ✅ | 0 | 8 条 ADR（4–8 区间内），均有选择/理由/风险或演进；§5.9 为决策记录而非未决问题 |
| 6. 运行链路 | ✅ | 1 ⚠️ | 5 条链路均有编号步骤与实现原则；保存向导缺「可替换变量确认」环节（R3-2） |
| 7. 领域对象与关键契约 | ✅ | 0 | 对象表/Schema（嵌套 ≤2）/8 端点 API 表/写点矩阵/数据边界/命名与术语映射 |
| 8. 非功能需求、风险与运行策略 | ✅ | 0 | 性能表（仅性能类数值化）、L1–L5 降级链、安全表、成本、可观测性、7 项风险 |
| 9. 实施建议与技术选型 | ✅ | 0 | Phase A/B/C 条目到文件路径，每 Phase 有独立验证目标 |
| 10. 架构结论 | ✅ | 0 | 核心判断 + 演进方向 |
| 质量检查项（Q.1–Q.5） | ✅ | 0 | r2 的 Q.2 限流重复已收敛为 §8.3 规范出处 + §7.3 一行交叉引用 |

**结构完整度：100%**（全部章节实质性完成；r2 遗留的 Q.2 问题已修复）。

### 2.2 需求满足度

| 维度 | ✅ | ⚠️ | ❌ | 完成度 |
|------|-----|-----|-----|--------|
| 2. 范围覆盖（PRD §1.4 十三项） | 13 | 0 | 0 | 100% |
| 3. 用户故事满足（US-01–US-12） | 12 | 0 | 0 | 100% |
| 4. 业务规则执行（规则 1–30） | 30 | 0 | 0 | 100%（规则 3 附强化建议 R3-N1） |
| 5. 异常与边界覆盖（异常旅程 6 条 + 关键状态 15 行） | 5/6 | 1 | 0 | 92%（空态引导双入口缺口，= R3-1） |
| 6. 成功标准可达性（AC-01–AC-11，**关键维度**） | 9 | 2 | 0 | 82%（AC-04 变量确认、AC-10 空态双入口；见 R3-1/R3-2） |
| 7. 风险与降级对齐 | 全部 | 0 | 0 | 100%（PRD 无独立风险/降级清单，对应物为异常旅程与关键状态，§8.2/§8.6 对齐） |
| 8. UX 设计支撑（6 组线框交互） | 5 | 1 | 0 | 92%（保存向导变量确认环节缺失，= R3-2） |
| 9. ADR 需求溯源（8 条） | 8 需求驱动 | 0 | 0 | 100%，无孤儿决策 |
| 10. 范围合规性（不做项 10 条） | 10 | 0 | 0 | 100%，未引入任何不做项 |
| 11. 一致性检查（**关键维度**） | 4/4 | 0 | 0 | 100%（R3-N4 为表达级摘要不一致，不构成矛盾） |
| 13. 过度设计检测 | 无存疑设计 | 0 | 0 | 100%（8 项反过度设计清单 + 无 Redis/队列/全文检索/新表） |
| 14. 闭环完整性（**关键维度**，5 条链路） | 5 | 0 | 0 | 100%，无悬空步骤 |
| 15. 安全底线（**关键维度**） | 全部 | 0 | 0 | 100%（PRD 无显式合规底线清单；§8.3 覆盖认证/授权/校验/限流/AI 安全） |

**需求满足度：去重后 2 个独立 ⚠️（均位于关键维度 6，波及维度 5/8），0 ❌。**

## 三、r2 修复项回归核验

r2 提出的 2 ⚠️ + 3 📝 全部已修复，且未引入新矛盾：

| r2 编号 | 修复核验 | 结果 |
|---------|---------|------|
| ⚠️-1 限流表述重复/清单微差 | §7.3 末段收敛为「限流口径见 §8.3：现状仅 POST 接入；Phase A 统一覆盖全部 5 个写端点（POST / PUT / DELETE / duplicate / representative-result），读端点不限流」；§8.3 为规范出处；Phase A 任务 8 列 5 端点 | ✅ 三处口径一致（POST 现状已有 + 其余四项 Phase A 接入），重复全量表述已消除 |
| ⚠️-2 确认后焦点移至结果页首要内容 | ADR-6 补「确认类动作完成导航后，目标页面将初始焦点置于首要内容（PRD 键盘操作旅程第 4 步）」；Phase B 验证目标补「确认导航后结果页初始焦点落点」断言 | ✅ 两处一致，键盘操作旅程 4 步全部闭环 |
| 📝-1 PUT 409 code 统一任务缺失 | Phase A 任务 6 补「PUT 名称冲突 code 由 `CONFLICT` 统一为 `TEMPLATE_NAME_CONFLICT`（§8.2 双轨说明）」 | ✅ 与 §8.2 一致 |
| 📝-2 app-shell 路径 | ADR-8 与 Phase B.6 均写全路径 `src/components/app-shell.tsx` | ✅ 与代码事实一致 |
| 📝-3 列表页图标按钮 44×44 | Phase B.4 补「清除搜索等页面级图标按钮遵循 ≥ 44×44px 命中面积与可理解名称（PRD 规则 29）」 | ✅ 覆盖点闭合 |

## 四、代码事实验证（r3 全量抽查）

架构文档声称的代码事实共抽查 23 项，**22 项属实，1 项字段名精度问题（R3-N3）**：

| # | 声称 | 核验结果 |
|---|------|---------|
| 1 | `templates` 表现状无验证状态/规则列，`sourceGenerationTaskId` 已存在 | ✅ `src/lib/db/schema.ts:185-212` |
| 2 | `generation_tasks.source_template_id` FK 为 `NO ACTION` 会阻断删除 | ✅ `drizzle/0003_swift_nitro.sql:5`（`ON DELETE no action`），AC-07 硬前置判断属实 |
| 3 | `source_template_id` 现无单列索引 | ✅ schema.ts 中 generation_tasks 索引不含该列 |
| 4 | 下一迁移编号 `0005` | ✅ 现有 0000–0004 |
| 5 | POST /api/templates：401 `UNAUTHORIZED` / 409 `TEMPLATE_NAME_CONFLICT` / 429 `RATE_LIMITED` / 503 `SERVICE_UNAVAILABLE`（DB 不可用识别） | ✅ `src/app/api/templates/route.ts` |
| 6 | 限流现状：仅 POST 接入、路由内本地 Map、30 次/小时/IP | ✅ route.ts:36-56（`checkRateLimit` 内存滑动窗口） |
| 7 | GET：limit 默认 10 上限 50、search ≤100 且 trim 空串不过滤 | ✅ route.ts:357-383 |
| 8 | PUT 409 现返回 `CONFLICT`（双轨属实） | ✅ `[id]/route.ts:320` |
| 9 | DELETE 204；duplicate 201、"(copy)" 去重算法 | ✅ `[id]/route.ts` / `duplicate/route.ts` / repository:204-235 |
| 10 | 既有 `duplicateTemplate` 不复制 `description` 与 `sourceGenerationTaskId` | ✅ repository 只复制 content/variables/sourceAssetId/sourceImageUrl |
| 11 | `src/lib/rate-limit.ts` 存在（既有可复用限流库） | ✅ `checkRateLimit(identifier, action, config)` |
| 12 | `render-readiness.ts` 已是单一派生函数 `deriveRenderReadiness` | ✅ 存在；消费方 workspace/page.tsx 与 output-card.tsx |
| 13 | `use-workspace-state`：sessionStorage key `style-gen-workspace-state`、`currentTemplateId` 存在，`memoryIdentity` 待扩展 | ✅ hooks/use-workspace-state.ts:15/59 |
| 14 | `primeWorkspaceSnapshotFromTemplate` 为既有组装逻辑，失败回退 `?templateId=` 拉取 | ✅ templates/page.tsx:60-94（字段不齐即不写快照）+ workspace/page.tsx:227-267 消费 `?templateId=` 并记录 `currentTemplateId` |
| 15 | `style-memory-view-model.ts` 存在 `NAME_TAG_RULES` 名称派生逻辑（待删除） | ✅ view-model.ts:31/46 |
| 16 | IterationDetail 字段：`promptSnapshot`/`recipe`+`recipeSource`/`variables`+`variablesSource`/`sourceAssetId`/`id`/`resultFileUrl`/`sourceTemplateName` | ✅ types/models.ts:290-318 |
| 17 | IterationDetail 无 `negativePromptText`（有 `negativePromptSnapshot`） | ⚠️ → R3-N3：§6.3 V1 分支引用的字段名与实际可得字段不一致 |
| 18 | V2 配方字段：`styleInvariants`（`kind: hard/soft`、`.value`）/`negativeConstraints`/`optionalModifiers[].defaultValue`/`styleFingerprint.tokens`；V1：`mustKeep`/`styleTags`/`visualKeywords` | ✅ types/models.ts:38-46/101-107/124-157 |
| 19 | left-sidebar "Library" 标签 + ariaLabel "Style Memory Library"；app-shell memory 变体已输出 "Style Memory" | ✅ left-sidebar.tsx:24-25 / app-shell.tsx:20/27（逐字一致） |
| 20 | `save-style-memory-dialog.tsx` 为待重构的单步对话框 | ✅ 现状：背景点击可关（:123-124）、名称空即显红色错误文案（:179-184）——正是 PRD 规则 14/28 要修的点，重构目标成立 |
| 21 | 新建项确实不存在：`templates/[id]/page.tsx`、`ui/modal-dialog.tsx`、`ui/dropdown-menu.tsx`、`style-memory/` 目录、`use-focus-trap.ts`；`state-presenter.tsx` 已存在 | ✅ 与 Phase B/C「新」标注一致 |
| 22 | 仓库零 Radix 依赖；四个受影响 e2e spec（template / ai-first-style-memory / template-default-values / workspace-ai-first-iteration-memory）均存在 | ✅ pnpm-lock 无 radix；`e2e/` 目录四个文件齐全 |
| 23 | `POST /api/generation` 既有 `sourceTemplateId`；`template_created`/`template_updated` 日志事件既有；`promptSummary` 服务端截断 120 字符 | ✅ generation/route.ts:140 / templates 路由 / models.ts:281 |

**结论：代码事实准确率极高，所有关键契约引用（错误码、限流、FK、字段名、组件路径、既有机制）均可回溯到真实代码，无虚构事实。**

## 五、需求满足度详细检查（仅展开存在问题的维度）

### 维度 6：成功标准可达性（关键维度）

AC-01、AC-02、AC-03、AC-05、AC-06、AC-07、AC-08、AC-09、AC-11 共 9 条：承接矩阵行齐备、语义未改写 PRD 原意、且有 §6.1–§6.5 链路 + ADR + Phase 验证目标三重支撑。抽验语义未漂移：AC-02 的「来源说明 = description」为显式口径决策并在 §2.4/§6.1/§7.6 三处同步；AC-07 的 FK 修复（ADR-2）经迁移文件核验为真实硬前置；AC-11 的 409/5xx 双分支与保留策略在 §6.3/§8.2 L3 一致。**全部 ✅。**

存在缺口的 2 条：

| AC | 状态 | 差距说明 |
|----|------|---------|
| AC-04 | ⚠️ | PRD 动作明确「依次确认代表结果、保留规则、**变量**和名称」，§1.3 保存流程决策为「先确认结果与保留规则，**再定义可替换内容**，最后命名」，线框步骤 2 含「可替换内容：主体 / 场景 / 服饰」。架构 §6.3 步骤 2 只描述规则/排除约束/快照的确认与预填算法，步骤 3 为命名——**「可替换变量及默认值」的确认交互环节缺失**（预填载荷与 POST 体虽含 `variables`，但向导步骤设计无变量确认段；工作区草稿流程 B 同样只写「步骤 2 预填自当前工作区配方」）。见 R3-2 |
| AC-10 | ⚠️ | PRD 结果第一条「空列表明确提供**从工作区或完成 Iteration** 创建第一条 Memory 的入口」（异常旅程第 1 条、关键状态第 1 行同义）。架构 §2.1 第 7 条只列「空态」状态名，§2.4 AC-10 行关键链路列只写「条件持久化 URL；401/503 既有错误码口径；工作区快照不触碰」，Phase B.4 只写「空态/未登录/服务异常/骨架」——**空态双入口内容无设计**。现有代码空态仅「Create from Reference / Open Workspace」且均跳工作区（templates/page.tsx:489-499），照现状实现会漏 Iterations 入口。见 R3-1 |

### 维度 5：异常与边界覆盖

异常旅程 2–6（搜索无结果、旧资产缺失、保存冲突/失败、列表/详情不可用、未登录）三要素完整，由 §3.2 关键分支表 + §8.2 L1–L5 + URL/快照持久化覆盖，与 PRD 口径一致，全部 ✅。异常旅程第 1 条（空态引导）= R3-1，⚠️。

### 维度 8：UX 设计支撑

列表、详情、预检、工作区身份条、删除确认 5 组线框交互均有前端状态与接口支撑（§3.1、§4.2 UI 交互链路补充、§6.5、ADR-6）；键盘操作旅程 4 步在 r2 修复后全部闭环（含确认后结果页首要内容聚焦）。保存向导线框的「可替换内容」段 = R3-2，⚠️。另附 R3-N1（颜色独立性表达）、R3-N2（工作区影响判定保守超集）两个备注。

### 全绿维度摘要（压缩）

- **维度 2（范围）**：PRD 十三项逐一落入 §2.1 七组交付，各有 ADR/链路/Phase 支撑。
- **维度 3（用户故事）**：US-01–US-12 在 §3.1/§4.2/§6.1–§6.5 均有完整操作路径（含状态、API、数据变更）。
- **维度 4（业务规则 1–30）**：30/30 有执行机制且在合适层面（非仅「提到」）：规则 2/4（§3.3 不变式 + 服务端三写点 + 读时防御）、规则 8（§6.1 显式谓词 + 提示文案承诺口径）、规则 12（Q5 默认不勾选 + 服务端校验）、规则 15（§6.4 规范化集合比较算法双侧同口径）、规则 20（§6.5 三分支判定算法）、规则 22（ADR-7 单一结论源）、规则 25（ADR-2 FK SET NULL）、规则 27–30（ADR-6 原语规格）。规则 3 由 Phase B.3「状态徽标文案」隐式满足，附强化建议 R3-N1。
- **维度 7（风险与降级）**：PRD 无独立清单，对应物（异常旅程/关键状态）与 §8.2 L1–L5、§8.6 七项风险对齐。
- **维度 9（ADR 溯源）**：8/8 需求驱动（ADR-1←规则 2/12 与 AC-04/05；ADR-2←AC-07/规则 25；ADR-3←规则 15/详情；ADR-4←规则 24；ADR-5←复用决策/AC-06；ADR-6←AC-08/规则 27–30；ADR-7←规则 22/AC-06；ADR-8←产品语言决策）。
- **维度 10（范围合规）**：10 条不做项零引入；§2.2 另加 5 条架构层约束。
- **维度 11（一致性）**：ADR 间无矛盾；§3.3 不变式三写点与 §7.4 五行写点矩阵口径自洽（duplicate/迁移只产生 pending，不触及不变式）；Schema 与 API 对齐（PUT 五字段、POST 扩展体、8 端点与 §4.1「8 端点 2 新增」一致）；状态机与链路一致；限流三处口径已统一。R3-N4 为表达级摘要不一致。
- **维度 13（过度设计）**：无存疑设计。反清单 8 项（无版本表/无独立资产表/无冗余计数/无 Redis/无 Radix/无全文检索/无标签体系/无更名）+ LATERAL 读时聚合、自建原语等选择均有成本论证。
- **维度 14（闭环）**：5 条链路（6.1–6.5）从触发到终态可独立走通，无悬空依赖；代表结果校验（须等于 sourceGenerationTaskId 且 completed 且有 resultAssetId）、候选集定义、回退路径均闭合。
- **维度 15（安全）**：PRD 无显式合规底线；§8.3 覆盖逐请求鉴权、userId scope、输入上限、限流、无新增模型调用、日志不输出规则全文。

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无 ❌ 项。

### 部分满足的需求（中优先级，⚠️）

| 编号 | 严重级别 | 所在章节 | 问题描述 | 修复建议 |
|------|---------|---------|---------|---------|
| R3-1 | ⚠️ | §2.4 AC-10 行 / §2.1 第 7 条 / §6.1 / Phase B.4 | 空列表引导无设计：PRD 要求空态明确提供「从工作区**或完成 Iteration** 创建第一条 Memory」双入口（AC-10 结果第一条、异常旅程第 1 条、关键状态第 1 行三处一致），架构仅在状态清单里列了「空态」二字，AC-10 承接行与 Phase B.4 均未落内容；现有代码空态两个按钮均只去工作区，照现状实现将漏 Iterations 入口导致 AC-10 首句验收失败 | 在 §2.4 AC-10 行关键链路列（或 §6.1 实现原则 / Phase B.4）补一句：「空态提供双入口——打开工作区（保存待验证方向）与查看 Iterations（从完成 Iteration 保存已验证方向）」 |
| R3-2 | ⚠️ | §6.3 A 步骤 2 / §6.3 B 步骤 2 / §2.1 第 4 条 | 保存向导缺「可替换变量及默认值」确认环节：PRD AC-04 动作明确「依次确认代表结果、保留规则、变量和名称」，§1.3 决策与线框步骤 2 均含「可替换内容」，两条保存流程的旅程第 4 步均要求确认变量及默认值；架构向导步骤 2 只设计了规则/排除/快照确认，变量仅有预填载荷映射（§6.3 步骤 1）与 POST 体（§7.3），无用户确认/编辑默认值的交互设计，照此实现 AC-04「确认变量」子句不可达 | 在 §6.3 步骤 2 补半句：「同屏确认可替换变量及默认值（预填自 IterationDetail.variables / 当前工作区变量，默认值可编辑，随提交体携带）」，§2.1 第 4 条括号同步为「（结果与代表结果 → 保留规则与可替换变量 → 命名）」 |

### 备注级问题（低优先级，不阻塞实施）

| 编号 | 严重级别 | 所在章节 | 问题描述 | 修复建议 |
|------|---------|---------|---------|---------|
| R3-N1 | 📝 | ADR-6 / §2.1 第 2 条 / Phase B.3 | PRD 规则 3「验证状态必须同时使用文字和视觉标识，不能只用颜色」与 AC-08「状态与错误不只依赖颜色」在架构中无显式承接句（Phase B.3「状态徽标文案」隐含文字徽标，语义已满足，但无明确承诺与断言口径） | 在 §2.1 第 2 条或 Phase B 验证目标补一句「验证状态徽标同时含文字与视觉标识，错误与状态提示不只依赖颜色（PRD 规则 3 / AC-08）」 |
| R3-N2 | 📝 | §6.5 步骤 2 | 工作区影响判定将「非空且非同一 Memory」一律提示「有不同的未完成内容」，未区分 PRD 规则 20「没有不同未完成内容时可直接确认进入」的许可情形（保守超集：多提醒不破坏任何 AC——AC-06 前置即「存在不同的未完成内容」；非 AC 漂移，属判定依据未言明） | 在 §6.5 步骤 2 补一句判定依据：「快照存在且含参考图或提示文本即视为未完成内容」，使保守分支成为显式决策而非歧义 |
| R3-N3 | 📝 | §6.3 A 步骤 3（V1 分支） | V1 预填「`negativePromptText` 非空则整体作为一条排除约束」——`IterationDetail` 无 `negativePromptText` 字段（实际为 `negativePromptSnapshot`；`negativePromptText` 存在于 AnalysisTask 与工作区快照），流程 A 预填来源为 IterationDetail，字段名与可得字段不一致，实现者需自行映射 | 将该句改为「V1：排除约束预填取 `IterationDetail.negativePromptSnapshot`（流程 A）/ 工作区 `negativePromptText`（流程 B）非空时整体作为一条」 |
| R3-N4 | 📝 | §2.1 第 2 条 ↔ §6.1 | §2.1 搜索范围摘要列「名称、说明、保留规则、排除约束、变量名与标签」，遗漏 §6.1 谓词实际包含（且有专门理由段说明应纳入）的 `styleTokens`（风格指纹）与 `enhancementHints`（增强方向）——摘要比规范口径窄，属修复 r1 来源搜索问题时引入的清单级不一致，无实质矛盾 | 将 §2.1 第 2 条括号补齐为「名称、说明、风格规则（含指纹与增强方向）、排除约束、变量名与标签」 |

### 存疑设计（需评估）

无。

## 七、总结

- **结构完整度**：100%（r2 的 Q.2 唯一遗留项已修复，无新引入的结构或重复问题）
- **需求满足度**：PRD 13 项范围 / 12 个用户故事 / 30 条业务规则 / 11 条 AC / 10 条不做项 / 6 条异常旅程中，去重后仅剩 **2 个 ⚠️**（R3-1 空态双入口、R3-2 向导变量确认，均为一句话级内容补充，不改变模块边界、API、Schema、状态机或错误处理）；5 条运行链路闭环、8 条 ADR 全部需求驱动、四个关键维度（6/11/14/15）无 ❌
- **代码事实准确性**：23 项抽查 22 项属实、1 项字段名精度问题（R3-N3）；包括 FK `NO ACTION`、409 双轨、限流现状、IterationDetail 字段、导航文案、既有快照握手与 `?templateId=` 回退路径、Radix 零依赖、e2e spec 文件名等关键断言全部与真实代码一致
- **r2 修复回归**：2 ⚠️ + 3 📝 全部修复且未引入新矛盾/重复/术语漂移（限流三处口径统一、确认后焦点落点补齐、Phase A 409 任务、app-shell 全路径、列表页 44×44 均已落位）
- **整体评价**：⚠️ **需补充后可进入下一阶段（create-dev-plan）**
  - 无前置阻塞、无 ❌、关键维度无 ❌；2 个 ⚠️ 均为关键维度（6）的内容级缺口，非契约级
  - **收敛判断**：R3-1 与 R3-2 各补一句话即可清零全部 ⚠️；两处修复不涉及任何边界/契约/状态机变更，主会话修复后**无需第四轮全量复查**，自行核对两处落位即可进入 `create-dev-plan`。4 个 📝（R3-N1–N4）为表达级备注，均不阻塞实施，可随两处 ⚠️ 顺手修复或在开发计划中吸收
- **优先修复建议**：
  1. R3-1：AC-10 承接行/Phase B.4 补空态双入口一句话
  2. R3-2：§6.3 步骤 2 补「可替换变量及默认值」确认半句（两条保存流程同改）
  3. R3-N1–N4：四处表达级备注随手修（颜色独立性显式句、工作区判定依据、negativePrompt 字段名、§2.1 搜索清单补齐）
