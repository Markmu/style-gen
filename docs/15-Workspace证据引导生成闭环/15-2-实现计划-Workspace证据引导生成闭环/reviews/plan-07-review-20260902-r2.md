---
feat_id: plan-07
title: Workspace 闭环集成与回归
review_date: 2026-09-02
review_round: r2
result: 通过
---

# plan-07 验收报告 r2（Workspace 闭环集成与回归 · 第二轮）

- 功能文件: `plan-07-Workspace闭环集成与回归.md`（验收前 frontmatter `status: review`；通过后流转 `done`）
- Red 证据: `docs/e2e/evidence/plan-07-e2e-red-20260901.md`（结论：预期失败，测试有效；r1 已核实，本轮无变化）
- Green 证据（第一版）: `docs/e2e/evidence/plan-07-e2e-green-20260901.md`（四条命令数据经 r1 复核属实；文件本体本轮确认未被改动）
- Green 证据（r2 修正版，本轮验收输入）: `docs/e2e/evidence/plan-07-e2e-green-20260901-r2.md`（结论：通过）
- 第一轮验收报告: `reviews/plan-07-review-20260901.md`（未通过，2 blocker：B-1 对齐 spec 确定性失败、B-2 green 证据门覆盖声明不实）
- 验收范围: 仅 plan-07 第二轮（blocker 核销 + 变更范围确认 + 关键命令复跑 + 维度 2/3/7 复核）；维度 1/4/5/8/9 的产品侧结论沿承 r1（见「变更范围控制」）

## 验收结论

**通过（0 blocker）**。r1 的 2 个 blocker 全部核销：B-1（`e2e/workspace-degradation.spec.ts` 重试用例确定性失败）已按 r1 修复建议以纯测试基建方式修复（补 `mockGenerationPolling` completed 终态 mock，断言未放宽），本轮实跑该文件 6/6 全绿（原失败用例 L256 转绿）；B-2（green 证据门覆盖声明不实）已由 r2 修正版证据更正为「3 门内 / 11 门外」并补录门外 10 spec 联跑 60/60 实际执行证据，本轮逐文件比对 `package.json` 确认门归属声明属实，并抽验实跑 3 个门外文件全绿。第一轮验收通过的 7 个维度未回退（r1 之后产品代码 `src/` 零变更，mtime 取证），维度 2/3/7 状态复核无回退。按 skill 规则流转：功能 `review → done`，勾选验收 checklist；plan-01～07 全部 done，README frontmatter `in_execution → accepted`。

## 变更范围控制（r1 → r2 diff 取证）

以 r1 报告落盘时间（2026-09-02 01:26，本地时钟）为界做全仓 mtime 比对（排除 node_modules/.next/.git/test-results/playwright-report），r1 之后仅 4 个文件变更：

| 文件 | 变更性质 | 判定 |
| --- | --- | --- |
| `e2e/workspace-degradation.spec.ts` | B-1 修复：测试基建 mock 补充 | 属「风险与边界」允许列表文件；非断言放宽（见 B-1 核销） |
| `plan-07-Workspace闭环集成与回归.md` | 文末追加「执行记录」B-1 修复记录 + 14 文件状态表 | 文档更新，Task/边界/checklist 表未动 |
| `docs/e2e/evidence/plan-07-e2e-green-20260901-r2.md` | 新增 r2 修正版 green 证据 | B-2 修复要求产物 |
| `README.md` | §5 状态机表 plan-07 行同步（task-review running + r2 证据） | 文档更新；frontmatter 仍 `in_execution`（本轮验收前） |

**`src/` 产品代码、其余 e2e spec、package.json 在 r1 之后零变更**。r1 已通过的维度 1/4/5（命令数据）/8/9 产品侧结论在本轮直接沿承；plan-07 验证命令 3/4（verify:full / verify:acceptance）的 r2 证据执行时点之后产品代码与 targeted 门成员均未变化，且命令 1/2 与关键 targeted spec 已在本轮复跑确认（见验证摘要），证据链对当前工作树持续有效。

## blocker 核销记录

### B-1 核销（对齐 spec 确定性失败 → 已修复，实跑绿，断言未放宽）

- **修复内容审查**（`e2e/workspace-degradation.spec.ts` L256–308 用例 + L281–290 修复注释）：为 `degeneration-generation-retry-task` 补 `mockGenerationPolling(page, 'degeneration-generation-retry-task', { ...loadFixture('generation-completed.json'), id 覆写 })`，即 mock `GET /api/generation/degeneration-generation-retry-task` 返回 completed 终态详情——与 r1 修复建议（「增加 mockGenerationPolling（processing 或 completed 详情），参照 workspace-layout.spec.ts 与主 spec TC-7.8 的 mock 组合」）完全一致；helper 本体（`e2e/helpers/mock-api.ts` L271–283）与 fixture（`generation-completed.json`，status=completed）均未改动。
- **断言强度比对**（对照 r1 记录的失败形态）：用例保留全部原有断言——首次 POST 500 后 `generation-submit-error` 可见且含服务端错误文本（'Service Temporarily Unavailable'）、`generation-dialog` count=0、`generation-submit-retry` 主动重试、重试成功后内联错误 `toBeHidden`、`generateButton` `toBeEnabled`、`generationPostCount === 2`。r1 失败点（L295 `toBeEnabled` 因 OAuth 导航 element not found）的根因（401 → `signIn("google")`）由 mock 消除，**无任何断言删减、放宽或超时放大**；mock 属测试前置条件补全，不改变被测行为契约。
- **本轮实跑**: `pnpm e2e -- e2e/workspace-degradation.spec.ts --project=workspace` → **6 passed / 0 failed（13.2s，exit 0）**，含原失败用例「生成错误Retry：内联错误主动重试后清除」（[6/6]）。
- 结论：**B-1 核销成立**。

### B-2 核销（green 证据门覆盖声明不实 → 已更正 + 补录，抽验属实）

- **门归属更正核实**: 逐文件比对 `package.json` 当前 `e2e:smoke` / `e2e:targeted` 脚本成员——14 个对齐文件中门内仅 3 个：`ai-first-shell`（targeted）、`workspace-ai-first-iteration-memory`（smoke+targeted）、`workspace-ai-first-render-dock`（smoke+targeted）；其余 11 个（degradation、edge-cases、error-path、happy-path、helpers/workspace-actions.ts、style-memory-reuse、workspace-degradation、workspace-history-strip、workspace-layout、workspace-prompt-generate、workspace-three-column-layout）均不在任何门内。r2 证据「3 门内 / 11 门外」的更正声明与事实**完全一致**，第一版「全部被命令 3/4 覆盖」的声明已按 B-2 要求作废并说明。
- **门外执行证据补录核实**: r2 证据「命令 5」记录门外 10 个可执行 spec 联跑 **60 passed / 0 failed（1.1m，exit 0）**，并给出逐文件通过数（degradation 7、edge-cases 3、error-path 6、happy-path 5、style-memory-reuse 14、workspace-degradation 6、workspace-history-strip 2、workspace-layout 8、workspace-prompt-generate 2、workspace-three-column-layout 7，合计 60，数字自洽）；helper `workspace-actions.ts` 经消费方 spec（15 用例）覆盖说明成立；第一版证据文件本体保持未改（mtime 取证）。
- **本轮抽验实跑（超出要求的 2 个，共 3 个门外文件）**：
  - `workspace-degradation.spec.ts`（单文件，B-1 所在文件）：6/6 绿（见上）。
  - `pnpm e2e -- e2e/happy-path.spec.ts e2e/workspace-three-column-layout.spec.ts --project=workspace` → **12 passed / 0 failed（18.2s，exit 0）**：happy-path 5（含 helpers/workspace-actions.ts 消费路径）、three-column-layout 7（含 TC-1.5 1280/1440 双视口列节奏断言）。
  - 抽验结果与 r2 证据逐文件计数一致（6/5/7），未见证据与实际不符。
- 结论：**B-2 核销成立**（更正如实、补录完整、抽验吻合）。

## 9 个维度结果（r2）

| # | 维度 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 文件交付完整性 | 通过 | 沿承 r1（清单 11 文件交付 + 清单外改动均在允许列表）；r1 后新增 diff 仅允许列表内 `workspace-degradation.spec.ts` 的测试基建修复，无新的清单外产品改动 |
| 2 | Task 列表完成度 | 通过 | 7/7 `done`，无 todo/waived 遗留（本轮复核未回退）；r1 指出的 Task 6 交付质量缺陷（对齐用例失效）已随 B-1 修复消除，Task 6「targeted/visual/存量 spec 对齐」现在名实相符 |
| 3 | 验收标准完成度 | 通过 | 最终功能验收 6 项 + US 矩阵 + 降级回归 + 全局质量门共 9 项实质证据在 r1 逐项核实基础上，经本轮复跑（三 spec 65/65、verify:fast exit 0、B-1 用例绿、门外抽验绿）确认仍成立；**本轮执行 checklist 全量勾选**（见状态变更记录） |
| 4 | 实现规格符合度 | 通过 | r1 唯一未达项（§4「不得放宽行为标准」在 workspace-degradation.spec.ts 未兑现）已修复且断言未放宽（B-1 核销）；产品代码 r1 后零变更，§1–§3 与「不在范围」结论沿承 r1 |
| 5 | 验证命令执行 | 通过 | 命令 1（三 spec 联跑）与命令 2（verify:fast）本轮实跑通过；命令 3/4（verify:full / verify:acceptance）以 r2 证据（exit 0：smoke 18/18、targeted 148/148、build 成功）+ r1 复跑 + 「r1 后产品代码与门成员零变更」+ 本轮关键 targeted spec 复跑（主 spec/visual/dialog 65 用例）确认对当前工作树有效 |
| 6 | E2E 质量证据 | 通过 | red（65 用例 50P/15F，预期失败测试有效）+ green 双份（第一版四命令数据 + r2 修正版五命令）均覆盖 plan-07 且结论正确；B-2 核销后 14 个对齐文件证据链闭合（3 门内随门通过、10 门外实跑全绿、1 helper 经消费方覆盖） |
| 7 | 边界与风险 | 通过 | 5 个前端边界场景全部 `done`（本轮复核未回退）；r1 被部分推翻的「targeted 存量 spec 变红 → 仅更新过时断言」边界行已完全兑现（修复为 mock 前置补全，行为强度保留）；「E2E 不适用」声明正确；B-1 修复已按边界行要求记录于功能文件执行记录与 green 证据 |
| 8 | 契约对齐 | 通过 | 沿承 r1（纯编排收口，无契约改动；GenerationDialog 兼容保留）；r1 后 `src/` 零变更，契约面不可能回退 |
| 9 | 代码审查 | 通过 | r1 唯一正确性 blocker（B-1，位于测试文件）已修复且修复方式最小（单用例 mock 补充 + 注释）；产品代码零 blocker 沿承 r1；5 条建议项沿承（见下），均非阻塞 |

## 验证命令输出摘要（本轮实际执行）

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `pnpm e2e -- e2e/workspace-degradation.spec.ts --project=workspace`（B-1 加验） | 0 | 6 passed / 0 failed（13.2s），原失败用例 L256「生成错误Retry：内联错误主动重试后清除」通过 |
| `pnpm e2e -- e2e/happy-path.spec.ts e2e/workspace-three-column-layout.spec.ts --project=workspace`（B-2 门外抽验） | 0 | 12 passed / 0 failed（18.2s）：happy-path 5 + three-column-layout 7（含 TC-1.5 双视口） |
| `pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts e2e/workspace-generation-dialog.spec.ts e2e/ai-first-visual-regression.spec.ts --project=workspace`（验证命令 1 复跑） | 0 | 65 passed / 0 failed（1.9m）：主 spec 55 + dialog 3 + visual 7 |
| `pnpm verify:fast`（验证命令 2 复跑） | 0 | workflow:check + test:workflow + type-check + lint + Vitest 118 文件 / 1211 用例全绿 |
| 验证命令 3/4（verify:full / verify:acceptance） | —（证据采信） | r2 证据 2026-09-01 执行 exit 0（smoke 18/18 / targeted 148/148 / build 成功）；其后产品代码与两门成员零变更（mtime 取证），本轮已复跑两门中的主 spec/visual/dialog 与 verify:fast 全量 |

注：本轮无 flake；未放宽任何断言。

## blocker 列表

无。

## 建议项列表（沿承 r1，均非阻塞，不影响本轮通过）

1. **DESIGN.md 降级口径更新**：L76「horizontally navigable professional canvas」与 `<1280` 单列堆叠降级的字面张力，建议收尾期把降级口径写回 DESIGN.md。
2. **内联错误文案语言统一**：`generation-submit-error`、rail 文案、live region 播报中文与英文产品口径混排，建议第 15 期收尾统一。
3. **死代码清理**：`output-settings.tsx` L1 排队 UI 与 `decision-panel.tsx` 渲染路径已不被 `src/app` 消费，建议删除或归档。
4. **内联错误文本消毒对齐**：新内联错误位建议复用 `getSafeGenerationMessage` 消毒逻辑。
5. **门外存量 spec 的回归归属**：10 个门外 Workspace 存量 spec 仅手动全量执行（本期即因此漏检 B-1，修复成本被 r1 加验拦截在验收门内），建议收尾期评估将 error-path / degradation 系 / layout 系纳入 targeted 门或 CI 周期性全量档。

## 状态变更记录

| 项 | 变更 |
| --- | --- |
| `plan-07-Workspace闭环集成与回归.md` frontmatter | `status: review` → **`done`**（本轮通过流转） |
| 功能文件验收 checklist | 9 项全部由 `[ ]` 改为 **`[x]`**（实质证据映射见维度 3 与 r1 报告核实记录） |
| README frontmatter | `status: in_execution` → **`accepted`**（plan-01～07 全部非 deprecated 功能均为 `done`，满足 skill accepted 条件） |
| README「## 3. 验收标准追踪矩阵」 | AC-01～AC-07 当前状态 `planned` → **`done`**（checker 对 accepted 状态的硬性要求；各 AC 均已经 plan-01～07 review 证据核实） |
| README「## 5. 开发状态机」 | 验收指令原定不动，但 `pnpm workflow:check` 对 `accepted` 状态强制要求（状态行当前步骤须 `done`、最近证据列须含反引号 `.md` 引用）。按 skill「状态一致性修复：README 展示状态与底层产物不一致时按底层产物修正」执行**最小必要同步**：plan-01～06 最近证据改为反引号 review 路径（第 14 期 accepted README 同款格式）；plan-07 行 `task-review/running` → `done/done`，最近证据指向本报告；更新时间 2026-09-02。同步后 `pnpm workflow:check` 与 `pnpm test:workflow` 复跑通过 |

## 结论摘要

plan-07 第二轮验收**通过**：B-1 以最小测试基建修复核销（实跑 6/6 绿、断言未放宽、与 r1 修复建议一致），B-2 以 r2 修正版证据核销（门归属 3/11 声明与 package.json 比对属实、门外 10 spec 60/60 补录自洽、本轮抽验 3 文件全绿吻合），r1 通过项经「产品代码零变更」取证沿承，维度 2/3/7 复核无回退，三 spec 联跑 65/65 与 verify:fast 全绿确认当前工作区状态。两个 blocker 清零、无新增 blocker。plan-07 流转 `done` 并勾选 checklist；实现计划 7 个功能全部 done，README frontmatter 流转 `accepted`（AC 矩阵与状态机表按 checker accepted 契约做最小必要同步，`pnpm workflow:check` 与 `pnpm test:workflow` 复跑通过）。
