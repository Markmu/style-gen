---
feat_id: plan-01
title: Prompt 编译与画幅基础
review_date: 2026-09-01
reviewer: task-review skill（独立执行 agent）
verdict: pass
---

# plan-01 验收报告：Prompt 编译与画幅基础

- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-01-Prompt编译与画幅基础.md`
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`（`workflow_type: create-dev-plan`）
- red 证据: `docs/e2e/evidence/plan-01-e2e-red-20260901.md`（结论：预期失败，测试有效）
- green 证据: `docs/e2e/evidence/plan-01-e2e-green-20260901.md`（结论：通过）
- 验收前置：plan-01 frontmatter `status: review`，满足 task-review 前置校验。

## 验收结论

**通过（pass）。** 9 个维度全部通过，无 blocker。plan-01 frontmatter `status` 由 `review` 改为 `done`，验收标准 checklist 8 项全部勾选（均已由本 review 逐项验证）。

## 9 个验收维度结果

### 维度 1：文件交付完整性 — 通过

文件清单 9 个文件全部存在且非空（`create` 4 个、`modify` 5 个）：

| 动作 | 路径 | 行数 | 结果 |
| --- | --- | --- | --- |
| modify | `src/types/models.ts` | 520 | 存在，含第 15 期类型区块（L180-261） |
| modify | `src/lib/prompt-composer.ts` | 423 | 存在，含 `composePromptDocument` 实现 |
| modify | `src/lib/__tests__/prompt-composer.test.ts` | 455 | 存在，新增 describe 11 用例 + 既有 2 用例 |
| create | `src/lib/prompt-adjustments.ts` | 204 | 存在且非空 |
| create | `src/lib/__tests__/prompt-adjustments.test.ts` | 305 | 存在且非空 |
| create | `src/lib/generation/aspect-ratio.ts` | 94 | 存在且非空 |
| create | `src/lib/generation/__tests__/aspect-ratio.test.ts` | 113 | 存在且非空 |
| modify | `src/lib/ai/providers/fal-image-gen.ts` | 119 | 存在，portrait 映射 + 调用前拒绝 |
| modify | `src/lib/ai/providers/__tests__/fal-image-gen.test.ts` | 338 | 存在，18 用例 |

`git status` 对照：源代码变更（5 个 modify + 4 个 create/新目录 `src/lib/generation/`，目录内仅含清单 2 个文件）与文件清单完全一致，无清单外源代码改动。文档变更均为 workflow 产物（计划目录、需求调研、red/green 证据），`风险与边界 > 允许修改的额外文件: 无` 未被违反。

### 维度 2：Task 列表完成度 — 通过

7 个 Task 全部 `done`，无 `todo`、无 `waived`，状态枚举合法（`plan.task_step_status`）。说明列与实际交付一致（Task 2/5 red 测试先行的证据见 red 证据文件；Task 7 green 见 green 证据文件与本 review 复跑）。

### 维度 3：验收标准完成度 — 通过（本 review 逐项验证后勾选）

| 验收标准 | 验证方式 | 结果 |
| --- | --- | --- |
| AC-02 三档 enabled invariant ID 集合一致，仅 observation 数与密度变化 | `prompt-composer.test.ts`「同一控制快照下三档的 enabled invariant ID 集合完全一致」等 3 用例 | 已验证通过 |
| AC-02 手动全文命中/未命中由纯函数稳定表达，不覆盖原 Recipe | `applyAdjustmentToCustomText` 4 用例（applied/appended/not_found）+ 不可变返回 | 已验证通过 |
| AC-05 四动作仅更新对应 adjustment，零规则不产生虚假目标 | `applyInvariantAdjustment` 7 用例 + 摘要零规则用例 | 已验证通过 |
| AC-03 优先级/最近比例/并列/1:1 回退全部自动化测试 | `aspect-ratio.test.ts` 14 用例 | 已验证通过 |
| AC-01 `QuickGenerationAuthorizationSnapshot` intent/detail/policy 类型层不可写其他值 | `models.ts` L227-233：`intent: "reconstruction"`、`detailLevel: "standard"`、`aspectRatioPolicy: "reference_or_fallback"` 字面量类型，`generationSettings: Omit<GenerationParams, "aspectRatio">` | 已验证通过 |
| 性能：10 invariants、20 variables 单次 ≤50ms（宽松隔离基线） | `prompt-composer.test.ts` 性能 describe：50 次采样取 median ≤50ms 断言，本次运行通过 | 已验证通过 |
| E2E 不适用声明 + 下游 spec 承接 | 「风险与边界 > E2E 不适用说明」明确声明；README 执行拓扑声明 plan-02/04/05/07 承接 `e2e/workspace-evidence-guided-render-loop.spec.ts` | 已验证（豁免成立） |
| `pnpm type-check` 与 `pnpm verify:fast` 通过 | 本 review 复跑，均 exit 0 | 已验证通过 |

checklist 覆盖功能概要 `关联验收标准 [AC-01, AC-02, AC-03, AC-05]` 全部 4 项，无遗漏、无“人工确认”替代。

### 维度 4：实现规格符合度 — 通过

- **§1 类型与不可变边界**：`CreationPace`、`QuickAuthorization`、`PromptIntent`、`PromptDetailLevel`、`PromptEditorMode`、`InvariantAdjustment`、`PromptControlSnapshot`、`QuickGenerationAuthorizationSnapshot`、`CompiledPromptSegment`、`CompiledPrompt`、`DirectionIterationListItem`、`DirectionIterationFeed` 12 个类型全部落地（`models.ts` L180-261）且与架构 §7.2 命名一致；快照结构扁平、字面量固定、`aspectRatio` 被排除；`applyInvariantAdjustment` 纯函数返回新数组，Recipe/evidence 无写回路径。
- **§2 Prompt 文档编译**：`composePromptDocument(recipe, controls)` 按意图区分 reconstruction 原内容 / same_style 变量模板；三档均编入全部 enabled hard/soft invariants、当前变量与 enabled modifiers；concise 零 observation、standard 每维至多一条 ≥0.7 最高项、professional 全部 ≥0.5 未覆盖项（Recipe 原序）；每个片段生成 sourceKind/sourceId/dimension/startIndex/endIndex；去重按 trim+压缩空格+小写归一；OUTPUT_MAX_LENGTH 截断安全阀保持 segment 范围合法。既有 `composePromptOutputs` / `getPromptTemplateVariables` / `assertKnownStyleDimensions` 导出保留。
- **§3 adjustment 与摘要**：同一 invariant 唯一最新 adjustment（findIndex 覆盖）；strengthen/relax 确定文案后缀、replace trim 后非空且 ≤200（超限抛校验错误）；「保留」取已启用且未 disable 的 invariants（含 soft），「改变」按 trim/压缩空格/忽略大小写比较；自定义全文命中 range 局部替换/删除并做悬空标点清理，未命中非 disable 追加 `Adjustments:`，未命中 disable 返回 `not_found` 且文本不变。
- **§4 画幅算法与 Provider**：`SUPPORTED_ASPECT_RATIOS` 顺序 `1:1, 4:3, 16:9, 3:4, 9:16`；距离 `abs(log(reference/candidate))` 严格小于比较保证并列取数组顺序第一项；来源优先级 restore > user > reference > fallback；无尺寸且无高优值回退 1:1/fallback；fal `FAL_IMAGE_SIZES` 完整映射 `portrait_4_3`/`portrait_16_9`，`toFalImageSize` 在 `client.subscribe` 之前抛 `ImageGenError`（调用前拒绝，不静默回退）。
- **不在范围核查**：无 Workspace 交互组件、sessionStorage v5、GenerationTask 数据库字段/API、结果 rail 的任何实现（git status 无 `src/components`、`src/app`、`src/lib/db` 变更）。

### 维度 5：验证命令执行 — 通过

本 review 于 2026-09-01 复跑 plan-01「验证命令」全部 3 条：

| # | 命令 | 退出码 | 关键输出 |
| --- | --- | --- | --- |
| 1 | `pnpm vitest --run src/lib/__tests__/prompt-composer.test.ts src/lib/__tests__/prompt-adjustments.test.ts src/lib/generation/__tests__/aspect-ratio.test.ts src/lib/ai/providers/__tests__/fal-image-gen.test.ts` | 0 | Test Files 4 passed (4)；Tests 60 passed (60)（14+18+15+13） |
| 2 | `pnpm type-check` | 0 | `tsc --noEmit` 无错误 |
| 3 | `pnpm verify:fast` | 0 | workflow contract + type + lint + 单元/组件测试：Test Files 111 passed (111)；Tests 1046 passed (1046) |

无 warning 阻塞项，结果与 green 证据记录一致。

### 维度 6：E2E 质量证据 — 通过（E2E 豁免成立，red/green 证据齐全）

- red 证据 `docs/e2e/evidence/plan-01-e2e-red-20260901.md` 存在，覆盖 plan-01 全部 4 个测试文件 30 用例，结论为「**预期失败，测试有效**」：13 failed 全部源于目标功能未实现（`composePromptDocument is not a function`、两个新模块 Cannot find package、fal 静默回退行为），17 passed 均为需保持的存量行为。
- green 证据 `docs/e2e/evidence/plan-01-e2e-green-20260901.md` 存在，同组命令 60/60 通过 + type-check + verify:fast exit 0，结论为「**通过**」，且未修改断言/产品代码/frontmatter。
- **E2E 豁免记录（由本 review 确认）**：plan-01 为纯内部共享纯函数/Provider 契约，无用户可观察路径；豁免原因已按 contract 写在功能文件「风险与边界 > E2E 不适用说明」，验收标准另有「E2E 适用性」条目声明用户行为由 plan-02/04/05/07 的 `e2e/workspace-evidence-guided-render-loop.spec.ts` 承接（README 执行拓扑与全局护栏 7 一致声明）。豁免成立，red/green 质量证据由相邻 Vitest 承载（red 证据文件已明确该承载方式）。

### 维度 7：边界与风险 — 通过

5 个后端边界场景全部 `done`，且逐一有自动化测试对应：

| 边界场景 | 状态 | 对应测试 |
| --- | --- | --- |
| adjustment 引用未知 invariant | done | `引用 Recipe 中不存在的 invariantId 时返回校验错误，不产生虚假规则` |
| 同一 invariant 重复 adjustment | done | `同一 invariant 的重复 adjustment 以最后一次显式动作覆盖且只保留一条` |
| 参考尺寸为 0/NaN | done | `参考尺寸为 0 时进入 1:1 回退` / `参考尺寸为 NaN 时进入 1:1 回退` |
| Provider 收到未知画幅 | done | fal 测试未知值（3:2）调用前 reject 且 `mockSubscribe` 不被调用 |
| 旧 composer 调用方 | done | `composePromptOutputs` 既有 2 用例保持通过 |

暂停条件未触发：fal 公开支持列表（`landscape_16_9/landscape_4_3/square/portrait_4_3/portrait_16_9`）完整覆盖架构 5 档，与架构定义无冲突；V2 Recipe invariantId 稳定存在。风险备注「禁止 LLM 二次改写」符合：全部编译路径纯内存、无外部调用。

### 维度 8：契约对齐 — 通过

- 交接上下文声明的 5 个契约对象全部按命名导出：`PromptControlSnapshot`、`QuickGenerationAuthorizationSnapshot`（含 `GenerationParams` Omit 派生）、`CompiledPrompt`/`CompiledPromptSegment`、`InvariantAdjustment`（`src/types/models.ts`），共享画幅常量 `SUPPORTED_ASPECT_RATIOS`/`isSupportedAspectRatio`/`findClosestAspectRatio`/`resolveAspectRatio`（`src/lib/generation/aspect-ratio.ts`）。
- 下游消费方 plan-02/03/04/05 所需的可导入契约（类型 + `composePromptDocument` + `prompt-adjustments` 三个纯函数 + 画幅解析）均已暴露，导出名与 red 证据「测试中固化的新契约」逐字一致，无契约偏差。
- `describeInvariantAdjustment` 从 `prompt-adjustments` 导出并被 composer 与全文回退共用，保证两处文案语义同源。

### 维度 9：代码审查 — 无 blocker，2 条建议项

阻塞项检查：未发现正确性错误（不可变更新、校验先行、截断安全阀、优先级短路均正确）；无安全隐患（无凭据/日志/Prompt 全文新增暴露，纯函数无注入面）；无明显数据损坏风险（adjustment 不写回 Recipe）；无阻断下游的契约错误。

建议项（不阻塞验收）：

1. **可维护性**：`src/lib/ai/providers/fal-image-gen.ts` 的 `toFalImageSize` 用 `(SUPPORTED_ASPECT_RATIOS as readonly string[]).find(...)` 手写了一次白名单查找，语义上等价于已导出的 `isSupportedAspectRatio` 类型守卫；后续可改为直接复用守卫，消除重复实现。
2. **下游消费提示**：`composePromptDocument` 对带 adjustment 的 invariant 会在同一字符范围生成 `invariant` 与 `adjustment` 两个 segment（表达同一文本的双重来源）。这是可追溯性设计意图，但 plan-04/05 做 UI 高亮或删除定位时需按 sourceKind 去重选择，建议下游实现时留意。

## 状态变更记录

| 项 | 变更前 | 变更后 |
| --- | --- | --- |
| `plan-01-Prompt编译与画幅基础.md` frontmatter `status` | `review` | `done` |
| plan-01 验收标准 checklist（8 项） | 全部未勾选 | 全部勾选（本 review 逐项验证后勾选） |
| README「## 5. 开发状态机」表 | 未修改 | 未修改（按主 agent 分工，由主 agent 同步） |
| README frontmatter `status` | `in_execution` | 未修改（plan-02～07 尚未 done，不满足 `accepted` 条件） |

## 复验指引

复跑本报告维度 5 的 3 条命令即可复现全部通过状态；聚焦套件 60/60、`verify:fast` 1046/1046。
