# 架构文档检查报告：Iteration Memory 闭环补全（第 2 轮）

> 检查对象：`13-1-架构文档-Iteration-Memory闭环补全.md`
> 需求基准：`13-0-需求设计-Iteration-Memory闭环补全.md`
> 检查日期：2026-08-17（r2，共 3 轮）
> 检查方法：`.agents/skills/arch-check`（对照 `.agents/contracts/workflow-schema.json` 的 `architecture` 节点）。本轮任务：(a) 逐条验证第 1 轮 7 项修复并做修复回归检查；(b) 以全新视角完整复查结构、需求满足度、一致性、闭环、过度设计与代码事实。

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | frontmatter `input_documents` 第一项唯一指向本次 PRD；另两项（`reviews/prd-doc-check-...-r3.md`、`docs/design/DESIGN.md`）经文件系统核实均存在，不产生配对歧义 | 无 |
| Frontmatter contract | ✅ | 含 `workflow_type: arch-gen`、`status: review_ready`（∈ `wip/review_ready/done`）、`input_documents`、`open_questions` 四个必填字段 | 无 |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 §5.8「待确认问题：无（附决策去向：ADR-2/4/6）」一致 | 无 |
| 实现影响的未决问题 | ✅ | 无未决问题；关键不确定点均已收敛为 ADR 决策 | 无 |

> 本节无 `⛔` 项，不存在前置阻塞。

另核实：schema `architecture.sections` 十个章节名在本文档中全部存在（含本轮修复后的 §9「实施建议与技术选型」）；`scripts/check-workflow-consistency.mjs` 仅校验 new-feature spec 与 plan README，不校验架构文档章节，故章节合规以人工对照为准——对照结果为全部齐备，章节编号顺序（用户流程 3 / 系统上下文 4 / ADR 5）与 arch-gen checklist 自身编号一致。

## 二、第 1 轮修复验证（r2 专项）

| # | 修复项 | 状态 | 证据 |
|---|--------|------|------|
| 1 | §3.3 补「当前查询无匹配」态与「上一条/下一条」支撑；§2.4 AC-02 行补无匹配行动；§9 Phase B 补「搜索无结果」 | ✅ | §3.3 状态机新增 `LNoMatch`（"列表：ready（当前查询无匹配）"）及 `LReady --> LNoMatch` / `LNoMatch --> LLoading : 清除搜索 / 切换筛选` 迁移；关键规则补「保留搜索词与筛选条件，提供清除搜索或切换筛选行动（承接 AC-02 结果项）」与「已完成详情头部的'上一条 / 下一条'顺序切换由视图 store 的选中项与游标栈支撑」；§2.4 AC-02 行「关键链路/状态」列含「无匹配时保留搜索与筛选条件并提供清除 / 切换行动（'当前查询无匹配'态）」；§9 Phase B 第 1 条状态面清单含「搜索无结果」 |
| 2 | §7.2 IterationDetail 增加 `sourceAssetId: string | null`；§6.4 步骤 2 明确保存预填取值与来源缺失禁用保存 | ✅ | §7.2 新增 `sourceAssetId: string | null; // 所引用 analysis task 的来源资产 id；保存预填依赖`；§6.4 步骤 2 明确 `sourceAssetId = detail.sourceAssetId`、注明「既有模板接口仅接受数据库资产 id」，并规定「`sourceAssetId` 缺失（旧记录来源资产不可用）时禁用保存入口并说明来源缺失」。悬空依赖闭合（见 R2-04 的口径备注） |
| 3 | §6.1 步骤 2 明确页面级默认 `status=all`，API 默认 `completed` 仅近期条兼容 | ✅ | §6.1 步骤 2：「页面级默认 `status=all`，满足 PRD'默认看到全部生成尝试'，API 层默认 `completed` 仅服务于近期迭代条兼容」；与步骤 3 API 校验默认值、§4.2 近期条「默认参数（completed-only）」、§7.3 表格三处口径一致，无矛盾 |
| 4 | §9 标题改为「实施建议与技术选型」 | ✅ | §9 标题现为「实施建议与技术选型」，与 schema `architecture.sections` 精确一致；全文无残留「实施方案」字样，无失效交叉引用 |
| 5 | §4.1 补系统上下文 ASCII 框图 | ✅ | §4.1 新增 ASCII 框图（浏览器 → Next.js API 路由 → PostgreSQL / R2），标注「本期无新增二进制写入路径」与两处写入点，图后文字补边界不变声明；与 §7.5 数据边界口径一致 |
| 6 | §2 新增 2.5 P1 预留小节 | ✅ | §2.5「P1 预留：无。……候选演进方向统一见 §10 架构结论，按使用反馈单独立项」；与 §10 演进方向（pg_trgm、推送、标签/批量、资产修复）一致，无矛盾 |
| 7 | §7.3 表后补认证口径说明 | ✅ | §7.3 表后新增「认证口径：上表全部端点沿用 Auth.js session 认证与 userId 归属校验（统一策略见 §8.3）；除近期迭代条以默认参数调用外，无匿名访问路径」，与 §8.3 安全策略表互引一致 |

**修复回归检查**：7 项修复均未引入新矛盾。重点复核：① `LNoMatch` 与 §2.4 AC-02 行、§9 Phase B 三处口径一致（同一行为在不同章节按"状态机 / AC 映射 / 实施范围"分工出现，非重复冗余）；② `sourceAssetId` 与既有代码详情响应 shape 对齐（`GET /api/generation/[id]` completed 分支已返回 `sourceAssetId`，见第六节）；③ 页面级 `status=all` 与 API 默认 `completed` 双口径在 §4.2/§6.1/§7.3 三处表述互洽；④ §2.5 与 §10 无冲突；⑤ §9 改名后无内部引用失效。唯一遗留为状态机图的边完备性细节（R2-01，低）。

## 三、总览

### 3.1 结构完整性（50 项）

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5 | 0 | 0 | 100% |
| 3. 用户流程与状态 | ⚠️ | 2 | 1 | 0 | 67% |
| 4. 系统上下文与模块职责 | ✅ | 3 | 0 | 0 | 100% |
| 5. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| 质量检查项（Q.1-Q.5） | ✅ | 5 | 0 | 0 | 100% |
| **合计** | — | **49** | **1** | **0** | **98%** |

### 3.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖（PRD §1.4 共 11 项） | 11 | 0 | 0 | 100% |
| 3. 用户故事满足（US-01~10） | 10 | 0 | 0 | 100% |
| 4. 业务规则执行（BR 1~17） | 17 | 0 | 0 | 100% |
| 5. 异常与边界覆盖（10 场景） | 10 | 0 | 0 | 100% |
| 6. 成功标准可达性（AC-01~07） | 7 | 0 | 0 | 100% |
| 7. 风险与降级对齐 | 1 | 0 | 0 | 100%* |
| 8. UX 设计支撑（9 交互） | 9 | 0 | 0 | 100% |
| 9. ADR 需求溯源（7 条 ADR） | 7 | 0 | 0 | 100% |
| 10. 范围合规性（8 项不做） | 8 | 0 | 0 | 100% |
| 11. 一致性检查（4 子项） | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 无存疑设计 | — | — | 100% |
| **合计** | **85** | **0** | **0** | **100%** |

\* PRD 无独立「风险清单 / 降级策略」章节；架构 §8.6 五项风险与 §8.2 五级降级链与 PRD 各异常旅程口径一致（与 r1 结论相同）。

深度质量检查（关键维度）：维度 14 闭环完整性 5/5 ✅；维度 15 安全底线 ✅。

与 r1 相比的状态变化（均由修复驱动，本轮逐一复核确认）：

| 检查项 | r1 | r2 | 驱动修复 |
|--------|----|----|---------|
| D5「搜索无结果」场景 | ⚠️ | ✅ | 修复 1 |
| D6 AC-02（无匹配行动子句） | ⚠️ | ✅ | 修复 1 |
| D6 AC-06（来源预填悬空） | ⚠️ | ✅ | 修复 2 |
| D8「上一条 / 下一条」交互 | ⚠️ | ✅ | 修复 1 |
| D11 Schema 与 API 一致性（sourceAssetId） | ⚠️ | ✅ | 修复 2 |
| D11 状态机与流程一致性（页面默认 status） | ⚠️ | ✅ | 修复 3 |
| D14 链路 6.4 闭环 | ⚠️ | ✅ | 修复 2 |
| 结构 2.2 P1 预留 / 4.1 框图 / 7.3 认证 / 9 章节名 / Q.3 | ⚠️×5 | ✅ | 修复 3/4/5/6/7 |
| 结构 3.3 状态机 | ✅ | ⚠️ | r2 新发现（R2-01，图边完备性，文字规则已正确） |

## 四、结构完整性详细检查

### 全绿章节（摘要）

- **Frontmatter（F.1-F.6）**：四必填字段齐备且合法；`input_documents` 三项全部可解析。
- **1. 系统摘要（1.1-1.3）**：3 句核心能力；闭环锚点 **Attempt -> Understand -> Continue**（3 词）；首版定位明确（§1 + 文首边界声明）。
- **2. 范围（5 项全过）**：P0 范围四层清单（§2.1）；P1 预留（§2.5，本轮新增）；明确不做 7 条（§2.2）；成功标准表功能性指标全定性、仅性能类量化（列表 P95 < 500ms / 详情 P95 < 300ms）；AC 承接矩阵 7 行含「AC-ID + PRD 摘要 + 承接模块 + 关键链路/状态 + 风险/降级」五要素。
- **4. 系统上下文（3 项全过）**：ASCII 框图（本轮新增）+ 模块职责表 6 模块（职责/上游/下游齐备）+ §4.3 六项「刻意避免的过度设计」。
- **5. ADR（6 项全过）**：7 条 ADR（4-8 区间），每条含选择/理由/风险对策或演进余地，正文 ≤5 行；§5.8 待确认问题为「无 + 决策去向记录」。
- **6. 运行链路（3 项全过）**：6.1-6.5 五条链路均为编号步骤 + 实现原则，覆盖全部 P0 功能端到端。
- **7. 领域对象（6 项全过）**：核心对象表、最小 Schema（嵌套 ≤2）、API 边界表 + 认证口径（本轮新增）、后端状态机（沿用声明）、数据边界、命名与术语映射。
- **8. NFR（6 项全过）**：性能表、五级降级链、安全表、成本表、2 个新日志事件、五项风险表。
- **9. 实施建议与技术选型（2 项全过）**：Phase A/B/C 条目到文件路径，每 Phase 有独立验证目标。
- **10. 架构结论（2 项全过）**：核心判断 + 演进方向。
- **质量项 Q.1-Q.5**：无 DDL/目录树/环境变量（§6.1 SQL 片段为查询契约、§7.2 interface 为允许例外、§9 文件路径为 9.1 要求）；无重复；一致性无矛盾（Q.3 缺口已由修复 2 闭合）；量化约束达标（ADR 7 / 模块 6 / API 5 / 嵌套 ≤2）；frontmatter 与正文一致。

### 存在 ⚠️ 的章节

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 3.3 | 状态机 | ⚠️ | mermaid 图 + 关键规则齐备，但图内错误态与无匹配态的**出口迁移不完整**（详见 R2-01）：`LError`、`DError` 无出边；`DProcessing --> DClosed`（返回列表）缺失；`LNoMatch` 入边画为 `LReady --> LNoMatch` 而非经 `LLoading`。文字关键规则已正确表达全部行为，属图完备性缺口而非设计缺失 |

## 五、需求满足度详细检查

本轮全维度 ✅，按输出压缩规则以摘要 + 关键证据呈现；r1 报告第四节的长表在未被修复改写的条目上仍有效，可作逐条对照。

### 维度 2：范围覆盖 — 11/11 ✅

PRD §1.4 十一项逐一承接，证据同 r1（§4.2/§6.1-6.5/§7.2/ADR-1/5/6/7）；本轮修复使范围(4)视图保活（§6.1 步骤 2 页面默认 + ADR-6）与范围(10)保存闭环（§6.4 步骤 2 + §7.2 sourceAssetId）的证据链闭合。

### 维度 3：用户故事满足 — 10/10 ✅

US-01/02 → §6.1；US-03 → §6.2 + §7.2；US-04/05/06 → §6.3；US-07 → §6.4；US-08 → §3.3 DProcessing + §6.5；US-09 → ADR-6 + §3.3 关键规则；US-10 → §3.2 未登录分支 + §8.2。全部操作路径在 §3.1/§6 可逐步走通。

### 维度 4：业务规则执行 — 17/17 ✅

r1 的 17 条逐条映射全部维持；修复影响的规则复核：规则 4（搜索范围，§6.1 步骤 4 ILIKE + 名称联表）、规则 13（工作区保护三豁免，§6.3 步骤 2）、规则 14（保存预填「来源/提示/变量」三件套齐备 + 名称必填，§6.4 步骤 2）、规则 15（重复保存已保存态，ADR-5 + §4.2）。规则 9 的来源缺失现在有 `sourceAssetId`/`sourceImageUrl` 双字段口径支撑。

### 维度 5：异常与边界覆盖 — 10/10 ✅（r1 为 9✅/1⚠️）

「搜索无结果」由 ⚠️ 转 ✅：§3.3 `LNoMatch` 态 + 关键规则（保留搜索词与筛选条件、清除/切换行动）+ §2.4 AC-02 行 + §9 Phase B 状态面清单，四证据齐备，与 PRD §3.2「搜索无结果」行（保留条件 + 清除搜索或切换筛选）逐要素对齐。其余 9 个场景（进行中、失败、列表 5xx、单条 5xx/404、未登录、首次空态、来源缺失、保存冲突 409、轮询失败熔断）证据同 r1，未回退。

### 维度 6：成功标准可达性 — 7/7 ✅（r1 为 5✅/2⚠️）

| AC | 状态 | 本轮复核证据 |
|----|------|-------------|
| AC-01 | ✅ | §2.4 矩阵、§6.1 status=all 三态条目、无结果状态面不假图；近期条行为不回退有 §4.2 默认参数 + 代码事实（`listCompleted`）双重支撑 |
| AC-02 | ✅（升） | §2.4 行含「无匹配时保留搜索与筛选条件并提供清除 / 切换行动（'当前查询无匹配'态）」，AC 结果句四个子句（组合筛选命中 / 较早记录可访问 / 返回保位 / 无匹配行动）全部承接 |
| AC-03 | ✅ | §6.2 逐字段组装算法 + §7.2 区分 recipe/prompt/variables/negativePrompt/params/modelName；缺失标记三值 |
| AC-04 | ✅ | §6.2/§6.5 轮询迁移、失败说明 +「修正并继续」；「不展示虚假结果」由 resultFileUrl 仅 completed 非空 + 状态面保证。「无重复提交入口」子句为隐式满足（processing 详情设计仅含返回列表，见 R2-03 建议明示） |
| AC-05 | ✅ | §6.3 三豁免守卫 + 替换确认 + 恢复载荷逐字段（含上一轮结果）+ 步骤 5「不发出任何生成或写请求」 |
| AC-06 | ✅（升） | §6.4 步骤 2 预填四要素（content/variables/sourceAssetId/sourceGenerationTaskId）+ 名称必填；§7.2 `sourceAssetId` 字段闭合悬空依赖；服务端校验（§6.4 步骤 3）与客户端门槛（步骤 1）双层强制 |
| AC-07 | ✅ | §3.2 分支表 + §8.2 降级链；401 不清空本地工作台状态；「登录后回原入口」「重试后回原入口」在状态面行动中体现 |

功能性指标全定性、性能类量化合规的目标值与 §2.3/§8.1 一致；无 AC 被弱化或改写。

### 维度 7：风险与降级对齐 — ✅（摘要，同 r1）

架构 §8.6 五项风险（ILIKE 性能、存量无快照、守卫误判、轮询读放大、模板名搜索局限）与 §8.2 五级降级链均与 PRD 异常旅程对齐；「模板名搜索依赖 sourceTemplateId」局限与 PRD 规则 4 语义吻合。

### 维度 8：UX 设计支撑 — 9/9 ✅（r1 为 8✅/1⚠️）

「上一条 / 下一条」由 ⚠️ 转 ✅：§3.3 关键规则明确「由视图 store 的选中项与游标栈支撑顺序切换，切换详情同样不重置列表」，承接 PRD §3.1 已完成详情线框头部导航。其余 8 项交互（近期条入口、搜索 + 四筛选、加载较早、列表行字段、详情分区、替换确认对话框、保存/已保存态、三态详情变体）证据同 r1，未回退。

### 维度 9：ADR 需求溯源 — 7/7 需求驱动 ✅

ADR-1 ← PRD §1.3 Iteration 定义 / 规则 1-2；ADR-2 ← §1.4(6)「当时采用的」/ 规则 8-9；ADR-3 ← 规则 2 近期条行为保持；ADR-4 ← §1.3 核心继续动作 / 规则 11-13；ADR-5 ← 规则 15 / AC-06；ADR-6 ← §1.4(4) / AC-02 / 规则 16；ADR-7 ← AC-04 / 规则 10。无孤儿决策。

### 维度 10：范围合规性 — 8/8 ✅（摘要，同 r1）

PRD「明确不做」8 项在架构中均无引入；§2.2 逐项复述排除；§10 演进项明确标注为后续单独立项。

### 维度 11：一致性检查 — 4/4 ✅（r1 为 2✅/2⚠️）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | 7 条 ADR 互相支撑无矛盾（同 r1） |
| Schema 与 API 一致性 | ✅（升） | `sourceAssetId` 已入 §7.2，与 §6.4 步骤 2、§6.2 步骤 3 联表（analysis task 活引用）三处对齐；列表 DTO 与 §6.1 步骤 5 对齐 |
| 状态机与流程一致性 | ✅（升） | 页面级默认 `status=all` 与 API 默认 `completed` 双口径在 §4.2/§6.1/§7.3 互洽，消除「左侧导航直达违背 PRD 默认全量」的隐患 |
| 范围与设计一致性 | ✅ | §2.1 四层范围均有 ADR / 模块 / 链路支撑（同 r1） |

### 维度 13：过度设计检测 — 无存疑设计 ✅（摘要，同 r1）

模块 6、API 5（全复用既有端点）、零新增表 / 外部依赖 / 推送 / 检索引擎；§4.3 主动列出六项避免项。超出 PRD 字面的设计（promptSummary 截断、URL 同步、10s 列表重拉、复合索引、2 个日志事件）均可溯源。

## 六、代码事实一致性抽查（r2 复核）

本轮对任务指定的代码面独立复核（不依赖 r1 结论），全部属实：

| 架构声明 | 代码事实 | 结论 |
| --- | --- | --- |
| Iteration 复用 `generation_tasks`，状态四值 pending/processing/completed/failed（§7.4/§7.6） | `src/lib/db/schema.ts` L126-166：表、状态 check（L154-156）完全一致；`recipe_snapshot`/`variables_snapshot`/`source_template_id` 尚不存在，属本期新增，描述准确 | ✅ |
| `templates` 新增 `source_generation_task_id` 为加法变更（§2.1/ADR-5） | schema L169-191：现表无该列；已有 `source_asset_id`（L176）与三个索引，加列可行 | ✅ |
| 「所引用 analysis task 的来源资产」真实存在且支撑 §6.4/§7.2 `sourceAssetId`（修复 2 的取值来源） | `analysis_tasks.sourceAssetId` NOT NULL 引用 assets（schema L72-74）；`generation_tasks.analysisTaskId` NOT NULL（L130-132）；既有 `findByIdWithRecipe` 已联表返回 `sourceAssetId`/`sourceImageUrl`（repository L242-243、L274-275），且 `GET /api/generation/[id]` completed 分支已输出 `sourceAssetId`（route L54） | ✅ |
| 近期条走默认参数 completed-only、条目 shape 超集向后兼容（§4.2/ADR-1/§7.3） | `src/hooks/use-history-list.ts` L39 仅传 `pageSize=20`；`GET /api/generation` 调 `listCompleted`（route L44），现返回 `{id, resultFileUrl, createdAt}` + nextCursor，新增 status/promptSummary/params 为加法 | ✅ |
| API 默认 `completed` 仅近期条兼容、页面级 `status=all`（§6.1，修复 3） | 仓库 `listCompleted` 固定 `eq(status, 'completed')`（L174）；`pageSize` clamp [1,50]（route L34-41 与仓库 L170 双层）与 §6.1 步骤 3 一致 | ✅ |
| 「复用既有历史恢复模式」（§2.1/ADR-4） | `src/hooks/use-history-restore.ts` 存在（restore 回调 L112+）；`src/app/workspace/page.tsx` L105-106 已接线 `useHistoryList`/`useHistoryRestore` | ✅ |
| `GET /api/generation/[id]` 扩展为全状态详情、既有消费方向后兼容（§7.3） | 现实现 completed 走 `findByIdWithRecipe`、非 completed 返回通用 shape 含 errorMessage（route L40-87）；扩展为加法变更 | ✅ |
| `POST /api/templates` 同名 409、限流 30 次/小时/IP、仅接受 DB 资产 id（§6.4/§8.3，修复 2 的「既有模板接口仅接受数据库资产 id」依据） | `src/app/api/templates/route.ts`：`TEMPLATE_NAME_CONFLICT` 409（L184-192）、滑动窗口 30/h/IP（L30-52）、仅传 `sourceImageUrl` 无 `sourceAssetId` 返回 400「Source image URL requires a database-backed source」（L230-235） | ✅ |
| `/api/templates/[id]` GET 无变更复用（§7.3） | `src/app/api/templates/[id]/route.ts` 存在（另有 duplicate 子路由） | ✅ |
| store 挂载 workspace layout（ADR-6）；左侧导航追加 Iterations 项（§4.2） | `src/app/workspace/layout.tsx` 首行 `"use client"`，可承载客户端 store；`left-sidebar.tsx` navItems 现有 /workspace 与 /workspace/templates 两项（L14-26），追加可行；`/workspace/templates` 页面存在，「打开」跳转目标有效 | ✅ |
| §7.2 引用类型与 ULID/列命名/错误体/状态迁移触发点沿用（§7.6/§8.2/§7.4） | `src/types/models.ts` L160 `StoredVisualRecipe`、L231 `GenerationParams`、L258 `TemplateVariable`；`{error, code, retryable}` 响应结构、`executeSyncGeneration`/webhook/`startTimeoutTimer`（generation route L128-305）均核实 | ✅ |

## 七、新发现问题清单（r2）

本轮无高、中严重度问题；4 条低严重度润色项如下。无前置阻塞项、无未满足需求、无存疑设计。

| 编号 | 严重度 | 所在章节 | 问题描述 | 具体修复建议 |
|------|--------|---------|---------|-------------|
| R2-01 | 低 | §3.3 状态机 | 图内错误态与无匹配态的出口迁移不完整：① `LError` 无出边（重试应回 `LLoading`）；② `DError` 无出边（§3.2 分支表承诺「详情位给出重试或关闭」，应有 `DError --> DLoading` 与 `DError --> DClosed`）；③ `DProcessing --> DClosed`（返回列表）缺失，而 PRD 进行中旅程明确「用户可以返回列表」且线框含 [返回列表]；④ `LNoMatch` 入边画为 `LReady --> LNoMatch` 直连，与「`LReady --> LLoading : 变更筛选`」的加载语义跳跃，更准确应为 `LLoading --> LNoMatch : 查询后无匹配`。文字关键规则已正确表达全部行为，实现不致歧义，属图完备性缺口 | 在 §3.3 mermaid 补 4-5 条边：`LError --> LLoading : 重试`、`DError --> DLoading : 重试`、`DError --> DClosed : 关闭`、`DProcessing --> DClosed : 返回列表（视图状态保留）`，并将无匹配入边调整为 `LLoading --> LNoMatch : 查询后无匹配`（可保留 `LReady` 直连边作为语义速记或删除） |
| R2-02 | 低 | §6.1 步骤 4 | SQL 契约片段存在排版笔误：`(prompt_snapshot ILIKE '%q%' OR templates.name ILIKE '%q%)` 中第二个匹配串引号未闭合（应为 `'%q%'` 后再闭括号） | 改为 `(prompt_snapshot ILIKE '%q%' OR templates.name ILIKE '%q%')` |
| R2-03 | 低 | §2.4（AC-04 行）/ §3.3 | PRD AC-04 结果子句「不展示虚假结果或重复提交入口」中的**无重复提交入口**未在架构任何位置显式声明。当前为隐式满足：processing 详情变体仅含返回列表动作（PRD 线框），§6.2 原则只覆盖非 completed 上下文返回。若实现者在 processing 详情自行加「再次生成」入口，架构层无约束拦截 | 在 §2.4 AC-04 行「关键链路 / 状态」列或 §3.3 关键规则补一句：「processing 详情不提供生成 / 重复提交动作，仅返回列表与等待」，把隐式约束显式化 |
| R2-04 | 低 | §6.4 步骤 2 | 「`sourceAssetId` 缺失时禁用保存入口并说明来源缺失」是架构新增的产品口径，PRD 未定义该情形（规则 9 只要求标记缺失、保留内容可继续；规则 14 / AC-06 的门槛是 completed + 有真实结果）。该决策由代码事实倒逼（`POST /api/templates` 仅接受 DB 资产 id，否则 400），属合理扩展；但鉴于 `analysisTaskId` 与 `analysis.sourceAssetId` 在数据库均为 NOT NULL，该分支实际罕见（属防御性设计），建议明示口径来源，避免验收阶段被当作与 PRD 的偏差 | 在 §6.4 步骤 2 或 §8.6 风险表补一句口径说明：「禁用为防御性设计，受既有模板接口只接受数据库资产 id 约束（见 §7.3）；正常数据下 `sourceAssetId` 恒非空」，并按 §8.6 既有惯例在交接文档中明示 |

### 前置阻塞项 / 未满足需求 / 存疑设计

均无。

## 八、总结

- **结构完整度**：98%（49 通过 / 1 部分 / 0 缺失，共 50 检查项；唯一下降项为 R2-01 状态机图边完备性）
- **需求满足度**：100%（85 满足 / 0 部分满足 / 0 未满足；深度维度 14 为 5/5 ✅、维度 15 ✅）
- **第 1 轮修复验证**：7/7 全部落实，且未引入回归矛盾
- **整体评价**：✅ **架构充分满足需求，可进入开发计划阶段（create-dev-plan）**
  - 无前置阻塞（配对可靠、frontmatter 合法、open_questions 同步、无影响实现的未决问题）
  - 范围内所有需求（11 范围项 / 10 用户故事 / 17 业务规则 / 10 异常场景 / 7 条 AC / 9 UX 交互）全部 ✅；关键维度（6 成功标准 / 11 一致性 / 14 闭环 / 15 安全底线）无 ❌、无 ⚠️
  - r1 的 8 个问题（P-01~P-08）全部闭合；r2 新发现的 4 条均为低严重度文档润色项，不构成进入下一阶段的阻塞
- **优先修复建议**（可在进入 create-dev-plan 前顺手处理，或留待第 3 轮确认）：
  1. R2-01：补齐 §3.3 状态机错误态 / 无匹配态 / 进行中返回的出边，消除图与文字规则的完备性差
  2. R2-03：显式声明 processing 详情「无重复提交入口」，完整承接 AC-04 子句
  3. R2-04：为「sourceAssetId 缺失禁用保存」补充口径来源说明（代码事实 + 防御性定位）
  4. R2-02：修正 §6.1 SQL 片段引号笔误
