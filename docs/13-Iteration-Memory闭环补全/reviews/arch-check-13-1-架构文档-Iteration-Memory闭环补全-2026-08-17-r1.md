# 架构文档检查报告：Iteration Memory 闭环补全（第 1 轮）

> 检查对象：`13-1-架构文档-Iteration-Memory闭环补全.md`
> 需求基准：`13-0-需求设计-Iteration-Memory闭环补全.md`
> 检查日期：2026-08-17（r1，共 3 轮）
> 检查方法：`.agents/skills/arch-check`（对照 `.agents/contracts/workflow-schema.json` 的 `architecture` 节点），并按任务要求附加代码事实一致性抽查。

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | 架构 frontmatter `input_documents` 第一项唯一指向本次 PRD（`docs/13-Iteration-Memory闭环补全/13-0-需求设计-Iteration-Memory闭环补全.md`），另两项（PRD 检查报告 r3、`docs/design/DESIGN.md`）均实际存在，不产生配对歧义 | 无 |
| Frontmatter contract | ✅ | 含 `workflow_type: arch-gen`、`status: review_ready`（∈ `wip/review_ready/done`）、`input_documents`、`open_questions` 四个必填字段 | 无 |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 §5.8「待确认问题：无」一致；§5.8 同时给出决策去向记录（ADR-2/4/6） | 无 |
| 实现影响的未决问题 | ✅ | 无未决问题；关键不确定点（存量回退、守卫判定、store 生命周期）均已收敛为 ADR 决策 | 无 |

> 本节无 `⛔` 项，不存在前置阻塞。

## 二、总览

### 2.1 结构完整性

对照 schema `architecture.sections` 十个章节与 arch-check checklist 逐项检查（含 Frontmatter 6 项 + 各章节子项 + 跨章节质量项，共 50 项）。

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ⚠️ | 4 | 1 | 0 | 90% |
| 3. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 4. 系统上下文与模块职责 | ⚠️ | 2 | 1 | 0 | 83% |
| 5. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ⚠️ | 5 | 1 | 0 | 92% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ⚠️ | 1 | 1 | 0 | 75% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| 质量检查项（Q.1-Q.5） | ⚠️ | 4 | 1 | 0 | 90% |
| **合计** | — | **45** | **5** | **0** | **95%** |

十个 schema 章节全部存在、语义齐备；⚠️ 项均为局部形式问题（详见第三节与第六节 P-02/P-05/P-06/P-07/P-08），不影响架构实质内容。

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖（PRD §1.4 共 11 项） | 11 | 0 | 0 | 100% |
| 3. 用户故事满足（US-01~10） | 10 | 0 | 0 | 100% |
| 4. 业务规则执行（BR 1~17） | 17 | 0 | 0 | 100% |
| 5. 异常与边界覆盖（10 场景） | 9 | 1 | 0 | 95% |
| 6. 成功标准可达性（AC-01~07） | 5 | 2 | 0 | 86% |
| 7. 风险与降级对齐 | 1 | 0 | 0 | 100%* |
| 8. UX 设计支撑（9 交互） | 8 | 1 | 0 | 94% |
| 9. ADR 需求溯源（7 条 ADR） | 7 | 0 | 0 | 100% |
| 10. 范围合规性（8 项不做） | 8 | 0 | 0 | 100% |
| 11. 一致性检查（4 子项） | 2 | 2 | 0 | 75% |
| 13. 过度设计检测 | 1 | 0 | 0 | 100% |
| **合计** | **79** | **6** | **0** | **96%** |

\* PRD 无独立「风险清单 / 降级策略」章节（第 5 部分为决策记录）；架构 §8.6/§8.2 主动识别的风险与降级链（含存量快照缺失、ILIKE 性能、守卫误判、轮询放大、模板名搜索局限）与 PRD 各异常旅程对齐，记为满足。

深度质量检查（关键维度）：维度 14 闭环完整性 4✅/1⚠️（见第五节）；维度 15 安全底线 ✅（PRD 无合规底线章节，架构 §8.3 主动覆盖认证、输入收敛、注入、限流）。

## 三、结构完整性详细检查

### 全绿章节（摘要）

- **Frontmatter（F.1-F.6）**：`workflow_type: arch-gen`、`status: review_ready` 合法；`input_documents` 三项全部可解析且唯一回溯到本次 PRD；`open_questions: []` 与 §5.8 同步。
- **1. 系统摘要（1.1-1.3）**：核心能力 3 句概括（§1 首段）；核心闭环锚点 **Attempt -> Understand -> Continue**（3 词，§1）；首版定位由 §1「本期……」与文首斜体边界声明共同界定。
- **3. 用户流程与状态（3.1-3.3）**：主流程覆盖 PRD 主旅程 10 步（§3.1）；关键分支表 7 条，含入口/触发条件与架构处理（§3.2）；mermaid 状态机含列表/详情两组正交状态 + 关键规则说明（§3.3）。
- **5. 关键架构决策（5.1-5.6）**：7 条 ADR（数量在 4-8 区间），每条含选择/理由/风险对策或演进余地，正文均 ≤5 行；§5.8 待确认问题为「无 + 决策去向」。
- **6. 运行链路（6.1-6.3）**：5 条链路均为编号步骤 + 「实现原则」段落；覆盖全部 P0 功能端到端流程（检索、详情、恢复、保存、轮询）。
- **8. 非功能需求（8.1-8.6）**：性能目标表（仅性能类量化，功能性指标全部定性，符合 2.4 规则）、降级链按影响分级、安全策略表、成本表、可观测性最小方案（2 个新日志事件）、风险表齐备。
- **10. 架构结论（10.1-10.2）**：核心判断（读模型扩展 + 快照 + 客户端恢复，零新增表/端点/外部依赖）与演进方向明确。

### 存在 ⚠️ 的章节

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 2.2 | P1 预留 | ⚠️ | §2 只有「范围 / 明确不做」，无 P1 预留小节；演进候选仅出现在 §10 演进方向。PRD 本身无 P1 清单，属形式缺口（→P-07） |
| 4.1 | 系统上下文 | ⚠️ | §4.1 为纯文字描述（边界、依赖、变更点均说明），缺 checklist 要求的 ASCII 框图（→P-06） |
| 7.3 | API 边界 | ⚠️ | 表格列为「接口/方法/用途/变更与字段来源」，无 checklist 要求的「认证要求」列；认证统一在 §8.3 说明，信息不缺失但形式不符（→P-08） |
| 9（章节级） | 章节命名 | ⚠️ | schema `architecture.sections` 要求「实施建议与技术选型」，文档 §9 标题为「实施方案」；内容（Phase A/B/C + 文件路径 + 验证目标）完整（→P-05） |
| Q.3 | 一致性 | ⚠️ | Schema 与链路存在一处字段缺口：§6.4 保存预填依赖「分析来源资产（sourceAssetId）」，但 §7.2 IterationDetail 未定义该字段（→P-02，详见维度 11） |

其余检查项（Q.1 无 DDL/目录树/环境变量——§6.1 的 SQL 片段为查询契约非 DDL、§7.2 interface 属允许例外；Q.2 无重复；Q.4 ADR 7 条、模块 6 个、API 5 个、Schema 嵌套 ≤2；Q.5 frontmatter 与正文一致）全部通过。

## 四、需求满足度详细检查

### 维度 2：范围覆盖 — 11/11 ✅（摘要）

PRD §1.4「本期范围」11 项逐条有架构承接：①近期条入口（§4.2 入口与沉淀、§6.1 步骤1）②全状态展示（§6.1 status=all、ADR-1）③搜索 + 四种筛选（§6.1 `q`+`status` 白名单 all/processing/completed/failed）④浏览较早 + 视图保活（§6.1 步骤5-6、ADR-6）⑤列表项字段（§7.2 IterationListItem：状态/摘要/设置/时间/预览或占位）⑥完成详情全上下文（§7.2 IterationDetail + §6.2 组装算法）⑦进行中/失败详情（§3.3 详情三态、§6.2 原则3）⑧继续此方向（§6.3）⑨替换确认（§6.3 步骤2-3、ADR-4）⑩保存/已保存（ADR-5、§6.4）⑪全异常态说明（§3.2 分支表 + §8.2 降级链）。

### 维度 3：用户故事满足 — 10/10 ✅（摘要）

US-01~07 核心故事分别落在 §6.1（US-01/02）、§6.2（US-03）、§6.3（US-04/05/06）、§6.4（US-07）；US-08（进行中阶段）落 §3.3 DProcessing + §6.5；US-09（关闭详情保位）落 ADR-6 + §3.3 关键规则；US-10（登录/服务问题保留说明）落 §3.2 未登录分支 + §8.2。操作路径在 §3.1/§6 链路中均可逐步走通。

### 维度 4：业务规则执行 — 17/17 ✅（摘要）

逐条核对 PRD §3.3 规则 1-17，均有具体执行机制而非仅提及：规则1/2（ADR-1、§6.1 默认 completed + §4.2 近期条 completed-only）；规则3（§6.1 ORDER BY created_at DESC, id DESC + ADR-6）；规则4（§6.1 `prompt_snapshot ILIKE OR templates.name ILIKE` 联表）；规则5（status 白名单 + q/status 同发）；规则6（resultFileUrl 仅 completed 非空，其余状态面）；规则7（服务端截断 120 字符）；规则8（IterationDetail 区分 recipe/prompt/variables/negativePrompt/params/modelName）；规则9（recipeSource/variablesSource/sourceImageUrl 三值标记 + ADR-2 对策）；规则10（§6.5 轮询迁移）；规则11（§6.3 步骤1「修正并继续」同链路）；规则12（§6.3 步骤4 恢复载荷逐字段 + 上一轮结果）；规则13（§6.3 步骤2 三豁免守卫纯函数）；规则14（§6.4 completed + resultAssetId 门槛与预填）；规则15（ADR-5 反查最新 + §4.2 已保存态/打开入口）；规则16（ADR-6 store 生命周期覆盖 /workspace/*）；规则17（§8.2 降级链逐级说明保留内容与下一步）。

### 维度 5：异常与边界覆盖 — 9 ✅ / 1 ⚠️

| PRD 异常场景 | 架构错误处理 | 状态 | 证据 | 差距说明 |
|-------------|------------|------|------|---------|
| 任务仍在进行 | 三态详情 + 5s 轮询，迁移后原地切换 | ✅ | §6.2 步骤6、§6.5 | — |
| 生成失败 | 失败说明 + 保留上下文 + 修正并继续 | ✅ | §6.2 步骤5、§6.3 | — |
| 完整列表无法加载 | 5xx → 说明工作台不受影响 + 重试/返回 | ✅ | §3.2、§8.2 降级4 | — |
| 单条详情不可用 | 5xx/404 → 列表与视图状态不动，详情位重试/关闭 | ✅ | §3.2、§8.2 降级3 | — |
| 未登录 | 401 → 登录引导，本地工作台状态不清理，登录后回原入口 | ✅ | §3.2、§8.2 | — |
| 首次进入无记录 | LEmpty「引导首次创作」 | ✅ | §3.3 | — |
| **搜索无结果** | 状态机未区分「无匹配」与「无记录」；未承接 PRD「保留搜索词和筛选条件 + 清除搜索或切换筛选行动」 | ⚠️ | §3.3 LEmpty 仅标注「无记录（引导首次创作）」；§9 Phase B 状态面清单亦只有「未登录/空态/加载/错误」 | 缺「筛选后空集」子状态及其行动设计（→P-01） |
| 来源缺失（旧记录） | 快照缺失 → 活引用回退 + 显式 missing 标记 | ✅ | ADR-2、§6.2 步骤4 | — |
| 保存冲突（同名） | 沿用既有模板接口 409 处理 | ✅ | §6.4 步骤3-4（代码事实已核实 `TEMPLATE_NAME_CONFLICT`/409） | — |
| 轮询失败 | 连续 3 次失败停轮询 + 重试 + 保留内容 | ✅ | §6.5 步骤3 | — |

### 维度 6：成功标准可达性 — 5 ✅ / 2 ⚠️

| PRD 验收指标 | 架构目标值 | PRD 目标值 | 状态 | 证据 | 差距说明 |
|-------------|-----------|-----------|------|------|---------|
| AC-01 完整页面可达且覆盖全部状态 | 全状态列表 + 三态条目 + 状态面，近期条行为不回退 | 同左 | ✅ | §2.4 矩阵、§6.1 | — |
| AC-02 查找与连续浏览 | q+status 组合、游标加载较早、返回保位 | 同左 + **无匹配项时提供清除搜索或切换筛选的行动** | ⚠️ | §2.4 矩阵、ADR-6、§6.1 | 矩阵行未承接句尾「无匹配行动」子句；状态机无无匹配态（→P-01） |
| AC-03 已完成详情完整上下文 | 并排媒体 + 快照优先/回退/缺失标记，逐字段区分 | 同左 | ✅ | §6.2、§7.2 | — |
| AC-04 进行中与失败确定感 | 三态详情 + 轮询迁移 + 无虚假结果 | 同左 | ✅ | §6.2、§6.5、§3.3 | — |
| AC-05 完整恢复且不误覆盖 | 守卫三豁免 + 替换确认 + 恢复载荷逐字段 + 不自动生成 + 上一轮保留 | 同左 | ✅ | §6.3、ADR-4 | — |
| AC-06 沉淀为 Style Memory | completed+resultAssetId 门槛、预填、反查已保存态 | 保存流程以**该次迭代的来源**、可复用提示和变量为起点 | ⚠️ | §6.4、ADR-5 | §6.4 步骤2 预填需「分析来源资产」，但 §7.2 IterationDetail 无 `sourceAssetId` 字段，来源预填存在悬空依赖（→P-02） |
| AC-07 空态/登录/服务异常不破坏上下文 | 401/5xx/404 分级处理 + 保留说明 | 同左 | ✅ | §3.2、§8.2 | — |

功能性指标全部定性、性能类量化合规（§2.3 列表 P95 < 500ms / 详情 P95 < 300ms，仅性能量化，符合规则）；无任何 AC 被弱化或改写口径。

### 维度 7：风险与降级对齐 — ✅（摘要）

PRD 无独立风险/降级章节。架构 §8.6 五项风险（ILIKE 性能、存量无快照、守卫误判、轮询读放大、模板名搜索局限）与 §8.2 五级降级链均与 PRD 异常旅程口径一致，且「模板名搜索依赖 sourceTemplateId 传递」的局限与 PRD 规则 4「当记录来自已命名的 Style Memory 时」语义吻合。

### 维度 8：UX 设计支撑 — 8 ✅ / 1 ⚠️

| PRD UX 交互（§3.1 线框） | 架构状态/接口 | 状态 | 证据 |
|--------------------------|-------------|------|------|
| 近期迭代条：缩略图打开详情 / 「查看全部」 | onViewAll 接线 + 链接 `/workspace/iterations?status=all`；默认参数兼容 | ✅ | §4.2 关键交互链路 |
| 完整页面：搜索框 + 四筛选 | `q`/`status` 参数 + 视图 store + URL 同步 | ✅ | §6.1、ADR-6 |
| 继续浏览较早记录 | keyset 游标 + 游标栈 | ✅ | §6.1 步骤5-6 |
| 列表行（预览/状态/摘要/时间/设置） | IterationListItem DTO + 视图模型 | ✅ | §7.2、§4.2 |
| 详情上下文分区（证据/提示/变量/设置） | IterationDetail 逐字段 + DTO→视图模型 | ✅ | §7.2、§6.2 |
| **详情头部「上一条 / 下一条」导航** | 未承接 | ⚠️ | PRD §3.1 已完成详情线框头部含「上一条 下一条」，架构全文无对应设计（→P-03） |
| 替换确认对话框（两侧摘要/取消/继续切换） | 守卫 confirm 分支 + 对话框两文本槽 + 零变更取消 | ✅ | §4.2 关键交互链路、§6.3 步骤3 |
| 保存为 Style Memory / 已保存态 + 打开 | completed+资产门槛、savedTemplate 反查、跳转 `/workspace/templates` 定位 | ✅ | §4.2、§6.4、ADR-5 |
| 进行中/失败详情返回列表、修正并继续 | 详情三态变体 + 同一恢复链路 | ✅ | §6.3 步骤1、§3.3 |

### 维度 9：ADR 需求溯源 — 7/7 需求驱动 ✅（摘要）

ADR-1（复用 generation_tasks）← PRD §1.3 Iteration 定义、§3.3 规则1；ADR-2（提交时快照）← PRD §1.4(6)「当时采用的」、规则8/9；ADR-3（扩展既有端点）← 规则2 近期条行为保持 + 工程防漂移；ADR-4（客户端恢复）← §1.3 核心继续动作、规则12/13；ADR-5（templates 反向关联）← 规则15、AC-06；ADR-6（视图状态保活）← §1.4(4)、AC-02、规则16；ADR-7（轮询复用）← AC-04、规则10。无孤儿决策。

### 维度 10：范围合规性 — 8/8 ✅（摘要）

PRD「明确不做」8 项（纯分析记录、批量生成、批量/删除/导出/保留期、自动评分/AI 审计、色卡等新集成、Style Memory 模型改动、公开分享/协作/商业化、移动端重做）在架构中均无对应设计引入；§2.2 逐项复述排除，§10 演进方向中的标签/批量操作明确标注为后续单独立项，不属本期设计。

### 维度 11：一致性检查 — 2 ✅ / 2 ⚠️

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | 7 条 ADR 互相支撑（快照 ADR-2 ↔ 回退算法 §6.2；客户端恢复 ADR-4 ↔ 守卫 §6.3；反查 ADR-5 ↔ 保存 §6.4），无矛盾 |
| Schema 与 API 一致性 | ⚠️ | 列表条目 DTO 与 §6.1 步骤5 完全对齐；详情 DTO 与 §6.2 对齐，**唯独 §6.4 步骤2 依赖的 `sourceAssetId` 未出现在 §7.2 IterationDetail**（→P-02） |
| 状态机与流程一致性 | ⚠️ | §3.3 页面状态机与 §6 链路一致（轮询迁移、视图保活、错误可重试）；但**页面级默认 status 未写明**——API 默认 completed（近期条兼容，§6.1 步骤3），左侧导航直达且 URL/store 均无 status 时若沿用 API 默认，将违背 PRD §2.1 主旅程第 2 步「默认看到最近的所有生成尝试」（→P-04） |
| 范围与设计一致性 | ✅ | §2.1 范围四层（数据/读/写/前端）均有 ADR、模块与链路支撑，无悬空范围项 |

### 维度 13：过度设计检测 — 无存疑设计 ✅（摘要）

模块 6 个、API 5 个（全部复用既有端点）、零新增表/外部依赖/推送/检索引擎；§4.3 主动列出 6 项「刻意避免的过度设计」并给出理由。超出 PRD 字面的设计（promptSummary 服务端截断、URL 同步、10s 列表低频重拉、复合索引、2 个日志事件）均可追溯到 BR-7/AC-02/BR-10/性能目标/§8.5，属需求驱动或合理扩展。

## 五、深度质量检查

### 维度 14：闭环完整性 — 4 ✅ / 1 ⚠️

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| 6.1 进入与检索 | ✅ | 无 | 入口→URL/store 初始化→API 校验→SQL→DTO→游标栈，输入输出齐备 |
| 6.2 打开详情 | ✅ | 无 | selectedId→归属校验→联表聚合→逐字段组装→三态渲染 |
| 6.3 继续此方向 | ✅ | 无 | 守卫（输入两侧状态）→确认→恢复载荷（与详情 DTO 一一对应）→导航→再生成走既有 POST |
| 6.4 保存为 Style Memory | ⚠️ | 步骤2「sourceAssetId = 分析来源资产」 | 依赖字段未定义于 §7.2 IterationDetail；且代码事实核实：既有 `POST /api/templates` 仅接受 DB-backed `sourceAssetId`（只传 sourceImageUrl 会被 400 拒绝），该字段不可省略（→P-02） |
| 6.5 进行中轮询 | ✅ | 无 | 5s/10s 双频→迁移切换→3 次失败熔断，与 §8.1 并发 ≤2 一致 |

### 维度 15：安全底线 — ✅（摘要）

PRD 无合规底线章节。架构 §8.3 覆盖认证与归属（Auth.js session + userId 校验）、输入面收敛（q ≤100、status 白名单、pageSize clamp、双 id 服务端归属校验）、Prompt 注入（本期无新 LLM 调用、纯文本渲染）、限流（沿用 30 次/小时/IP），与代码事实一致。

### 代码事实一致性抽查（任务附加项）— 全部属实 ✅

| 架构声明 | 代码事实 | 结论 |
| --- | --- | --- |
| 复用既有 `generation_tasks` 表，状态四值 pending/processing/completed/failed（§7.4/§7.6） | `src/lib/db/schema.ts` L126-166：表与状态 check 完全一致；`recipe_snapshot`/`variables_snapshot`/`source_template_id` 尚不存在，属本期新增，描述准确 | ✅ |
| 近期迭代条走 `GET /api/generation` 默认参数（completed-only），条目 shape 超集向后兼容（§4.2、§7.3） | `src/hooks/use-history-list.ts` 仅传 `pageSize=20`；`src/app/api/generation/route.ts` GET 调 `listCompleted`（completed + keyset 游标 + clamp [1,50]）；现有条目 {id, resultFileUrl, createdAt} 为新 shape 子集 | ✅ |
| 「复用既有历史恢复模式」（§2.1）；恢复接线已有基础 | `src/hooks/use-history-restore.ts` 存在且 `src/app/workspace/page.tsx` 已接线恢复（recipe/prompt/params/variables/analysisTaskId/上一轮结果写入工作台状态） | ✅ |
| `GET /api/generation/[id]` 扩展为全状态详情，既有消费方向后兼容（§7.3） | 现实现：completed 走 `findByIdWithRecipe`（含 recipe/sourceImageUrl/variables），非 completed 返回通用 shape；扩展为加法变更 | ✅ |
| `POST /api/templates` 沿用同名 409、校验与限流 30 次/小时/IP（§6.4/§8.3） | `src/app/api/templates/route.ts`：`TEMPLATE_NAME_CONFLICT` 409、内存滑动窗口 30/h/IP 均属实 | ✅ |
| `/api/templates/[id]` GET 无变更复用（§7.3） | `src/app/api/templates/[id]/route.ts` 存在 | ✅ |
| store 挂载 workspace layout（ADR-6）；左侧导航加 Iterations 项（§4.2） | `src/app/workspace/layout.tsx` 为 client 组件可承载 store；`left-sidebar.tsx` 现有 /workspace 与 /workspace/templates 两项，追加项可行；`/workspace/templates` 页面存在，「打开」跳转目标有效 | ✅ |
| ULID 26 位、列 snake_case、错误体 `{error, code, retryable}`、同步回写/Webhook/超时定时器沿用（§7.6/§8.2/§7.4） | `varchar(26)`/`generateId`、`{error, code, retryable}` 响应结构、`executeSyncGeneration`/webhook/`startTimeoutTimer` 均在代码中核实 | ✅ |
| §7.2 引用类型 `StoredVisualRecipe`/`TemplateVariable`/`GenerationParams{aspectRatio, quality}` | `src/types/models.ts` L160/L258/L231 全部存在 | ✅ |

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无（无 ❌ 项）。

### 部分满足的需求（中优先级）

| 编号 | 严重度 | 所在章节 | 问题 | 修复建议 |
|------|--------|---------|------|---------|
| P-01 | 中 | §3.3、§2.4（AC-02 行） | 「搜索无结果」状态未设计：状态机 LEmpty 仅标注「无记录（引导首次创作）」，未区分「筛选后无匹配」子状态；AC-02 结果句尾「无匹配项时提供清除搜索或切换筛选的行动」未在承接矩阵中出现，PRD §3.2「搜索无结果」关键状态（保留搜索词和筛选条件 + 清除/切换行动）无架构承接 | 在 §3.3 状态机增加「列表：ready（当前查询无匹配）」子状态（由 LReady 派生，保留 q/status，行动为清除搜索或切换筛选），并在 §2.4 AC-02 行「关键链路/状态」列补「无匹配态与清除/切换行动」；§9 Phase B 第 1 条状态面清单补「搜索无结果」 |
| P-02 | 中 | §7.2、§6.4 步骤2 | IterationDetail 契约缺 `sourceAssetId`：保存预填需要「分析来源资产」，且既有 `POST /api/templates` 只接受 DB-backed `sourceAssetId`（仅 sourceImageUrl 会被 400 拒绝，已核实代码），导致 §6.4 步骤2 悬空、AC-06「以该次迭代的来源为起点」不可达 | 在 §7.2 IterationDetail 中增加 `sourceAssetId: string | null`（与 sourceImageUrl 同口径的缺失标记），并在 §6.4 步骤2 明确取值来源为该字段 |

### 低优先级问题

| 编号 | 严重度 | 所在章节 | 问题 | 修复建议 |
|------|--------|---------|------|---------|
| P-03 | 低 | §3.3 / §4.2 | PRD §3.1 已完成详情线框头部含「上一条 / 下一条」导航，架构未承接（视图 store 游标栈本可支撑） | 在 §3.3 关键规则或 §4.2 页面模块职责中补一句：「详情上一条/下一条由视图 store 的选中项 + 游标栈支撑顺序切换」；若首版决定不做，需回到 PRD 确认线框口径 |
| P-04 | 低 | §6.1 步骤2 / §7.3 | 页面级默认 status 未写明：API 默认 completed 仅为近期条兼容，左侧导航直达（URL/store 均无 status）时若透传 API 默认，将违背 PRD「默认看到最近的所有生成尝试」 | 在 §6.1 步骤2 明确「页面级默认 `status=all`；API 默认 `completed` 仅服务近期条兼容调用方」 |
| P-05 | 低 | §9 | 章节命名与 schema `architecture.sections` 不一致：schema 为「实施建议与技术选型」，文档为「实施方案」 | 将 §9 标题由「实施方案」改为「实施建议与技术选型」（内容已达标，仅改名） |
| P-06 | 低 | §4.1 | 系统上下文为纯文字描述，缺 checklist 要求的 ASCII 框图 | 补一张最小 ASCII 框图：浏览器 ↔ Next.js API 路由 ↔ PostgreSQL/R2，标注本期新增的聚合读路径与两处写入点 |
| P-07 | 低 | §2 | 缺「P1 预留」小节（checklist 2.2），演进候选只在 §10 出现 | 在 §2.2 前后补一行「P1 预留：无（候选演进见 §10：pg_trgm、推送、标签/批量、资产修复）」 |
| P-08 | 低 | §7.3 | API 边界表无「认证要求」列（checklist 7.3），认证信息集中在 §8.3 | 在 §7.3 表中增加「认证」列（5 个端点均为 Auth.js session），或表下注明「认证统一见 §8.3」 |

### 存疑设计（需评估）

无。未发现需要删除或简化的存疑设计。

## 七、总结

- **结构完整度**：95%（45 通过 / 5 部分 / 0 缺失，共 50 检查项）
- **需求满足度**：96%（79 满足 / 6 部分满足 / 0 未满足，共 85 需求项；深度维度 14 为 4✅/1⚠️、维度 15 ✅）
- **整体评价**：⚠️ **需小幅补充后可进入下一阶段**
  - 无前置阻塞（配对可靠、frontmatter 合法、open_questions 同步、无影响实现的未决问题）
  - 无 P0 需求 ❌；关键维度（6/11/14/15）无 ❌，仅存 ⚠️
  - 全部 8 个问题均为文档级局部修订，无设计返工；其中 P-01、P-02 建议在进入 create-dev-plan 前修复（影响状态机与 DTO 契约），P-03~P-08 可随上述修订一并处理
- **优先修复建议**：
  1. P-02：§7.2 IterationDetail 增加 `sourceAssetId` 字段，闭合 §6.4 保存链路悬空依赖
  2. P-01：§3.3 增加「搜索无结果」子状态并补 AC-02 承接，对齐 PRD §3.2 关键状态
  3. P-03/P-04：明确「上一条/下一条」承接方式与页面级默认 `status=all` 口径
