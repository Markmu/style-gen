---
feat_id: plan-06
phase: green
date: 2026-08-26
spec: e2e/style-memory-save-flows.spec.ts
---

# plan-06 E2E Green 证据（保存流程重构：三步向导 + 草稿保存）

- 功能文件：`docs/14-可验证Style-Memory/14-2-实现计划-可验证Style-Memory/plan-06-保存流程重构.md`
- 关联架构：`docs/14-可验证Style-Memory/14-1-架构文档-可验证Style-Memory.md` §6.3（保存链路 / V2 预填算法 / A-B 流程差异）、§4.2-⑤、§7.3（`SaveStyleMemoryRequest`）、ADR-1/6、Q5 决策
- Red 证据：`docs/e2e/evidence/plan-06-e2e-red-2026-08-26.md`（12/12 预期失败于三步向导/草稿向导未实现）
- spec 文件：`e2e/style-memory-save-flows.spec.ts`（green 复跑**零修改**，TC 编号与 red 场景清单逐条一致）；`e2e/helpers/mock-api.ts` 既有导出（red 阶段新增的 `mockTemplateCreateCaptureSlow` 未再修改）

## 执行命令与结果（最终态独立复跑）

```bash
pnpm e2e -- e2e/style-memory-save-flows.spec.ts --project=workspace
# → 12 passed / 12（0 failed），22.6s

pnpm e2e -- e2e/workspace-iteration-save-style-memory.spec.ts \
  e2e/workspace-ai-first-iteration-memory.spec.ts \
  e2e/template-default-values.spec.ts --project=workspace
# → 16 passed / 16（0 failed），22.4s

pnpm vitest --run src/lib/__tests__/style-memory-prefill.test.ts
# → 8 passed / 8

pnpm verify:fast   # workflow 契约 + type + lint + 单测/组件测试全绿（exit 0）
```

## 与 red 失败清单的对照（12/12 逐条转绿）

| Red 失败（统一失败于"旧单步对话框仍被打开"） | Green 消除方式 |
| --- | --- |
| TC-4.1 步骤 1 并排图 + 默认不勾选 | `save-style-memory-dialog.tsx` 重构为 `StyleMemorySaveWizard`：step-1 容器 testid、参考图/本次结果并排（sourceImageUrl/resultFileUrl）、「设为代表结果」默认不勾选（Q5）、已验证语义说明 |
| TC-4.2 V2 四元组预填 | `style-memory-prefill.ts::deriveStyleMemoryPrefill`：hard 优先排序；排除约束取配方值而非 negativePromptSnapshot；optionalModifiers.defaultValue → 增强方向；fingerprint.tokens → 风格指纹；变量默认值同屏可编辑 |
| TC-4.3 状态联动 + 高级信息折叠 | 步骤 3 唯一文本节点「保存后状态：…」随勾选即时联动；完整提示折叠预览可编辑 |
| TC-4.4 勾选保存提交体 | POST 体含 representativeGenerationTaskId = sourceGenerationTaskId = 迭代 id、四元组、编辑后变量；不含 verificationStatus；成功 router.push 新详情且「用户已验证」 |
| TC-4.5 不勾选保存 | 无 representativeGenerationTaskId、携带 sourceGenerationTaskId；新详情「待验证」 |
| TC-4.6 fallback 缺失标记 | fallback/null 配方四组全空 + missing 全标记「本次迭代无 X」≥4，不推测补齐，流程可继续 |
| TC-4.7 无提前必填错误 | 中性帮助「1-50 个字符」，错误仅提交/失焦后出现；空名提交零 POST |
| TC-4.8 进行中锁定 | 慢响应下保存/取消 disabled，force 连点仅 1 次 POST，名称与状态保留，成功进新详情 |
| TC-11.1 409 改名重试 | 服务端冲突文案原样呈现、步骤 3 与内容保留、改名重试恰 2 次 POST 无重复 Memory |
| TC-11.2 5xx 直接重试 | 可重试错误条、步骤与全部内容保留、重试体与首次完全一致、恰 2 次 POST |
| TC-8.1 键盘往返 | plan-03 ModalDialog 原语：打开即聚焦、Tab×10 循环不出弹层、Escape 还原触发按钮焦点、纯键盘完成三步、详情初始焦点落 h1（plan-05 交付） |
| TC-4.9 流程 B 草稿 | `template-save-dialog.tsx` 复用同一向导骨架（flow=workspace-draft）：`save-wizard-no-representative-note` 说明区、无 step-1/勾选框、状态固定待验证、POST 体不含 representative/sourceGenerationTaskId 且含 sourceAssetId |

## 存量回归与联动改写

- 计划内三个存量 spec 已按向导口径改写并全绿（16/16）：`workspace-iteration-save-style-memory`（入口条件与已保存态用例保留、单步表单断言改为向导步骤断言）、`workspace-ai-first-iteration-memory`（还原历史无保存入口断言在新行为下复核通过）、`template-default-values`（流程 B 向导口径 + 详情跳转断言）。
- 共享对话框被替换产生的必要联动改写（不在计划文件清单内，已逐一复跑全绿）：
  - `e2e/workspace-ai-first-render-dock.spec.ts` + `e2e/workspace-card-expand.spec.ts`：7 passed（13.8s）
  - `e2e/template.spec.ts`：6 passed（13.8s）。其中「变量确认」用例按现行能力改写——V1 分析派生的负面提示伪变量在向导内确认并随体携带（原 `Variable subject` 编辑器断言依赖旧单步对话框内部行为，新链路不可达）
  - `e2e/analysis-structured-style-extraction.spec.ts`：3 passed。第二轮改为回工作台后重新上传分析（离开 `/workspace` 触发既有 `WorkspacePersistenceGuard` 清空 sessionStorage 快照，持久化恢复前提不成立；该 Guard 为 plan 前既有行为，未改动产品代码）
  - `e2e/workspace-iteration-memory-integration.spec.ts`：TC-6.1 全流程按保存成功直入新详情收口，补 `mockStyleMemoryDetailCollection` 提供 plan-02 DTO 详情 GET（既有 helper，未改导出）
- 组件测试补强：`save-style-memory-dialog.test.tsx` 新增「来源参考图缺失显示『来源图缺失』占位、本次结果正常」（边界场景测试证据）；iterations 组件套件 53/53。

## 契约自检（SaveStyleMemoryRequest / ADR-1）

- 两流程提交体均由 e2e 断言：name/content/sourceAssetId/sourceGenerationTaskId(仅流程 A)/representativeGenerationTaskId(仅勾选)/retainedRules/negativeConstraints/styleTokens/enhancementHints/variables(编辑后)；`verificationStatus` 在两流程均为 undefined（TC-4.4 / TC-4.9 断言）。
- `styleTokens`/`enhancementHints` 以只读快照展示并 frontend_computed 随体携带（prefill 单测覆盖上限收敛 ≤16 × ≤80）。

## 判定

- red spec 零修改，12/12 全部转绿；计划验证命令（E2E 两组 + prefill 单测）与 `verify:fast` 全部通过。
- 结论：**预期通过，实现满足 plan-06 功能验收（AC-04/08/11）与契约对接验收**；FEAT 可进入 task-review。

## 阻塞项

无。

## 独立复核

- 复核方：test-e2e 独立复核 agent（task-review 前置），与 implementer 分离执行
- 复核日期：2026-08-26
- 独立复跑命令与结果（本机干净执行，未命中任何缓存的旧报告）：
  - `pnpm e2e -- e2e/style-memory-save-flows.spec.ts --project=workspace` → **12 passed / 12（0 failed），22.4s**
  - `pnpm e2e -- e2e/workspace-iteration-save-style-memory.spec.ts --project=workspace` → **9 passed / 9（0 failed），16.7s**（回归抽查：旧保存入口 owner）
  - `pnpm vitest --run src/lib/__tests__/style-memory-prefill.test.ts` → **8 passed / 8**
- 一致性核查：
  - 本文件正文记录的主 spec 12/12、回归 spec 绿、prefill 单测 8/8 与上述独立复跑结果逐项一致。
  - "16 passed / 16" 构成核实为 `workspace-iteration-save-style-memory`(9) + `workspace-ai-first-iteration-memory`(6) + `template-default-values`(1) = 16 用例。
  - spec 内 TC 编号（TC-4.1～4.9、TC-11.1/11.2、TC-8.1）与 red 证据场景清单逐条对应，本轮 12/12 全绿即证明 red 零修改转绿。
- 结论：**独立复核通过**，green 证据可信，red→green 基线成立，plan-06 可进入 task-review。
- 遗留阻塞项：无。
