---
feat_id: plan-06
phase: red
date: 2026-09-01
---

# plan-06 E2E Red 证据（首选 Memory 与结果新参考）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.1.7、§3.2（首选滚出五条成功窗口 / 作为新参考 / 首选保存更新 Memory 行）、ADR-6（生成结果作为新参考复用 Asset）、§6.6（结果作为新参考）、§6.7（首选结果与 Style Memory）、§7.3（`/api/analysis` sourceAssetId 分支与 representative-result 端点）
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-06-首选Memory与结果新参考.md`（status: ready-to-dev，Task 1：red 补首选/Memory/新参考 E2E 场景）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（本次增量新增 plan-06 分组 TC-6.1–TC-6.10，plan-02/04/05 分组与汇总外未改动）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（在主 spec 末尾追加 plan-06 describe，workspace project，mock API 全量拦截，不触达 live Provider/R2）

## 场景覆盖（plan-06 实现规格 §5 / 验收 AC-04、AC-06、AC-07）

| 用例 | 场景 | 类型 |
| --- | --- | --- |
| TC-6.1 | 设置与更换首选：preferred 写入经 Iteration detail 验证（详情 GET 可观察），更换后指向新结果且原首选复位 | Happy |
| TC-6.2 | 首选滚出五条成功窗口仍有效：`direction-preferred-external` 提示 + `direction-preferred-open-detail` 打开详情（AC-06 窗口外按 detail 有效） | Edge |
| TC-6.3 | 无效 preferred 清理：detail 属不同方向 → 清除 ID 并以 `direction-preferred-invalid` 说明原因；不呈现窗口外提示 | Error |
| TC-6.4 | 无来源 Memory：从首选结果打开既有 SaveStyleMemoryDialog 且「Set as representative result」预选；取消零写入；create 请求携带 sourceGenerationTaskId = representativeGenerationTaskId | Happy |
| TC-6.5 | 有来源 Memory：Memory 动作打开既有代表结果确认（RepresentativeResultSelector），preferred 结果 radio 预选；确认后才 POST `/api/templates/[id]/representative-result` | Happy |
| TC-6.6 | Memory 更新成功后四类回读即时可见：templates 列表 / `templates/{id}` 详情 / representative candidates / direction feed 全部重新读取；`direction-memory-status` 无需整页刷新变为 user_verified + 新代表结果；preferred 保持；无需 page.reload | Happy |
| TC-6.7 | 写入成功但部分回读失败（feed 503）：`memory-refresh-partial-error`「已保存，刷新失败」+ `memory-refresh-retry` 只重试读取；重试后恢复；全程 representative-result POST 保持 1 次（不回滚、不重复写） | Error |
| TC-6.8 | 设置/更换首选不改变验证状态：`direction-memory-status` 保持 pending_verification，representative-result / PUT 请求数为 0（preferred 从不写 templates） | Edge |
| TC-6.9 | 作为新参考取消守卫：`new-reference-confirm-dialog` + 未完成内容说明；取消零写入（零 analysis/generation POST）、草稿逐字不变、焦点回触发器 | Edge |
| TC-6.10 | 确认作为新参考：POST /api/analysis 恰 1 次且仅携带 `sourceAssetId`（无 fileUrl/width/height/mimeType/assetId，ADR-6 零复制）；工作区进入 analyzing 新方向；/workspace/iterations 完整历史仍可见旧方向条目（AC-04 旧方向可回溯） | Happy |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
```

## 结果与失败摘要

**47 个用例：37 passed / 10 failed**（plan-02 TC-2.1–TC-2.10、plan-04 TC-4.1–TC-4.12、plan-05 TC-5.1–TC-5.15 共 37 条既有用例全部通过保持绿；plan-06 TC-6.1–TC-6.10 全部失败）。

失败点分布（每条均为 plan-06 功能未实现，不是测试/环境错误）：

| 用例 | 失败点（首个失败断言） |
| --- | --- |
| TC-6.1 | `expect.poll(() => detailV1.callCount).toBeGreaterThan(0)` — Received: 0（点击 preferred 后未发生 Iteration detail 验证 GET；当前实现直接写 ws.preferredIterationId） |
| TC-6.2 | `getByTestId('direction-preferred-external')` element(s) not found（滚出窗口后仅有无 testid 的纯文本提示，无「打开详情」动作） |
| TC-6.3 | rail `data-preferred-id` Expected `""` / Received `"dir-pref-bad"`（无 detail 验证，无效首选不被清除） |
| TC-6.4 / TC-6.6 / TC-6.7 | `locator.click: Test timeout` — `[data-testid="direction-item-save-memory"]` 不存在（rail 无 Memory 动作入口） |
| TC-6.5 / TC-6.8 | `getByTestId('direction-memory-status')` element(s) not found（工作区无来源 Memory 验证状态位） |
| TC-6.9 / TC-6.10 | `getByTestId('new-reference-confirm-dialog')` element(s) not found（`onUseAsNewReference` 回调为空实现，无方向切换守卫） |

典型失败输出：

```text
Error: expect(locator).toBeVisible() failed
Locator: getByTestId('new-reference-confirm-dialog')
Expected: visible
Timeout: 10000ms
Error: element(s) not found
```

- 37 条既有用例在同一运行中全部通过，证明 dev server、mock（session/presign/analysis/generation/cdn/direction feed/templates collection）与测试环境正常；失败均源于 plan-06 UI 入口/接线未实现（`direction-item-save-memory`、`direction-preferred-external/invalid`、`direction-memory-status`、`new-reference-confirm-dialog`、preferred detail 验证）。
- 红阶段迭代记录：初版 TC-6.2 用裸 seed `preferredIterationId` + `page.reload` 驱动，发现会被 `restoreFromPersistedState` 有效性校验丢弃且 reload 存在 300ms 持久化防抖竞态；已改为「active 定时刷新节奏中 `feed.set` 推进滚出」与「pendingIterationRestore 通道 seed 来源 Memory 方向」的确定性驱动（与 TC-4.10 既有 seed 模式一致），用例文档已注明该约束。
- 失败截图（10 张）: `test-results/workspace-evidence-guided__*-workspace/test-failed-1.png`。
- spec/helper lint 通过（`pnpm exec eslint e2e/workspace-evidence-guided-render-loop.spec.ts e2e/helpers/mock-api.ts` 0 errors）；`pnpm exec tsc --noEmit` 无涉及本两文件的错误。

## 测试基建披露

- 扩展 `e2e/helpers/mock-api.ts`（mock 允许惯例，本次为 plan-06 必需的 API mock 扩展，均为纯增量）：
  - `mockAnalysisCreateCapture(page, taskId)` — 捕获 POST /api/analysis 请求体并返回 pending 任务；仅拦截 POST，GET 轮询继续由 `mockAnalysisPolling` 提供；在既有 `mockAnalysisCreate` 之后注册即只捕获后续新请求（后注册 route 优先），驱动 TC-6.9/6.10 的 `{sourceAssetId}` 请求契约断言。
  - `mockStyleMemoryDetailCollection` 返回对象增补 `listQueries`（列表 GET query 快照数组）与 `detailGets`（详情 GET id 序列）——仅加计数、不改任何响应行为，既有消费方不受影响，驱动 TC-6.6 四类回读断言。
- 无新增 fixture：Style Memory 详情/候选/代表结果复用既有 `mockStyleMemoryDetailCollection`，create 复用 `mockTemplateCreateCapture`，方向 feed/详情复用 plan-05 stateful mock。
- spec 内共享构造器：`sourceMemoryDetail`（来源 Memory 详情 DTO）、`representativeCandidate`（候选条目）、`seedSourceMemoryDirection`（经 `pendingIterationRestore` 一次性通道 seed 「来自来源 Memory 的方向」）。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图（10 张）: `test-results/workspace-evidence-guided__*-workspace/test-failed-1.png`
- 本次修改/新增:
  - `e2e/workspace-evidence-guided-render-loop.spec.ts`（追加 plan-06 describe：TC-6.1–TC-6.10 + 共享构造器/helper；既有 37 条用例零改动）
  - `e2e/helpers/mock-api.ts`（新增 `mockAnalysisCreateCapture`；`mockStyleMemoryDetailCollection` 增补回读计数）
  - `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（增量 plan-06 分组 + 汇总更新）

## 结论

预期失败，测试有效。plan-06 的 10 条用例全部因 plan-06 功能（preferred detail 验证、窗口外首选状态、无效清理说明、rail Memory 动作入口、来源 Memory 验证状态位、统一刷新协调器与部分刷新失败位、作为新参考的方向切换守卫与 sourceAssetId 分析接线）未实现而失败；plan-02/04/05 的 37 条既有用例保持通过。implementer 可以本证据为开始条件，按用例文档 plan-06 分组中的 data-testid / mock 契约实现至 green。
