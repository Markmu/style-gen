---
feat_id: plan-04
phase: green
date: 2026-09-01
---

# plan-04 E2E Green 证据（Prompt 控制与保留改变摘要）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.2（手动全文/armed/旧任务分支）、§6.2（Prompt 意图、表达与手动全文）、§6.3（参考画幅推荐）、§7.2（PromptControlSnapshot / AspectRatioSource / PromptEditorMode）、§8.6（三档规则恒等、未知画幅请求前拒绝）
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-04-Prompt控制与保留改变摘要.md`（status: review）
- red 证据: `docs/e2e/evidence/plan-04-e2e-red-20260901.md`（22 用例：10 passed / 12 failed，plan-04 TC-4.1–TC-4.12 全部因 `prompt-intent-controls` / `keep-change-summary` / `aspect-ratio-source` 入口不存在而预期失败，plan-02 保持绿）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（plan-04 分组 TC-4.1–TC-4.12，plan-02 分组未改动）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（与 red 阶段同一 spec，workspace project 端口 3001，mock API 全量拦截，不触达 live Provider/R2）

## 执行命令与结果

### 1. 相邻 Vitest（plan-04 验证命令第一条）

```bash
pnpm vitest --run src/components/workspace/__tests__/prompt-intent-controls.test.tsx src/components/workspace/__tests__/keep-change-summary.test.tsx src/components/workspace/__tests__/prompt-card.test.tsx src/components/workspace/__tests__/structured-prompt-editor.test.tsx src/components/workspace/__tests__/unified-prompt-editor.test.tsx src/components/workspace/__tests__/output-card.test.tsx
```

- 结果: **6 个测试文件全部通过，77 passed / 0 failed**
  - `src/components/workspace/__tests__/prompt-intent-controls.test.tsx` — 10 passed（两轴默认态、即时切换、dirty confirm、键盘与 armed 锁定）
  - `src/components/workspace/__tests__/keep-change-summary.test.tsx` — 5 passed（真实规则/变量派生、空态、定位与 polite 通知）
  - `src/components/workspace/__tests__/prompt-card.test.tsx` — 14 passed（新结构、compiled prompt 与降级状态）
  - `src/components/workspace/__tests__/structured-prompt-editor.test.tsx` — 9 passed（三视图受控、structured 只读与旧任务兼容）
  - `src/components/workspace/__tests__/unified-prompt-editor.test.tsx` — 21 passed（手动全文 dirty 上抛、pending selection 保护与降级全文）
  - `src/components/workspace/__tests__/output-card.test.tsx` — 18 passed（reference/user/restore/fallback 来源展示与推荐标注）

### 2. 同一 E2E spec（red 证据同款命令）

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
```

- 结果: **22 passed / 0 failed**（34.1s，1 worker，workspace project）
- 分组: plan-02 TC-2.1–TC-2.10 全部保持绿（10/10）；plan-04 TC-4.1–TC-4.12 全部转绿（12/12）
- 逐用例（plan-04）: TC-4.1 两轴与编辑方式默认态（same_style/standard/variables）；TC-4.2 intent 即时重编译与 reconstruction 说明、invariant 恒等；TC-4.3 三档 detail invariant 恒等、观察按档位补充；TC-4.4 变量在切换后保持并进入编译结果；TC-4.5 三编辑入口可达、structured 只读、返回后最终 Prompt 来源不变；TC-4.6 手动全文取消逐字保留/确认替换并清 dirty；TC-4.7 摘要真实派生并可定位 Recipe invariant 与变量编辑器；TC-4.8 无 invariants 空态不伪造；TC-4.9 reference 推荐标注且 user 选择不被覆盖；TC-4.10 restore 优先与旧快照 text 降级；TC-4.11 未知画幅请求前拒绝、POST 仅白名单；TC-4.12 armed 期间两轴只读并说明确认快照。

### 3. 仓库 fast gate（plan-04 验证命令第三条）

```bash
pnpm verify:fast
```

- 结果: **通过**（`pnpm workflow:check && pnpm test:workflow && pnpm type-check && pnpm lint && pnpm test` 全链绿）
  - `pnpm workflow:check` — pass（5 plans, 5 standalone specs, 87 Skill documents）
  - `pnpm test:workflow` — pass 4 / fail 0
  - `pnpm type-check`（`tsc --noEmit`）— pass
  - `pnpm lint`（`eslint src`）— 0 errors（仅存量 `_`/未使用变量 warning，25 warnings）
  - `pnpm test`（Vitest 全量）— 114 test files / **1157 passed / 0 failed**

## 产物

- Playwright HTML 报告: `playwright-report/index.html`
- 报告备注: green 阶段无失败，`test-results/` 无失败截图/trace；本环境 Playwright HTML reporter 只落盘 `index.html`（与 red 阶段记录一致的可复现行为，与实现无关）。
- green 阶段未修改任何产品代码与测试断言，仅执行验证；测试资产与 red 阶段完全一致（`e2e/workspace-evidence-guided-render-loop.spec.ts`、fixture `analysis-v2-completed-no-invariants.json`、spec 内 `seedWorkspaceV5State` / `completeDeepAnalysis` 共享 helper）。

## 结论

**通过。** red 阶段 12 个失败用例在实现后全部转绿（22 passed / 0 failed，plan-02 10 条保持绿），同一 spec、同一 workspace project、同一 mock 契约；plan-04 三条验证命令（相邻 Vitest 77/77、targeted E2E 22/22、`pnpm verify:fast` 全绿）全部通过。E2E-TDD 两阶段证据（red + green）已闭合，plan-04 保持 `review` 状态待 task-review 验收。
