---
feat_id: plan-01
phase: red
date: 2026-09-01
---

# plan-01 E2E Red 证据（相邻 Vitest 承载）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §6.2、§6.3、§7.2、ADR-3、§8.1
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-01-Prompt编译与画幅基础.md`
- **E2E 不适用（纯函数/Provider 契约，理由见 plan-01「验收标准 > E2E 适用性」与「风险与边界 > E2E 不适用说明」），red 证据由相邻 Vitest 承载**：用户可观察行为由 plan-02/04/05/07 的 `e2e/workspace-evidence-guided-render-loop.spec.ts` 覆盖（README 执行拓扑已声明），本功能先提供其可导入契约。

## 覆盖测试文件与用例清单

- `src/lib/__tests__/prompt-composer.test.ts`（modify，新增 describe「composePromptDocument（plan-01 §2 / AC-02）」10 用例 + 保留既有 2 用例作为兼容导出证据）
  - reconstruction 意图使用原内容而不是变量模板（§2）
  - same_style 意图使用变量模板并保留 enabled modifier 占位符（§2）
  - 同一控制快照下三档的 enabled invariant ID 集合完全一致，仅补充 observation 数变化（AC-02；soft invariant 不因 concise 删除；observation 数 0/1/4 递增）
  - concise 不添加未覆盖 observation（§2）
  - standard 每维至多加入一条置信度 ≥0.7 的最高未覆盖 observation（§2；color_2 0.85 命中，camera 0.6 不进入，已覆盖 color_1 不重复）
  - professional 加入全部 ≥0.5 的未覆盖 observation，同分按 Recipe 原序（§2；camera_1/camera_2 并列 0.6 按原序，0.3 与已覆盖项排除）
  - 每个文本片段记录合法字符范围并可定位来源值（§2 segments：sourceKind/sourceId/dimension/startIndex/endIndex）
  - 同一 Recipe 与控制快照重复编译结果完全一致（确定性）
  - 片段维度按既有维度顺序稳定输出（§2「按 Recipe 维度原序」）
  - 未启用的 invariant 不进入任何档，零启用不伪造规则
- `src/lib/__tests__/prompt-adjustments.test.ts`（create，15 用例，§3 / AC-02 / AC-05）
  - applyInvariantAdjustment 7 用例：新增 strengthen 只记录目标 invariant；同一 invariant 重复 adjustment 以最后一次覆盖且唯一（边界场景「同一 invariant 重复 adjustment」）；仅更新对应 invariant；未知 invariantId 抛含该 ID 的校验错误（边界场景「adjustment 引用未知 invariant」）；replace trim 后必须非空；replace trim 后 ≤200 字符（201 拒绝、trim 后 200 通过）；disable/relax 无需 replacementValue
  - deriveKeepChangeSummary 4 用例：保留取全部未禁用 invariants（含 soft）；被 disable 的不进入保留；未启用不进入、零规则不产生虚假目标；改变按 trim/压缩空格/忽略大小写比较变量当前值与默认值
  - applyAdjustmentToCustomText 4 用例：replace 命中 segment range 局部替换；disable 命中 range 删除该表达；非 disable 未命中追加 `Adjustments:` 段；disable 未命中文本不变并返回「未找到可删除表达」状态
- `src/lib/generation/__tests__/aspect-ratio.test.ts`（create，14 用例，§4 / AC-03）
  - 白名单 2 用例：唯一公开画幅顺序 `1:1, 4:3, 16:9, 3:4, 9:16`；isSupportedAspectRatio 只接受白名单值（4:5/3:2/空串为 false）
  - findClosestAspectRatio 4 用例：4:5→3:4（Task 5 指定场景）；参考比例恰为候选返回该候选；宽幅 2.0→16:9；对数距离并列按白名单数组顺序取第一项（3:4 vs 9:16 几何均值在 IEEE 754 下两距离精确相等，断言取 3:4）
  - resolveAspectRatio 8 用例：restore 优先于 user 与 reference；user 优先于 reference；仅参考尺寸返回最近值并标 reference（800×1000→3:4）；无尺寸且无高优值回退 1:1/fallback；宽或高为 0 回退 1:1（边界场景「参考尺寸为 0」）；NaN 回退 1:1；无尺寸时 restore 生效；无尺寸时 user 生效
- `src/lib/ai/providers/__tests__/fal-image-gen.test.ts`（modify，新增/替换 3 用例 + 保留既有 15 用例）
  - aspectRatio 3:4 应映射为 portrait_4_3（§4）
  - aspectRatio 9:16 应映射为 portrait_16_9（§4）
  - 未知 aspectRatio（3:2）应在调用 Provider 前抛含该值的 ImageGenError，且 mockSubscribe 不被调用（§4 / 边界场景「Provider 收到未知画幅：调用前拒绝，不回退 square」）。本用例**替换**了旧用例「aspectRatio 其他值应默认映射为 square」——旧断言编码的正是 plan-01 明确禁止的静默回退行为，属于计划内变更而非存量回归

### 测试中固化的新契约（供 implementer 对齐）

- `composePromptDocument(recipe, controls)` 返回 `CompiledPrompt{text, segments}`；controls 为 §7.2 `PromptControlSnapshot` 形状（测试内以结构化字面量表达，Task 1 类型落地后对齐）
- `src/lib/prompt-adjustments.ts` 导出 `applyInvariantAdjustment(recipe, adjustments, next)`、`deriveKeepChangeSummary(recipe, controls)`（返回 `{keptInvariantIds, changedVariableNames}`）、`applyAdjustmentToCustomText(text, segments, adjustment)`（返回 `{text, status: "applied" | "appended" | "not_found"}`）
- `src/lib/generation/aspect-ratio.ts` 导出 `SUPPORTED_ASPECT_RATIOS`、`isSupportedAspectRatio`、`findClosestAspectRatio(referenceRatio)`、`resolveAspectRatio({referenceWidth, referenceHeight, userValue, restoreValue})`（返回 `{aspectRatio, source}`）

## 执行命令

```bash
pnpm vitest --run src/lib/__tests__/prompt-composer.test.ts src/lib/__tests__/prompt-adjustments.test.ts src/lib/generation/__tests__/aspect-ratio.test.ts src/lib/ai/providers/__tests__/fal-image-gen.test.ts
```

（与 plan-01「验证命令」首条一致；`pnpm type-check` / `pnpm verify:fast` 属 Task 7 green 阶段命令。）

## 结果与失败摘要

**4 个测试文件全部 failed：13 failed / 17 passed（30 用例）**

- `prompt-composer.test.ts`（10 failed / 2 passed）：全部为新 describe 用例，失败原因均为 `TypeError: composePromptDocument is not a function`——目标导出（Task 3）尚未实现；2 个通过用例为既有 `composePromptOutputs` 兼容断言（「实现后必须保持」，边界场景「旧 composer 调用方」）
- `prompt-adjustments.test.ts`（套件加载失败）：`Error: Cannot find package '@/lib/prompt-adjustments'`——`src/lib/prompt-adjustments.ts`（Task 4）尚未创建，15 用例全部无法加载
- `aspect-ratio.test.ts`（套件加载失败）：`Error: Cannot find package '@/lib/generation/aspect-ratio'`——`src/lib/generation/aspect-ratio.ts`（Task 6）尚未创建，14 用例全部无法加载
- `fal-image-gen.test.ts`（3 failed / 15 passed）：
  - 3:4 / 9:16 两个用例：`expected "vi.fn()" to be called with arguments … image_size: "portrait_4_3" / "portrait_16_9"`，实际 `toFalImageSize` 将未知比例（含 3:4、9:16）静默回退为 `square`——正是 plan-01 §4 要消除的行为
  - 未知值拒绝用例：`AssertionError: promise resolved "{ mode: 'sync', …(3) }" instead of rejecting`——当前实现对 3:2 不做调用前拒绝
  - 15 个通过用例均为既有行为（正常生成、FAL_KEY、超时、错误包装、16:9/4:3/1:1 映射、模型与 num_images、credentials），与 plan-01 无冲突

## 判定

- 失败原因全部是 plan-01 目标功能（`composePromptDocument` 与 segments、prompt-adjustments 纯函数、共享画幅模块、fal portrait 映射与调用前拒绝）尚未实现，不是测试代码、mock 或环境错误：四个测试文件 ESLint 0 error 0 warning；17 个通过用例均为既有行为与兼容守卫（composePromptOutputs 两用例、fal 15 用例），符合「实现后必须保持」的约束。
- 附注：测试文件引用了尚不存在的导出（`composePromptDocument`、`@/lib/prompt-adjustments`、`@/lib/generation/aspect-ratio`），`pnpm type-check` 在实现落地前对此报错属 TDD red 预期，green 阶段随 Task 1/3/4/6 实现消除。
- 结论：**预期失败，测试有效。**
