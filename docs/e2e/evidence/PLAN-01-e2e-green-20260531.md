---
feat_id: PLAN-01
phase: green
date: 2026-05-31
---

# PLAN-01 E2E Green 证据

- 关联架构: `docs/11-Workspace三栏工作台重构/11-1-架构文档-Workspace三栏工作台重构.md`
- 功能文件: `docs/11-Workspace三栏工作台重构/开发计划/PLAN-01-三列骨架与模式切换.md`
- 用例文档: `docs/e2e/11-e2e-用例-Workspace三栏工作台重构.md`
- Spec: `e2e/workspace-three-column-layout.spec.ts`
- 执行命令: `pnpm e2e -- e2e/workspace-three-column-layout.spec.ts`
- 结果: 通过，7 passed / 0 failed

## 覆盖场景

- TC-1.1 空态 Workspace 渲染三列骨架和 TopModeSwitcher。
- TC-1.2 上传 / 分析中三列卡片持续挂载。
- TC-1.3 分析完成后自动高亮 Editing。
- TC-1.4 手动点击模式只改变高亮，不重置工作台数据。
- TC-1.5 1280px / 1440px 下保持 1:1:1.2 列宽节奏和 16px 间距。
- TC-1.6 sessionStorage 损坏时静默回到 idle 三列状态。

## 回归补充

- `Save as Template` 已从 Prompt 卡片重新接回 `TemplateSaveDialog`。
- 额外执行 `pnpm e2e -- e2e/workspace-three-column-layout.spec.ts e2e/template.spec.ts` 时，模板保存主路径通过；仅 `Use Template 跳转Workspace并按默认值加载变量` 仍失败，因为该用例期待 PLAN-03 范围的 `UnifiedPromptEditor` 和变量输入控件。

## 性能抽样

- 构建命令: `pnpm build`
- 启动命令: `AUTH_REQUIRED=false pnpm exec next start -p 3002`
- 测量方式: Playwright 打开 `http://localhost:3002/workspace`，等待 `[data-testid="workspace-three-column-layout"]` 可见。
- 样本: `856ms`, `1315ms`, `821ms`, `813ms`, `798ms`
- 中位数: `821ms`
- 判定: 通过 `<1s` 工作台首屏渲染目标。
