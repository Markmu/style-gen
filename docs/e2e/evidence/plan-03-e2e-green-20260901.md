---
feat_id: plan-03
phase: green
date: 2026-09-01
---

# plan-03 E2E Green 证据（生成快照与方向查询 API，相邻 route/repository 集成测试承载）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` ADR-4/5/6、§6.4、§6.6、§7.2/7.3/7.4、§8.2/8.3/8.5
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-03-生成快照与方向查询API.md`（status: review）
- red 证据: `docs/e2e/evidence/plan-03-e2e-red-20260901.md`（42 failed / 131 passed，判定「预期失败，测试有效」）
- **E2E 不适用说明（与 red 阶段一致）**: 本功能为服务端契约，独立 UI E2E 不适用（理由见 plan-03「验收标准 > E2E 适用性」与「风险与边界 > E2E 不适用说明」），green 门由同一批五个 route/repository 集成测试文件承载；用例清单见 red 证据「覆盖测试文件与用例清单」。用户可观察结果（方向结果区、比较、新参考切换）由 plan-05/06/07 的 `e2e/workspace-evidence-guided-render-loop.spec.ts` 覆盖。
- spec 文件: 无独立 Playwright spec；承载文件为 `src/lib/repositories/__tests__/generation-task-repository.test.ts`、`src/lib/repositories/__tests__/asset-repository.test.ts`、`src/app/api/generation/__tests__/route.test.ts`、`src/app/api/generation/[id]/__tests__/route.test.ts`、`src/app/api/analysis/__tests__/route.test.ts`
- 本步骤未修改任何产品代码与测试断言，未修改 plan-03 frontmatter（保持 review）与 README 状态机。

## 执行命令与结果

### 1. plan-03 验证命令（red 证据同一批五个测试文件，`[id]` 路径单引号防 glob）

```bash
pnpm vitest --run src/lib/repositories/__tests__/generation-task-repository.test.ts src/lib/repositories/__tests__/asset-repository.test.ts src/app/api/generation/__tests__/route.test.ts 'src/app/api/generation/[id]/__tests__/route.test.ts' src/app/api/analysis/__tests__/route.test.ts
```

- 结果: **5 个测试文件全部通过，173 passed / 0 failed**
- 分文件对照（文件总数与 red 阶段完全一致）:

| 测试文件 | red 失败/通过 | green 通过 | 合计 |
| --- | --- | --- | --- |
| `src/lib/repositories/__tests__/generation-task-repository.test.ts` | 10 / 41 | 51 | 51 |
| `src/lib/repositories/__tests__/asset-repository.test.ts` | 3 / 6 | 9 | 9 |
| `src/app/api/generation/__tests__/route.test.ts` | 21 / 51 | 72 | 72 |
| `src/app/api/generation/[id]/__tests__/route.test.ts` | 3 / 11 | 14 | 14 |
| `src/app/api/analysis/__tests__/route.test.ts` | 5 / 22 | 27 | 27 |
| 合计 | 42 / 131 | 173 | 173 |

- red 阶段 42 个失败用例（`getDirectionIterationFeed`/`findAssetByIdForUser` 未实现、快照与画幅校验缺失、direction feed、sourceAssetId 分支、Provider 异常终态、限流与 §8.5 事件）全部转绿；123 个既有用例 + 8 个即绿回归锁（120_000/300_000ms fake-timer 等）全部保持通过（173 = 123 既有 + 50 新增）。

### 2. 仓库 fast gate（plan-03 验证命令末条）

```bash
pnpm verify:fast
```

- 结果: **通过（exit code 0）**
  - `pnpm workflow:check` — pass（5 plans, 5 standalone specs, 87 Skill documents）
  - `pnpm test:workflow` — pass
  - `pnpm type-check`（`tsc --noEmit`）— pass
  - `pnpm lint`（`eslint src`）— pass，0 error（仅存量 `_`/未使用变量/hooks warning）
  - `pnpm test`（Vitest 全量）— 112 test files / **1122 passed / 0 failed**

## 关联实现阶段证据（引用 plan-03 文件内「## 执行证据（implement，2026-09-01）」章节）

以下证据由 implement 阶段记录在 `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-03-生成快照与方向查询API.md` 的「执行证据（implement，2026-09-01）」章节，本 green 验证引用不改写:

- **迁移审查**: `drizzle/0006_pale_darkhawk.sql` 仅 `ALTER TABLE "generation_tasks" ADD COLUMN "prompt_control_snapshot" jsonb;`，`meta/0006_snapshot.json` diff 仅该 nullable jsonb 列，`_journal.json` 追加 idx 6；无 destructive SQL、无回填、无新表/索引。
- **Fresh apply（可丢弃本地开发库）**: `DATABASE_URL` 指向 Docker 容器 `style-gen-db`（localhost:5433，已确认可丢弃），执行 `pnpm db:reset` → `pnpm db:push`（`Changes applied`）→ `information_schema.columns` 确认 `prompt_control_snapshot / jsonb / is_nullable=YES`；种子数据已清理。
- **性能验收（架构 §8.1）**: 500 rows 种子（480 completed + 6 pending + 6 processing + 8 failed）三查询合计 50 次采样 **p95 0.389ms ≤ 300ms**；EXPLAIN (ANALYZE, BUFFERS) 命中既有部分索引/top-N heapsort，按护栏不新增复合索引。

## 产物

- 本功能无 Playwright E2E spec，故无 HTML 报告/失败截图/trace；上方 Vitest 输出即 green 证据。
- green 阶段测试资产与 red 阶段完全一致（同五个测试文件），未新增/修改 mock、fixture 与断言。

## 结论

**通过。** red 阶段 42 个失败用例在实现后全部转绿（173 passed / 0 failed，同一批五个测试文件、同一命令），既有 123 用例与 8 个回归锁全部保持；`pnpm verify:fast` 全绿（含 type-check、lint 0 error、Vitest 1122/1122）。E2E-TDD 两阶段证据（red + green）已闭合，plan-03 保持 `review` 状态待 task-review 验收。
