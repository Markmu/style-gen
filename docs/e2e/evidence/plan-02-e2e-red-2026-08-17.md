---
feat_id: plan-02
phase: red
date: 2026-08-17
---

# plan-02 E2E Red 证据（Iteration Memory 列表页）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §3.3（列表状态机）、§6.1（进入与检索链路）、§7.2/§7.3（IterationListItem DTO 与 API 边界）、§8.2（降级策略）
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-02-IterationMemory列表页.md`（status: ready-to-dev，Task 1 = E2E red）
- 计划 README: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/README.md`
- spec 文件: `e2e/workspace-iteration-memory-list.spec.ts`（新建，workspace project，mock 模式，无需 live 凭证）
- 用例列表说明: 按任务边界约束（不改 plan-02 文件清单之外的文件），plan-task 用例表嵌入本证据文件而非独立 `docs/e2e/*-e2e-用例-*.md`。

## 测试资产现状

- Playwright projects: `auth`（端口 3000，需认证）、`workspace`（端口 3001，`AUTH_REQUIRED=false`）；dev server 由 config `webServer` 管理
- 现有相关 spec: `e2e/workspace-history-strip.spec.ts`（近期条，不回归）、`e2e/workspace-ai-first-iteration-memory.spec.ts`（近期迭代条，不回归）
- 可复用 helpers: `mockAuthSession`、`mockApiError`、`mockGenerationList`/`mockGenerationListSequence`（近期条旧 shape）、`mockCdnImages` 模式（各 spec 内联）
- 本次扩展 `e2e/helpers/mock-api.ts`（plan-02 文件清单内 modify）:
  - `mockIterationList(page, items, { onRequest })` — GET `/api/generation` 完整列表 mock，服务端语义对齐 plan-01 真实实现（`src/app/api/generation/route.ts` GET + `src/types/models.ts` `IterationListItem`）：条目 shape `{ id, status: processing|completed|failed, promptSummary, resultFileUrl|null, params: { aspectRatio, quality }, createdAt }`；`status` 缺省默认 `completed`（近期条兼容）、`all` 不过滤；`q` 对 promptSummary 大小写不敏感包含匹配；`cursor` 为 keyset（上一页最后一条 id）；`pageSize` clamp [1,50] 默认 20；`nextCursor` 为当页末条 id（还有更早记录时）；`onRequest` 捕获请求 query 供透传断言
  - `mockLoggedOutSession(page)` — `/api/auth/session` 返回未登录（未登录面场景前置）
- 失败截图/上下文: `test-results/workspace-iteration-memory-*/`（Playwright 自动生成）

## 用例清单（覆盖 plan-02 §5 E2E 验收 + AC-01/02/07 + L1 图片降级）

| # | 场景 | 类型 | 前置条件 | 步骤 | 断言 | 目标 spec |
|---|------|------|---------|------|------|-----------|
| TC-2.1 | 直达默认全状态三态列表 | Happy | mock 三态条目（completed/processing/failed） | 打开 `/workspace/iterations` | 页面标题可见；completed 条目渲染真实结果图（img src=resultFileUrl）且含 promptSummary、`16:9`、`hd`、状态徽标；processing/failed 条目为状态面且 `img` 数为 0（禁止占位假图）；搜索框为空、"All" 单选选中；首次请求 `status=all` | workspace-iteration-memory-list.spec.ts |
| TC-2.2 | 搜索 + 状态筛选组合 | Happy | 同上 | 输入 `neon`；再切 `Failed` 单选 | 列表先收窄到 2 条（不含 processing），再收窄到 1 条 failed；请求 query `q=neon`、`status=failed`；URL 写回 `q`/`status` | 同上 |
| TC-2.3 | "继续浏览较早记录"游标加载 | Happy | mock 25 条（pageSize=20 → 两页） | 初始渲染后点击加载较早按钮 | 首屏 20 条；点击后合计 25 条、最旧条目恰好出现 1 次（无重复）；第二页请求携带非空 `cursor`；耗尽后按钮消失 | 同上 |
| TC-2.4 | 导航往返保位 | Happy | mock 26 条（24 条 completed 含 "Neon"） | 设搜索 `neon` + 筛选 `Completed`，加载较早至 24 条，列表 scrollTop=600；点侧栏 "Generate" 软导航到 `/workspace`，`goBack()` 返回 | 搜索词、筛选选中、已加载条数（游标栈重放）、列表 scrollTop（>400）全部保持 | 同上 |
| TC-2.5 | 搜索无匹配 | Edge | mock 三态条目 | 切 `Failed` 筛选后输入无匹配词 `aurora-void` | NoMatchFace 可见；搜索词与筛选保留；提供"清除搜索 / 切换筛选"行动；点清除后列表恢复（failed 1 条）、面消失 | 同上 |
| TC-2.6 | 未登录面 | Edge | session 未登录 + 列表 401；预置 localStorage 工作台状态 | 打开页面 | UnauthorizedFace 可见，含登录引导与返回工作台行动；`style-gen-workspace-state` 本地状态未被清空；无列表条目 | 同上 |
| TC-2.7 | 列表 5xx 面 | Error | 列表请求 500 | 打开页面后解除 500 mock 并点重试 | ListErrorFace 可见，文案提及 workspace，提供重试与返回工作台；重试后 3 条渲染、错误面消失 | 同上 |
| TC-2.8 | 首次无记录空态 | Happy（AC-07） | mock 空列表 | 打开页面 | EmptyFace 可见，含开始首次创作引导与返回工作台；无列表条目 | 同上 |
| TC-2.9 | 结果图 404 降级 | 降级（L1） | completed 条目 resultFileUrl 404 | 打开页面 | 条目降级为占位说明（unavailable/failed to load 类文案）；promptSummary、`16:9`、状态等其余信息保留 | 同上 |

### 实现方需提供的源码选择器契约（red → green 对齐用）

- `[data-testid="iteration-list"]` — 列表滚动容器（保位断言依赖其 `scrollTop`）
- `[data-testid="iteration-list-item"][data-status="completed|processing|failed"]` — 三态条目
- `[data-testid="iteration-state-face"][data-face="empty|no-match|unauthorized|error"]` — 四种状态面
- `textbox` 可访问名含 "Search iteration"；`radiogroup` 可访问名含 "Status filter"/"Filter by status"，选项 All/Processing/Completed/Failed
- `button`："Load earlier…"、"Clear search"、"Retry"；返回工作台动作（link 或 button，名称 /back to workspace|return to workspace|open workspace/i）

## 执行命令

```bash
pnpm e2e -- e2e/workspace-iteration-memory-list.spec.ts --project=workspace
```

## 关键失败输出摘录

- 汇总: `9 failed`（TC-2.1 ~ TC-2.9 全部失败，无跳过、无通过）
- 页面实际渲染为 Next.js 404（`/workspace/iterations` 路由不存在），错误上下文页面快照:

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e5]:
    - heading "404" [level=1] [ref=e6]
    - heading "This page could not be found." [level=2] [ref=e8]
```

- TC-2.1 首条断言失败（页面标题不存在）:

```text
Error: expect(locator).toBeVisible() failed
Locator: getByRole('heading', { name: /iteration memory/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found
```

- 其余用例失败形态一致，均为目标元素不存在:
  - TC-2.3/TC-2.9: `expect(locator).toHaveCount(expected) failed … Received: 0`（`iteration-list-item` 为 0）
  - TC-2.5/TC-2.6/TC-2.7/TC-2.8: `expect(locator).toBeVisible() failed`（`iteration-state-face[data-face=…]` 不存在）
- 失败截图: `test-results/workspace-iteration-memory-*/test-failed-1.png`

## 判定

- 失败原因是 plan-02 目标功能尚未实现：`src/app/workspace/iterations/page.tsx` 及其组件/hook/store 均不存在，dev server 对该路径返回 404；不是测试代码、mock shape、fixture 或环境错误（mock 层 `mockIterationList` 的 q/status/cursor 分支在请求到达前即被 404 短路，mock 与 helper 无运行时错误）。
- 结论: **预期失败，测试有效。**
