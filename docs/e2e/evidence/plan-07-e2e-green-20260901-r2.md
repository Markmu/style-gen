---
feat_id: plan-07
phase: green
date: 2026-09-01
---

# plan-07 E2E Green 证据 r2（Workspace 闭环集成与回归 · 修正轮）

本文件是 `docs/e2e/evidence/plan-07-e2e-green-20260901.md`（第一版）的**修正版**，不是替代删除：第一版文件本体保持原样不改，其执行数据（四条命令 0 failed）经 task-review 复核属实；本 r2 只修正其「存量 spec 对齐清单」节的不实覆盖声明（B-2），并在 B-1 修复后补录门外对齐文件的实际执行证据。

## 修正缘由（引用第一版证据与验收报告）

- 第一版证据: `docs/e2e/evidence/plan-07-e2e-green-20260901.md` L65–82 声明「以上对齐文件全部被命令 3/4 的 smoke 与 targeted 门覆盖并保持通过」。
- 验收报告: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/reviews/plan-07-review-20260901.md` blocker **B-2** 逐文件比对 `package.json` 的 `e2e:smoke` / `e2e:targeted` 清单证伪该声明：14 个对齐文件中仅 3 个在门内，其余 11 个不在任何门内、green 阶段实际未被执行；同报告 blocker **B-1** 在对门外文件加验时发现 `e2e/workspace-degradation.spec.ts` L256「生成错误Retry：内联错误主动重试后清除」确定性失败（用例缺 `GET /api/generation/{id}` 轮询 mock，401 触发 `signIn("google")` 导航离开 Workspace）。
- B-1 已由修复轮解决（见功能文件「执行记录」2026-09-01 节）：`e2e/workspace-degradation.spec.ts` 为 `degeneration-generation-retry-task` 补 `mockGenerationPolling`（completed 终态，`generation-completed.json` fixture + id 覆写），纯测试基建补充，非断言放宽。
- 本轮按 review 修复要求执行：更正门覆盖表述（下方「门归属更正」）+ B-1 修复后补录门外文件实际执行证据（下方「命令 5」）。

## 门归属更正（修正第一版 L65–82 的声明）

按 `package.json` 当前 `e2e:smoke` / `e2e:targeted` 脚本逐文件比对，14 个对齐文件的**真实**门归属为：

| # | 文件 | 门归属（更正后） |
| --- | --- | --- |
| 1 | `e2e/ai-first-shell.spec.ts` | **targeted 门内**（`e2e:targeted` 成员） |
| 8 | `e2e/workspace-ai-first-iteration-memory.spec.ts` | **smoke + targeted 门内** |
| 9 | `e2e/workspace-ai-first-render-dock.spec.ts` | **smoke + targeted 门内** |
| 2 | `e2e/degradation.spec.ts` | **门外**（任何门不含，仅手动全量 `pnpm e2e` 执行） |
| 3 | `e2e/edge-cases.spec.ts` | 门外 |
| 4 | `e2e/error-path.spec.ts` | 门外 |
| 5 | `e2e/happy-path.spec.ts` | 门外 |
| 6 | `e2e/helpers/workspace-actions.ts` | 门外（helper，非独立 spec；经消费方 spec 覆盖——happy-path / workspace-layout / workspace-prompt-generate 等，均在本轮命令 5 执行） |
| 7 | `e2e/style-memory-reuse.spec.ts` | 门外 |
| 10 | `e2e/workspace-degradation.spec.ts` | 门外（B-1 所在文件） |
| 11 | `e2e/workspace-history-strip.spec.ts` | 门外 |
| 12 | `e2e/workspace-layout.spec.ts` | 门外 |
| 13 | `e2e/workspace-prompt-generate.spec.ts` | 门外 |
| 14 | `e2e/workspace-three-column-layout.spec.ts` | 门外 |

即：**3 个门内（由命令 3/4 覆盖），11 个门外**（第一版「全部被命令 3/4 覆盖」的声明作废）。门外 11 项中 10 个为可独立运行的 spec，本轮命令 5 已实际执行且全绿（见下）；helper `workspace-actions.ts` 经其消费方 spec（happy-path 5、workspace-layout 8、workspace-prompt-generate 2 等用例，均在命令 5 内通过）覆盖。门内 3 个文件本轮亦随命令 3/4 再次通过（smoke/targeted 明细见下）。

## 执行命令与结果（五条全部通过，0 failed）

运行日期 2026-09-01（本地时钟 2026-09-02 凌晨，同一工作日会话口径与第一版一致）；运行 project `workspace`（端口 3001，`AUTH_REQUIRED=false`，mock API 全量拦截）。本轮未修改任何产品代码与测试断言（工作树仅含第一版 green 之后的 B-1 修复 diff，见功能文件执行记录）。

### 命令 1 — plan-07 主验证（三 spec，workspace project）

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts e2e/workspace-generation-dialog.spec.ts e2e/ai-first-visual-regression.spec.ts --project=workspace
```

**65 passed / 0 failed（2.0m，exit 0）**，与第一版 green 同口径：

| spec | 通过 | 失败 |
| --- | --- | --- |
| `workspace-evidence-guided-render-loop.spec.ts`（TC-2.1–2.10、TC-4.1–4.12、TC-5.1–5.15、TC-6.1–6.10、TC-7.1–7.8） | 55 | 0 |
| `workspace-generation-dialog.spec.ts`（进行中/成功/失败三条内联契约） | 3 | 0 |
| `ai-first-visual-regression.spec.ts`（TC-8.1–8.5 + TC-7.9） | 7 | 0 |

### 命令 2 — pnpm verify:fast

```bash
pnpm verify:fast
```

**通过（exit 0）**：`workflow:check` ✓、`test:workflow` ✓、`type-check` ✓、`lint` ✓、Vitest 单元/组件 **118 文件 1211 passed / 0 failed（5.74s）**。本轮无 flake，未放宽任何断言。

### 命令 3 — pnpm verify:full

```bash
pnpm verify:full
```

**通过（exit 0）**：verify:fast 全绿 + `next build` 生产构建成功 + `e2e:smoke`（4 spec：smoke、ai-first-design-system、workspace-ai-first-iteration-memory、workspace-ai-first-render-dock）**18 passed / 0 failed（21.3s）**。

### 命令 4 — pnpm verify:acceptance（最终发布前验收门）

```bash
pnpm verify:acceptance
```

**通过（exit 0）**：verify:fast 全绿 + `next build` 成功 + `e2e:targeted`（14 个 targeted spec）**148 passed / 0 failed（3.2m）**。TC-7.1–TC-7.8 在本门内再次通过；对齐文件中门内 3 个（`ai-first-shell`、`workspace-ai-first-iteration-memory`、`workspace-ai-first-render-dock`）在本门内通过。

### 命令 5 — 门外 10 个对齐存量 spec 联跑（B-2 补录：实际执行证据）

```bash
pnpm e2e -- e2e/degradation.spec.ts e2e/edge-cases.spec.ts e2e/error-path.spec.ts e2e/happy-path.spec.ts e2e/style-memory-reuse.spec.ts e2e/workspace-degradation.spec.ts e2e/workspace-history-strip.spec.ts e2e/workspace-layout.spec.ts e2e/workspace-prompt-generate.spec.ts e2e/workspace-three-column-layout.spec.ts --project=workspace
```

**60 passed / 0 failed（1.1m，exit 0）**，逐文件：

| spec | 通过 | 失败 |
| --- | --- | --- |
| `degradation.spec.ts` | 7 | 0 |
| `edge-cases.spec.ts` | 3 | 0 |
| `error-path.spec.ts`（含 red 预告的两条生成失败内联对齐） | 6 | 0 |
| `happy-path.spec.ts` | 5 | 0 |
| `style-memory-reuse.spec.ts` | 14 | 0 |
| `workspace-degradation.spec.ts`（含 B-1 原失败用例 L256「生成错误Retry：内联错误主动重试后清除」） | 6 | 0 |
| `workspace-history-strip.spec.ts` | 2 | 0 |
| `workspace-layout.spec.ts` | 8 | 0 |
| `workspace-prompt-generate.spec.ts` | 2 | 0 |
| `workspace-three-column-layout.spec.ts`（含 TC-1.5 1280/1440 两视口） | 7 | 0 |

B-1 核销：review 两轮复跑均红的 `workspace-degradation.spec.ts:256` 用例在本联跑中通过（[41/60]），与修复轮单文件复跑 6/6 一致。门外 helper `e2e/helpers/workspace-actions.ts` 无独立用例，其消费方（happy-path / workspace-layout / workspace-prompt-generate 等 15 用例）全部在本命令内通过。

## 验收对照（沿用第一版，本轮复核未变）

- AC-01～AC-07：主 spec 55 条（TC-7.1–TC-7.8 收口 + plan-02/04/05/06 分组回归）命令 1 green。
- US-01～US-11：US 覆盖矩阵由 TC-2.x/4.x/5.x/6.x/7.x 与 targeted 门（148 条）追踪，无孤立或弱化 AC。
- §8.2 降级回归：L1→TC-7.5、L2→TC-7.6（+TC-5.13/5.14）、L3→TC-7.3（+TC-5.4）、L4→TC-7.7（+TC-2.6/2.10 与 `degradation.spec.ts`）、L5→TC-7.8 全部 green；门外存量降级 spec（degradation 7 + workspace-degradation 6 + error-path 6）本轮命令 5 实跑全绿。
- 焦点/键盘（TC-7.4）与三视口视觉（TC-7.9，1440×900 / 1280×800 / 390×844 + 1280×720 DOM 守卫）green。
- 全局质量门：命令 2/3/4（verify:fast / verify:full / verify:acceptance）全部通过。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`（按运行顺序被每次 run 覆盖，最终内容为命令 5 的 60/60；各命令结果以本证据记录为准）
- 失败截图/trace: 无（五条命令 0 failed，`test-results/` 无失败产物）
- 本阶段未修改任何产品代码、测试代码、README「## 5. 开发状态机」与 plan-07 frontmatter（status 保持 `review`）；第一版 green 证据文件本体未改动。

## 结论

**通过**。plan-07 验证命令在本修正轮全部通过：主验证 65/65、verify:fast（Vitest 1211/1211 + workflow/type/lint）、verify:full（smoke 18/18）、verify:acceptance（targeted 148/148）、门外 10 个对齐存量 spec 联跑 60/60（B-1 原失败用例转绿）。第一版证据的不实门覆盖声明已按 B-2 要求更正（3 门内 / 11 门外），门外可执行文件已全部实跑补证，14 个对齐文件证据链闭合。plan-07 可携本 r2 证据（连同第一版四命令数据）重新提交 task-review。
