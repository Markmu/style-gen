---
feat_id: plan-08
phase: green
date: 2026-07-07
spec: e2e/ai-first-visual-regression.spec.ts
---

# plan-08 Green E2E Evidence

## 范围

- 功能文件：`docs/12-全站AI优先界面风格复刻/12-2-实现计划-全站AI优先界面风格复刻/plan-08-第12期Targeted-E2E与视觉QA.md`
- 架构文档：`docs/12-全站AI优先界面风格复刻/12-1-架构文档-全站AI优先界面风格复刻.md`
- Red evidence：`docs/e2e/evidence/plan-08-e2e-red-20260707.md`
- Red spec：`e2e/ai-first-visual-regression.spec.ts`
- 用例文档：`docs/e2e/12-e2e-用例-全站AI优先界面风格复刻.md`
- phase：green
- feat_id：plan-08

## Targeted E2E Bundle

命令：

```bash
pnpm e2e -- e2e/ai-first-design-system.spec.ts e2e/ai-first-shell.spec.ts e2e/workspace-ai-first-evidence.spec.ts e2e/workspace-ai-first-render-dock.spec.ts e2e/workspace-ai-first-iteration-memory.spec.ts e2e/ai-first-style-memory.spec.ts e2e/ai-first-landing-states.spec.ts e2e/ai-first-visual-regression.spec.ts
```

结果：通过。

统计：

| Project | Passed | Failed | Total |
| --- | ---: | ---: | ---: |
| workspace | 47 | 0 | 47 |

HTML report：

- `playwright-report/index.html`

复核说明：

- 首轮完整 bundle 出现 2 个偶发失败：`e2e/ai-first-shell.spec.ts` 的 TC-2.1 Style Memory shared AppShell 与 TC-2.2 Style Memory active nav，首轮统计为 45 passed / 2 failed。
- 首轮失败截图和 error context 显示页面最终已渲染 `banner`、顶部 `Style Memory` nav 和侧栏选中态，失败表现为 5s 等待窗口内的 shell/nav locator 未及时满足。
- 随后单独复跑 `pnpm e2e -- e2e/ai-first-shell.spec.ts`，结果为 9 passed。
- 最后复跑同一第 12 期 targeted bundle，结果为 47 passed。当前 `test-results/` 下无失败截图、trace 或 error context。

说明：

- targeted bundle 覆盖 AC-01..AC-09：DesignTokenLayer、AppShell、Workspace Evidence/Prompt、Render Dock、Iteration Memory、Style Memory、Landing/Auth/empty/error states 和视觉 QA。

## 视觉 QA

结果：通过。

覆盖：

- 视口：1440x900、1280x800、390x844。
- 页面/状态：Landing、Workspace idle、Workspace analysis_ready、Workspace generation failed、Style Memory populated/empty/noResults/authRequired。
- 关键断言：主 UI 非空、关键 `data-testid` 可见、可见文本不重叠、按钮文本不溢出、Render Dock 不遮挡 Prompt editor、StatePresenter action 可见、Style Memory 卡片不退回旧文件列表。

Red 修复点：

- `output-card` 与 `unified-prompt-editor` 几何重叠已修复。实现侧增加 Prompt compact editor 高度约束、textarea 内部 compact 模式、Render Dock 紧凑 readiness 布局；视觉 QA helper 改为按可见裁剪矩形检测，避免把滚动容器中不可见内容误报为重叠。
- Style Memory 卡片已暴露 `data-testid="style-memory-card"` 和 `data-has-source-image`，保留 `.style-memory-card`、`Style tags`、`Reuse intent`。
- `workspace-actions` 不再依赖旧 `Ready to Generate` / `floating-generate-window` 主路径。

统计：

| Spec | Passed | Failed | Total |
| --- | ---: | ---: | ---: |
| `e2e/ai-first-visual-regression.spec.ts` | 5 | 0 | 5 |

## Legacy 迁移/隔离

已处理 plan-08 指定文件：

- `e2e/precision-glass-home.spec.ts`：迁移到 AI-first Landing / Style Memory 文案。
- `e2e/precision-glass-shell.spec.ts`：迁移到 AppShell / Style Memory nav 语义。
- `e2e/template.spec.ts`：列表和操作断言迁移到 Style Memory；保留现有 template API contract。
- `e2e/workspace-two-pane.spec.ts`：改为 Phase 12 三栏主体验兼容检查，不再断言旧主布局。
- `e2e/helpers/workspace-actions.ts`：改为等待 `workspace-three-column-layout`、`ai-status-header[data-phase="analysis_ready"]` 和 Render Dock `Generate`。

## 旧体系残留扫描

命令：

```bash
rg -n "Template Library|workspace-two-pane-layout|floating-generate-window|Ready to Generate|GenerateHistoryBar|No templates yet" src e2e docs/e2e
```

结果：命中 111 行，需要分类记录，未作为失败处理。

允许 / 说明性命中：

- 第 12 期 targeted specs 和单元测试中的负断言，例如 `toHaveCount(0)` / `queryByText(...)`，用于证明旧主文案不出现。
- `docs/e2e/12-e2e-用例-全站AI优先界面风格复刻.md` 和历史 evidence 中描述旧体系迁移背景、red 失败原因或扫描命令本身。
- 旧组件源码和对应单测仍保留：`floating-generate-window.tsx`、`generate-history-bar.tsx`、`workspace-two-pane-layout.tsx`。这些不作为第 12 期 targeted gate，未在主 Workspace 路径中作为最终验收入口。

未清零但隔离为非第 12 期 targeted gate：

- `e2e/happy-path.spec.ts`、`e2e/edge-cases.spec.ts`、`e2e/analysis-template-autofill.spec.ts`、`e2e/workspace-generation-dialog.spec.ts`、`e2e/degradation.spec.ts`、`e2e/workspace-layout.spec.ts`、`e2e/workspace-degradation.spec.ts`、`e2e/error-path.spec.ts` 仍含旧 `Ready to Generate` / `floating-generate-window` / `workspace-two-pane-layout` 断言。它们是历史 legacy specs，不属于 plan-08 targeted bundle。
- `src/components/workspace/floating-generate-window.tsx`、`src/components/workspace/generate-history-bar.tsx`、`src/components/workspace/light-generate-panel.tsx`、`src/components/workspace/workspace-two-pane-layout.tsx` 是保留源码或 legacy wrapper 命中，不作为本期主体验入口。

不允许命中处理：

- plan-08 指定的四个 legacy 文件已经迁移/隔离；剩余命中均为负断言、历史说明、非 targeted legacy specs 或保留源码。
- 未发现第 12 期 targeted bundle 的正向预期仍依赖旧主体验。

## 失败截图 / Trace

- 最终通过 run：无失败截图、无 trace、无 error context。
- 首轮偶发失败 artifact 在后续通过 run 后已被 Playwright 清理；本 evidence 以上方复核说明记录首轮失败场景和结论。

## 本轮未执行项

- 本 green-e2e agent 只执行 plan-08 targeted E2E、shell 复核和残留扫描；未运行 `pnpm type-check`、`pnpm lint`、`pnpm test` 或 `pnpm build`。
- 未修改 README `## 5. 开发状态机`，未执行 task-review，未推进功能状态。

## 结论

结论：通过。

plan-08 targeted E2E 总门最终 47 passed，视觉 QA 5 passed，旧体系残留扫描已分类且未发现第 12 期 targeted bundle 的正向预期仍依赖旧主体验。无当前失败截图/trace，无阻塞项。README `## 5. 开发状态机` 未修改，保留给主 agent 维护。
