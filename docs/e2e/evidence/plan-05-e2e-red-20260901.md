---
feat_id: plan-05
phase: red
date: 2026-09-01
---

# plan-05 E2E Red 证据（本次结果区与内联比较）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.1.5/3.1.6（方向 feed、结果选择与比较）、§3.2（排队转成功/失败、方向查询失败、对旧结果应用调整行）、ADR-5（复用生成查询不建会话结果存储）、ADR-7（内联 focus-managed 比较区，不用弹层/自动评分）、§6.4（生成、方向结果与首选）、§6.5（参考比较与局部调整）、§7.2/7.4（DirectionIterationFeed / DirectionIterationListItem / 状态流转）、§8.2（L2 列表/图片失败、L3 Provider 失败）
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-05-本次结果区与内联比较.md`（status: ready-to-dev，Task 1：red 补结果/比较 E2E 场景）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（本次增量新增 plan-05 分组 TC-5.1–TC-5.15，plan-02/04 分组未改动）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（在既有主 spec 末尾追加 plan-05 describe，workspace project，mock API 全量拦截，不触达 live Provider/R2）

## 场景覆盖（plan-05 实现规格 §1–§4 / 验收 AC-04、AC-05、AC-06、AC-07）

| 用例 | 场景 | 类型 |
| --- | --- | --- |
| TC-5.1 | 方向结果区三组状态同时内联呈现：5 张真实成功缩略图 + active face + latestFailure（截断原因 + 主动重试）并存且互不挤占；direction GET query 契约 `view=direction`+`analysisTaskId`+`pageSize=5` | Happy |
| TC-5.2 | 手动生成 queue→processing→success 全程内联：POST 后 active face 出现，终态新成功进 completed 最新位并自动成为瞬时 selected，绝不自动成为 preferred（AC-04/AC-06） | Happy |
| TC-5.3 | 六个成功结果只显示最新五个（pageSize=5 契约 + 最旧条目不进首屏缩略图），更旧结果保留「打开完整 Iteration」入口 | Edge |
| TC-5.4 | 最近失败内联显示截断原因；主动重试恰好一次 POST 创建新任务（active face 为新 id），原任务保持 failed 终态不复活（架构 §7.4） | Error |
| TC-5.5 | 当前选择与本次首选分离：preferred 只由用户操作写入（aria-pressed/data-preferred），切换 selected 不影响 preferred（AC-06） | Edge |
| TC-5.6 | 打开比较加载所选 Iteration：真实双图（参考/结果均真实 URL）、历史 Prompt 快照与「正在调整当前草稿」边界、打开聚焦标题、polite live region（ADR-7 / §6.5.1） | Happy |
| TC-5.7 | 多 invariant 维度：该维度全部真实 invariants/observations/segments 呈现，未选具体规则前四动作 disabled，选择真实规则后开放（§6.5.3） | Edge |
| TC-5.8 | 单 invariant 维度：唯一规则可见地预选（aria-pressed=true），四动作直接可用 | Edge |
| TC-5.9 | 零 invariant 维度：「该维度暂无可调整规则」，四动作 disabled，仅保留「其他/全文编辑」，不伪造 invariant/adjustment | Edge |
| TC-5.10 | 应用调整只更新当前草稿：重编译出现「严格保留」表达、焦点移至对应摘要项（keep-change-item data-target-id）、零 generation POST（AC-05 应用不自动生成） | Happy |
| TC-5.11 | 取消比较零写入：草稿逐字不变、面板关闭、焦点回比较触发器（direction-item-compare） | Edge |
| TC-5.12 | 「其他」维度进入全文编辑：editor mode 切 text 并聚焦 fulltext-prompt-editor（§6.5.2） | Edge |
| TC-5.13 | 方向 feed 失败（L2）：previous data 缩略图与草稿保留、提供重试，重试后恢复并更新（AC-07） | Error |
| TC-5.14 | 比较详情失败：rail 与草稿保留，提供重试与打开 Iteration 动作，重试后真实双图渲染（AC-07） | Error |
| TC-5.15 | completed 缺结果资产：显示来源异常（data-asset-missing）、不渲染假图、比较/首选等结果动作 disabled（§7.4） | 降级 |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
```

## 结果与失败摘要

**37 个用例：22 passed / 15 failed**（plan-02 TC-2.1–TC-2.10 与 plan-04 TC-4.1–TC-4.12 共 22 条既有用例全部通过保持绿；plan-05 TC-5.1–TC-5.15 全部失败）。

失败点分布（每条均为 plan-05 功能入口元素不存在）：

| 用例 | 失败 locator（Timeout 10000ms, element(s) not found） |
| --- | --- |
| TC-5.1 / TC-5.2 / TC-5.3 / TC-5.4 / TC-5.5 / TC-5.13 / TC-5.15 | `getByTestId('direction-result-rail')`（rail 未挂载） |
| TC-5.6 / TC-5.7 / TC-5.8 / TC-5.9 / TC-5.10 / TC-5.11 / TC-5.12（经共享 `openComparison`）/ TC-5.14 | `getByTestId('direction-result-rail')`（rail 未挂载，比较面板入口不可达） |

典型失败输出：

```text
Error: expect(locator).toBeVisible() failed
Locator: getByTestId('direction-result-rail')
Expected: visible
Timeout: 10000ms
Error: element(s) not found
  Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByTestId('direction-result-rail')
```

- 失败是功能未实现，不是测试/环境错误：plan-05 文件清单 create 项 `src/hooks/use-direction-iterations.ts`、`src/components/workspace/direction-result-rail.tsx`、`src/components/workspace/result-comparison-panel.tsx` 尚不存在，modify 项 `comparison-view.tsx` / `workspace/page.tsx` 尚未挂载方向 feed、结果 rail 与内联比较接线，故 rail 及其全部下游断言（active/failure face、selected/preferred、比较面板、维度→invariant、四动作、feed 错误位）均不可达。
- 15 个失败目录的 Page snapshot（`test-results/workspace-evidence-guided--*-workspace/error-context.md`）均不含 `direction-result-rail` / `result-comparison-panel`，三栏工作台与 plan-02/04 控件正常渲染，证明仅缺少 plan-05 UI 入口。
- plan-02/04 的 22 条既有用例在同一运行中全部通过，证明 dev server、mock（session/presign/analysis/generation/cdn/direction feed）与测试环境本身正常。
- 失败截图（15 张）: `test-results/workspace-evidence-guided--*-workspace/test-failed-1.png`。
- spec/helper lint 通过（`pnpm exec eslint e2e/workspace-evidence-guided-render-loop.spec.ts e2e/helpers/mock-api.ts` 0 errors）。

## 测试基建披露

- 扩展 `e2e/helpers/mock-api.ts`（mock 允许惯例，本次为 plan-05 必需的 API mock 扩展）：
  - `mockDirectionFeedStateful(page, initialFeed, options?)` — 拦截 `GET /api/generation?view=direction&…` 返回 `DirectionIterationFeed` DTO；`set(feed)` 推进服务端事实（active→completed 终态迁移由测试时序驱动，不受请求次数影响）、`fail({status, body})` 进入 L2 错误；非 direction 请求 `route.fallback()` 给先注册的 `mockGenerationList`；`onRequest` 暴露 `view/analysisTaskId/pageSize`。
  - `mockIterationDetailStateful(page, detail)` — 单条详情可控状态 mock：`fail()` 后详情 GET 全部 503（retryable）、`set(detail)` 恢复，驱动 TC-5.14。
  - `MockIterationDetail` 接口增补可选字段 `promptControlSnapshot?: Record<string, unknown> | null`（plan-03 详情 DTO 契约；可选保持既有 spec 构造兼容，本次未改动任何既有 helper 行为）。
- 新增 fixture: `e2e/fixtures/api-responses/analysis-v2-completed-dual-invariant.json`（复制 `analysis-v2-completed.json`，color 维度补第二条 observation `color_2` 与 soft invariant `color_invariant_2`，驱动 TC-5.7 多 invariant 分支）。
- spec 内共享构造器：`directionItem` / `activeItem` / `latestFiveCompleted`（feed 条目 DTO）、`completedIterationDetail`（详情 DTO，历史 promptSnapshot 故意与当前草稿不同以区分「历史上下文 vs 当前草稿」）、`openComparison`（seed feed+detail、完成分析、打开比较并断言标题聚焦）。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图（15 张）: `test-results/workspace-evidence-guided--*-workspace/test-failed-1.png`
- 本次修改/新增:
  - `e2e/workspace-evidence-guided-render-loop.spec.ts`（追加 plan-05 describe：TC-5.1–TC-5.15 + 共享构造器/helper）
  - `e2e/helpers/mock-api.ts`（新增 direction feed/detail stateful mock 与 `promptControlSnapshot` 可选字段）
  - `e2e/fixtures/api-responses/analysis-v2-completed-dual-invariant.json`（新增）
  - `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（增量 plan-05 分组 + 汇总更新）

## 结论

预期失败，测试有效。plan-05 的 15 条用例全部因 plan-05 功能（direction feed hook、本次结果 rail、内联比较面板与 selected/preferred/adjustment 接线）未实现而失败；plan-02/04 的 22 条既有用例保持通过。implementer 可以本证据为开始条件，按用例文档 plan-05 分组中的 data-testid / mock 契约实现至 green。
