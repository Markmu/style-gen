---
feat_id: plan-02
phase: green
date: 2026-08-17
---

# plan-02 E2E Green 证据（Iteration Memory 列表页）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §3.3（列表状态机）、§6.1（进入与检索链路）、§7.2（IterationListItem）、§8.2（降级策略）、ADR-6（视图状态保活）
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-02-IterationMemory列表页.md`（status: review，Task 9 = 实现至 E2E green）
- 计划 README: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/README.md`
- Red 证据: `docs/e2e/evidence/plan-02-e2e-red-2026-08-17.md`（9 failed / 0 passed，预期失败，测试有效；用例清单 TC-2.1 ~ TC-2.9 见该文件）
- spec 文件: `e2e/workspace-iteration-memory-list.spec.ts`（workspace project，mock 模式，无需 live 凭证）
- 用例清单: 嵌入于 red 证据文件（任务边界约束不改 plan-02 文件清单之外的文件），本 green 运行覆盖同一批用例、同一 spec

## 用例清单（与 red 证据同表，9 用例）

| # | 场景 | 类型 | 结果 |
|---|------|------|------|
| TC-2.1 | 直达默认全状态三态列表（completed 真实预览 / processing、failed 状态面无图；首请求 `status=all`） | Happy | 通过 |
| TC-2.2 | 搜索 + 状态筛选组合收窄（q=neon → 2 条；叠加 failed → 1 条；URL 写回 q/status） | Happy | 通过 |
| TC-2.3 | "继续浏览较早记录"游标加载（20 → 25 条、无重复、第二页携带非空 cursor、耗尽后按钮消失） | Happy | 通过 |
| TC-2.4 | 导航往返保位（搜索词、筛选选中、已加载深度 24 条、scrollTop > 400 全保持） | Happy | 通过 |
| TC-2.5 | 搜索无匹配（NoMatchFace 保留条件 + 清除搜索 / 切换筛选行动；清除后恢复 1 条） | Edge | 通过 |
| TC-2.6 | 未登录面（UnauthorizedFace 登录引导 + 返回工作台；`style-gen-workspace-state` 本地状态未清空） | Edge | 通过 |
| TC-2.7 | 列表 5xx 面（ListErrorFace 文案提及 workspace + 重试 + 返回工作台；重试后 3 条渲染、错误面消失） | Error | 通过 |
| TC-2.8 | 首次无记录空态（EmptyFace 首次创作引导 + 返回工作台；无列表条目） | Happy（AC-07） | 通过 |
| TC-2.9 | 结果图 404 降级（占位说明文案；promptSummary、16:9、completed 状态等其余信息保留） | 降级（L1） | 通过 |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-iteration-memory-list.spec.ts --project=workspace
```

## 结果输出摘录

```text
Running 9 tests using 1 worker

[1/9] [workspace] › TC-2.1 direct visit defaults to all statuses with real previews only for completed
[2/9] [workspace] › TC-2.2 keyword search and status filter combine to narrow the list
[3/9] [workspace] › TC-2.3 "load earlier" appends the older cursor page without duplicates
[4/9] [workspace] › TC-2.4 returning from the workspace preserves search, filter, loaded depth, and scroll
[5/9] [workspace] › TC-2.5 no search match keeps the conditions and offers clear and switch actions
[6/9] [workspace] › TC-2.6 signed-out visitors get a login guide without wiping local workspace state
[7/9] [workspace] › TC-2.7 list 5xx explains the workspace is unaffected and retry recovers
[8/9] [workspace] › TC-2.8 first visit with no records guides the first creation
[9/9] [workspace] › TC-2.9 a broken result image degrades to a placeholder without losing item info

9 passed (21.4s)
```

- 通过/失败数量: **9 passed / 0 failed / 0 skipped**
- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图/trace: 无（全部通过，未产生失败工件）

## 与 red 证据的对照说明

- 同一 spec、同一命令、同一 mock helper（`e2e/helpers/mock-api.ts` 的 `mockIterationList` / `mockLoggedOutSession` / `mockApiError` / `mockAuthSession`），red 阶段 9 用例因 `/workspace/iterations` 路由 404 全部失败；实现（plan-02 Task 2-8）完成后同一 spec 全绿，红绿对照成立。
- red 证据中实现方选择器契约全部落地：`iteration-list` / `iteration-list-item[data-status]` / `iteration-state-face[data-face]` / 搜索与筛选可访问名 / "Load earlier…" 等行动按钮。
- 本 green 运行未修改 spec 文件（实现期两条 spec 修正——TC-2.4 到达等待、TC-2.6 mock 保真度——已在功能文件"实现期发现与修正记录"中说明，本次运行原样通过）。

## 结论

**通过。** `pnpm e2e -- e2e/workspace-iteration-memory-list.spec.ts --project=workspace` 9/9 全绿（21.4s），覆盖 plan-02 §5 E2E 验收全部场景（三态列表渲染、搜索/筛选组合、加载较早、导航往返保位、无匹配行动、未登录面、列表 5xx 面、空态、L1 图片降级）。plan-02 green-e2e 步骤完成，可进入 review。
