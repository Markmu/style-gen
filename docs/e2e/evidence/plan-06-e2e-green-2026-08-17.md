---
feat_id: plan-06
phase: green
date: 2026-08-17
---

# plan-06 E2E Green 证据（入口接线与全流程集成）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §4.2（入口与沉淀模块：近期条"查看全部"与左侧导航 Iterations 项）、§2.3（成功标准与 US-01~US-10 全流程覆盖）、§9 Phase C（集成回归验证目标）
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-06-入口接线与全流程集成.md`（status: review，实现已完成，本证据为 green_e2e 步骤产物）
- 计划 README: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/README.md`
- red 证据: `docs/e2e/evidence/plan-06-e2e-red-2026-08-17.md`（同一 spec，4/4 预期失败——TC-6.1/6.2/6.4 落 `/history` 死链 404、TC-6.3 导航无 Iterations 项，判定"预期失败，测试有效"）
- spec 文件: `e2e/workspace-iteration-memory-integration.spec.ts`（workspace project，mock 模式，无需 live 凭证；全部复用既有 helper，未新增 mock/fixture）
- 用例列表: 见 red 证据同文件「用例清单」节（plan-03/04/05 先例：plan-task 用例表嵌入 evidence 而非独立用例文档）

## 用例清单（与 red 阶段同一 spec、同一映射）

| # | 场景 | 类型 | 覆盖 | 结果 |
|---|------|------|------|------|
| TC-6.1 | 全流程主线（近期条 → 列表 → 详情 → 恢复 → 生成 → 保存 → 打开） | Happy（US-01~05/07 端到端 + AC-01） | AC-01 | 通过 |
| TC-6.2 | 近期条"查看全部"入口回归（默认参数 completed-only 不变 + 目标页 status=all） | Happy + 回归 | AC-01 | 通过 |
| TC-6.3 | 左侧导航 Iterations 项（存在、可切换、高亮、不破坏既有导航） | Happy + 回归 | AC-01 | 通过 |
| TC-6.4 | 近期条无完成结果时入口仍可用并到达全状态列表 | Edge | AC-07, AC-01 | 通过 |

## US-01~US-10 覆盖对应关系（架构 §2.3 成功标准，plan-06 §5 全流程验收矩阵）

| US | 用户故事简述 | 本 spec 覆盖点 | 专项分支承载（本期均绿） |
|----|--------------|----------------|--------------------------|
| US-01 | 查看全部生成尝试 | TC-6.1/6.2/6.4：近期条"View all" → `/workspace/iterations?status=all`，三态（completed/processing/failed）条目渲染，All 单选选中 | plan-02 列表 spec |
| US-02 | 按提示内容和状态查找 | TC-6.1：搜索 `neon`（3→2 条）+ 筛选 completed（2→1 条），URL `q`/`status` 同步 | plan-02 列表 spec（组合/无匹配） |
| US-03 | 详情完整上下文 | TC-6.1：打开已完成详情（`data-status=completed`、`data-iteration-id`），保存对话框可见快照变量（subject/environment） | plan-03 详情 spec（并排展示 + 分区块） |
| US-04 | 安全带回工作台 | TC-6.1：守卫确认后恢复快照（提示/排除项/16:9/hd/来源图/上一轮结果），恢复零 POST；修改提示 Generate 断言 `POST /api/generation` 体（promptText=修改后、negativePromptText/analysisTaskId/params=快照值） | plan-04 恢复 spec |
| US-05 | 恢复前明确提醒 | TC-6.1：`replace-confirm-dialog` 展示当前与目标两侧提示摘要（CURRENT_PROMPT / TARGET_PROMPT）后确认 | plan-04 恢复 spec（取消分支） |
| US-06 | 失败记录保留完整上下文 | TC-6.1/6.2/6.4：全状态列表中 failed 条目可见（`iteration-list-item[data-status=failed]`） | plan-03 详情 spec（failed）+ plan-04 恢复 spec（修正并继续） |
| US-07 | 保存为 Style Memory | TC-6.1：保存对话框预填 promptSnapshot、恰 1 次 `POST /api/templates`（name/content/sourceAssetId/sourceGenerationTaskId）、已保存态 + Open 定位 `style-memory-card[data-focused=true]` + URL 清参 | plan-05 保存 spec（冲突/重复进入等） |
| US-08 | 进行中任务阶段可见 | TC-6.1/6.2/6.4：全状态列表中 processing 条目可见；TC-6.1 新迭代生成走 processing 轮询 mock | plan-03 详情 spec（processing 轮询切换） |
| US-09 | 关闭详情停留原位置 | TC-6.1：列表检索态（q=neon、completed 筛选）下进详情再回列表不重置（由主线隐含覆盖） | plan-02 列表 spec（返回保位专项） |
| US-10 | 登录/服务问题说明保留 | 本 spec 前置 `mockAuthSession`（正常会话路径） | plan-02 列表 spec（未登录/5xx 状态面专项） |

> 主线由 TC-6.1 端到端走通（近期条 → 查看全部 → 列表 → 详情 → 守卫恢复 → 新迭代生成 → 保存 → 打开定位）；US-02/03/05/06/08/09/10 的专项分支由 plan-02~05 各自 E2E 承载（本期各 spec 均绿，含本日 `pnpm e2e:targeted` 实跑）。US-01~US-10 全部可走通。

## 执行命令

```bash
pnpm e2e -- e2e/workspace-iteration-memory-integration.spec.ts --project=workspace
```

## 结果输出摘录

```text
Running 4 tests using 1 worker

[1/4] [workspace] › e2e/workspace-iteration-memory-integration.spec.ts:327:7 › plan-06 entry wiring and full Iteration Memory journey › TC-6.1 full journey: view-all entry → three-state list → search/filter → detail → guarded continue → restore → generate → save as Style Memory → open
[2/4] [workspace] › e2e/workspace-iteration-memory-integration.spec.ts:470:7 › plan-06 entry wiring and full Iteration Memory journey › TC-6.2 recent-strip View all reaches the full list with default all status while the strip keeps its completed-only default query
[3/4] [workspace] › e2e/workspace-iteration-memory-integration.spec.ts:501:7 › plan-06 entry wiring and full Iteration Memory journey › TC-6.3 the left sidebar exposes an Iterations nav item that switches and highlights without breaking existing entries
[4/4] [workspace] › e2e/workspace-iteration-memory-integration.spec.ts:534:7 › plan-06 entry wiring and full Iteration Memory journey › TC-6.4 View all stays usable from an empty recent strip and reaches the full list

4 passed (19.5s)
```

- 汇总: `4 passed`（TC-6.1 ~ TC-6.4），0 failed，0 flaky，0 skipped（exit code 0）
- 稳定性确认: 同日同命令复跑一次，同样 `4 passed (20.1s)`，无 flaky
- HTML 报告: `playwright-report/index.html`
- 失败截图/trace: 无（全绿，未产生失败产物）

## 判定

- red → green 对照: 同一 spec、同一 4 用例（TC-6.1~6.4），无任何行为断言放宽。red 阶段 4/4 失败源于 plan-06 目标缺口（`page.tsx` onViewAll 死链 `/history`、`left-sidebar.tsx` 无 Iterations 导航项）；实现交付接线修正（`router.push("/workspace/iterations?status=all")`）、左侧导航 Iterations 项（aria-current 高亮同现有项）后，4/4 通过。
- 关键回归断言在功能存在前提下逐项成立: 近期条默认参数（pageSize=20、无 status/q → 服务端 completed-only）不变（TC-6.2）；既有 Generate / Style Memory Library 导航项并列不回归（TC-6.3）；恢复动作零生成请求、生成 POST 体与模板 POST 体字段逐项断言（TC-6.1）。
- 本步骤未修改 spec 与任何源码文件（无测试自身缺陷需修正、无实现阻塞）。
- 结论: **通过。**
