---
feat_id: plan-05
phase: green
date: 2026-09-01
---

# plan-05 E2E Green 证据（本次结果区与内联比较）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.1.5/3.1.6、§3.2、ADR-5、ADR-7、§6.4、§6.5、§7.2/7.4、§8.1/8.2
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-05-本次结果区与内联比较.md`（status: review，Task 7：green 保存证据）
- Red 证据: `docs/e2e/evidence/plan-05-e2e-red-20260901.md`（37 用例：22 passed / 15 failed，plan-05 TC-5.1–TC-5.15 全部预期失败）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（plan-05 分组 TC-5.1–TC-5.15）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（workspace project，mock API 全量拦截，不触达 live Provider/R2）
- implement 阶段追加修复记录: 功能文件「## 执行记录」章节《2026-09-01 implement 完成后的全量回归修复：Prompt 卡编辑区压入 Render Dock（TC-8.2/8.3）》（`prompt-card.tsx` frame 下限/标题边距最小修复 + `direction-result-rail.tsx` 空态紧凑化；视觉 spec 断言未改动，其验证执行记录见该章节「验证」行）

## 执行命令（plan-05 验证命令三条，逐条执行）

```bash
pnpm vitest --run src/hooks/__tests__/use-direction-iterations.test.tsx src/components/workspace/__tests__/direction-result-rail.test.tsx src/components/workspace/__tests__/result-comparison-panel.test.tsx src/components/workspace/__tests__/comparison-view.test.tsx
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
pnpm verify:fast
```

## 结果

### 1. 组件/单测（vitest 四文件）

- **4 文件 / 36 测试：36 passed / 0 failed**
  - `src/components/workspace/__tests__/direction-result-rail.test.tsx`（11 tests）
  - `src/components/workspace/__tests__/result-comparison-panel.test.tsx`（12 tests）
  - `src/hooks/__tests__/use-direction-iterations.test.tsx`（6 tests）
  - `src/components/workspace/__tests__/comparison-view.test.tsx`（7 tests）
- 运行输出存在 React `act(...)` 警告（stderr 噪音，非失败），全部测试通过。

### 2. E2E（workspace project）

- **37 用例：37 passed / 0 failed**（56.2s）
  - plan-02 TC-2.1–TC-2.10（10 条）与 plan-04 TC-4.1–TC-4.12（12 条）既有用例保持绿
  - **plan-05 TC-5.1–TC-5.15（15 条）red 阶段全部失败，本阶段全部转绿**：
    - TC-5.1 三组状态内联并存（5 成功缩略图 + active face + latestFailure 截断原因/重试，direction GET 契约）
    - TC-5.2 queue→processing→success 全程内联；新成功自动成为瞬时 selected、不自动成为 preferred
    - TC-5.3 六个成功仅显示最新五个，更旧结果保留「打开完整 Iteration」
    - TC-5.4 失败截断原因内联；主动重试恰好一次 POST 创建新任务，原任务保持 failed 不复活
    - TC-5.5 selected 与 preferred 分离（preferred 仅用户操作写入，aria 状态不同）
    - TC-5.6 打开比较：真实双图、历史 Prompt 快照与「正在调整当前草稿」边界、标题聚焦、polite live region
    - TC-5.7 多 invariant 维度：未选规则前四动作 disabled，选中后开放
    - TC-5.8 单 invariant 维度：唯一规则可见预选，四动作直接可用
    - TC-5.9 零 invariant 维度：提示暂无可调整规则，仅保留「其他/全文编辑」
    - TC-5.10 应用调整只更新当前草稿：重编译出现加强表达、焦点移至摘要项、零 generation POST
    - TC-5.11 取消零写入：草稿逐字不变、焦点回比较触发器
    - TC-5.12 「其他」维度进入全文编辑并聚焦
    - TC-5.13 方向 feed 失败（L2）保留缓存与草稿，重试后恢复
    - TC-5.14 比较详情失败保留 rail/草稿，提供重试与打开 Iteration，重试后真实双图
    - TC-5.15 completed 缺结果资产：来源异常标记、不渲染假图、结果动作 disabled
- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图/trace: 无（0 失败）

### 3. 仓库快门（verify:fast）

- **通过（退出码 0）**: `workflow:check && test:workflow && type-check && lint && test` 五步串联全部成功
- 单测/组件测试全量: **117 文件 / 1190 测试：1190 passed / 0 failed**

## 结论

通过。plan-05 验证命令三条全部通过：组件/单测 36/36、E2E 37/37（red 阶段失败的 15 条 plan-05 用例全部转绿，plan-02/04 的 22 条既有用例保持绿）、verify:fast 117 文件 1190 测试零失败。AC-04/05/06/07 的用户可观察路径（内联结果、六选五、selected/preferred 分离、真实双图比较、零/单/多 invariant 分支、四动作、应用不自动生成、取消零写入、L2/L3 失败保留与重试）均经 E2E 验证；降级回归验收中的 `pnpm verify:fast` 门通过。本证据可作为 task-review 的验收输入。
