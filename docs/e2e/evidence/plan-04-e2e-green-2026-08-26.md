---
feat_id: plan-04
phase: green
date: 2026-08-26
---

# plan-04 E2E Green 证据（Style Memory 列表页）

- 关联架构: `docs/14-可验证Style-Memory/14-1-架构文档-可验证Style-Memory.md` §6.1（列表链路/搜索谓词/URL 条件持久化/空态双入口）、§4.2-③、ADR-8（导航术语）、§8.2（L4/L5）、§9 Phase B 点名 spec
- 功能文件: `docs/14-可验证Style-Memory/14-2-实现计划-可验证Style-Memory/plan-04-StyleMemory列表页.md`（status: review）
- Red 证据: `docs/e2e/evidence/plan-04-e2e-red-2026-08-26.md`（14/14 预期失败于功能缺失）
- spec 文件: `e2e/style-memory-list.spec.ts`（green 阶段**未做任何修改**，与 red 完全同文）
- mock helper: `e2e/helpers/mock-api.ts` 的 `mockStyleMemoryList` / `styleMemoryListItemDto`（green 阶段未修改；实现未触碰 mock-api.ts）

## 执行命令与结果

```bash
pnpm e2e -- e2e/style-memory-list.spec.ts --project=workspace
```

- 结果: **14 passed / 14（0 failed）**，Duration ≈ 18.2s（首次 green 运行）
- 与 red 用例清单（TC-1.1～TC-11）逐条对应，全部由同一断言转绿；无断言放宽、无 spec 修改。

```bash
pnpm e2e -- e2e/template.spec.ts e2e/ai-first-style-memory.spec.ts e2e/template-default-values.spec.ts --project=workspace
# 15 passed / 15

pnpm e2e -- e2e/ai-first-shell.spec.ts e2e/workspace-iteration-memory-integration.spec.ts e2e/precision-glass-shell.spec.ts --project=workspace
# 18 passed / 18

pnpm e2e -- e2e/ai-first-visual-regression.spec.ts --project=workspace
# 绿（与 style-memory-list 合跑 20 passed / 20；模板页截图基线按新卡片断言重拍，
#   该 spec 为结构断言 + 输出目录截图留档，无 committed 基线图）

pnpm e2e -- e2e/ai-first-landing-states.spec.ts e2e/workspace-iteration-save-style-memory.spec.ts --project=workspace
# 15 passed / 15（计划清单外、受新卡片/新文案直接影响的 2 个存量 spec，同步更新）

pnpm exec playwright test --project=workspace
# 全量 workspace 套件 195 passed / 195（无计划外回归）
```

## 与 red 失败清单的对照

| Red 失败 | Green 消除方式 |
| --- | --- |
| TC-1.1 卡片无徽标/代表结果/规则摘要，仍有名称派生标签与治理按钮 | `template-card.tsx` 新卡片（视图模型真实字段驱动），卡片仅"查看详情 / 使用" |
| TC-1.2 骨架无稳定 testid | `SkeletonCard` 增加 `data-testid="style-memory-card-skeleton"` |
| TC-2.1/2.2 服务端谓词命中被客户端二次过滤隐藏 | 列表页删除客户端过滤，直接渲染服务端返回（服务端谓词为准） |
| TC-2.3/2.4 三态筛选与组合缺失 | `use-template-search` 增加 `status` 参数 + 页面 `[全部] [用户已验证] [待验证]` 互斥筛选 |
| TC-2.4～2.6 URL 条件不持久 | `router.replace` 浅替换写 `search`/`status`（`cursor` 透传），返回列表从 URL 恢复 |
| TC-2.7 搜索提示 aria 口径不含全量谓词 | 输入框 aria-label 承载"名称、说明、风格规则（含风格指纹与增强方向）、排除约束、变量名与标签" |
| TC-8.1 清除按钮 24px | 清除搜索按钮 44×44px（h-11 w-11）+ aria-label"清除搜索" |
| TC-10.1 空态双入口缺失 | 空态提供"打开工作区"（/workspace）与"查看 Iterations"（/workspace/iterations）链接 |
| TC-10.2 401 callbackUrl 丢查询 | 登录入口 callbackUrl 携带原 `search`/`status` 返回原入口 |
| TC-10.3 503 隐藏工具栏 | 503 态保留搜索/筛选可见 + 重试恢复原条件与内容 |
| TC-11 导航仍为 "Library" | left-sidebar label/ariaLabel 统一 "Style Memory"（app-shell 核对无回归） |

## 存量 spec 更新记录（Task 7）

计划内 7 个：`template.spec.ts`（新 DTO mock + 中文断言；卡片级 Duplicate/Delete UI 用例按"详情为统一入口"决策移除，API 契约由 route 单测覆盖）、`ai-first-style-memory.spec.ts`（列表 mock 切 `mockStyleMemoryList`、卡片/状态断言重写、TC-6.3 卡片治理用例移除）、`template-default-values.spec.ts`、`ai-first-shell.spec.ts`、`workspace-iteration-memory-integration.spec.ts`、`precision-glass-shell.spec.ts`、`ai-first-visual-regression.spec.ts`。

计划外 2 个（受同一变更直接波及，同步更新）：`ai-first-landing-states.spec.ts`（empty/noResults/auth/failed 文案断言中文化 + 空态双入口链接）、`workspace-iteration-save-style-memory.spec.ts`（TC-5.8 列表新 DTO 分层 mock）。

相邻单测/组件测试更新：`src/lib/__tests__/style-memory-view-model.test.ts`（重写 9 用例）、`src/components/workspace/__tests__/template-card.test.tsx`（重写 6 用例，含 L1 回退）、`src/app/workspace/templates/__tests__/page.test.tsx`（9 用例）、`src/components/workspace/__tests__/left-sidebar.test.tsx`（导航改名）。

## 单元/组件与仓库门

```bash
pnpm vitest --run src/lib/__tests__/style-memory-view-model.test.ts
# 9 passed / 9

pnpm vitest --run src/app/workspace/templates/__tests__/page.test.tsx src/components/workspace/__tests__/left-sidebar.test.tsx src/components/workspace/__tests__/template-card.test.tsx src/lib/__tests__/style-memory-view-model.test.ts
# 4 文件 28 passed / 28

pnpm type-check        # 通过（0 error）
pnpm verify:fast       # 107 文件 / 970 用例全绿（含 workflow 契约检查）
```

## 性能验收（架构 §8.1，repository 代码路径测量）

seed 520 条 Memory + 560 条 generation_tasks（300 条 source_template_id 派生关联、260 条代表结果关联，`PLNBENCH*` 前缀）后，以 `findAllByUserId` 真实代码路径对本地 PostgreSQL 各场景 20 次请求测量 p95：default 4.6ms / status 筛选 3.1ms / 规则词搜索 5.3ms / 变量 label 搜索+筛选 3.6ms / 第 3 页 keyset 游标 3.4ms，全部 ≪ 500ms；测量后 seed 数据已清理（残留 0）。API 层 `requireAuth` 需真实 Google 会话，故未走 DevTools HTTP 面板（DB 查询为接口耗时主体，方法说明已记入 plan-04 文件）。

## 判定

- red spec 未修改且 14/14 转绿；存量受影响 spec（计划内 7 + 计划外 2）全绿；全量 workspace 套件 195/195 无回归；`verify:fast` 全绿。
- 结论: **预期通过，实现满足 plan-04 验收标准（AC-01/02/08/10 + 导航术语）。**

## 独立复核（2026-08-26，test-e2e green 阶段复核 agent）

- 复跑命令（与上文 green 命令逐字一致）：`pnpm e2e -- e2e/style-memory-list.spec.ts --project=workspace`
- 复跑结果：**14 passed / 14（0 failed）**，Duration 18.4s，与首次 green 运行（14/14，≈18.2s）一致。
- 用例核对：复跑输出的 14 个用例 ID（TC-1.1、TC-1.2、TC-2.1～TC-2.7、TC-8.1、TC-10.1～TC-10.3、TC-11）与 red 证据用例清单逐条一致；spec 内 `test('` 计数为 14。
- 同文性旁证：`e2e/style-memory-list.spec.ts` mtime（13:37）早于 red 证据完成时间（13:46），且早于本日 green 复跑，佐证"green 阶段未修改 spec、与 red 同文"的声明（spec 为未提交新文件，无 git 基线可 diff，故以 mtime + 用例清单双证据采信）。
- 依赖核对：`e2e/helpers/mock-api.ts` 存在 `styleMemoryListItemDto`（:649）与 `mockStyleMemoryList`（:661）；red 证据 `docs/e2e/evidence/plan-04-e2e-red-2026-08-26.md` 存在且记录 14/14 预期失败。
- 证据文件核查：frontmatter（`feat_id: plan-04` / `phase: green` / `date: 2026-08-26`）、命令、计数、结论与实际运行一致，无需修正。
- 复核结论：**独立复跑通过（14/14 绿），green 证据成立，无阻塞项。** 复核未修改实现/测试代码、README 状态机与 frontmatter。
