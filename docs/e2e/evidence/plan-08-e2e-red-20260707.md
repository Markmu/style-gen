---
feat_id: plan-08
phase: red
date: 2026-07-07
spec: e2e/ai-first-visual-regression.spec.ts
---

# plan-08 Red E2E Evidence

## 范围

- 功能文件：`docs/12-全站AI优先界面风格复刻/12-2-实现计划-全站AI优先界面风格复刻/plan-08-第12期Targeted-E2E与视觉QA.md`
- 架构文档：`docs/12-全站AI优先界面风格复刻/12-1-架构文档-全站AI优先界面风格复刻.md`
- 用例文档：`docs/e2e/12-e2e-用例-全站AI优先界面风格复刻.md`
- Red spec：`e2e/ai-first-visual-regression.spec.ts`

## 命令 1：视觉 QA red spec

```bash
pnpm e2e -- e2e/ai-first-visual-regression.spec.ts
```

结果：失败，符合 red 预期。

统计：

| Project | Passed | Failed | Total |
| --- | ---: | ---: | ---: |
| workspace | 3 | 2 | 5 |

HTML report：

- `playwright-report/index.html`

失败截图 / error context：

- `test-results/ai-first-visual-regression-8aedc--overlap-or-button-overflow-workspace/test-failed-1.png`
- `test-results/ai-first-visual-regression-8aedc--overlap-or-button-overflow-workspace/error-context.md`
- `test-results/ai-first-visual-regression-70d54-avoid-legacy-file-list-copy-workspace/test-failed-1.png`
- `test-results/ai-first-visual-regression-70d54-avoid-legacy-file-list-copy-workspace/error-context.md`

### 失败点 1：Render Dock 遮挡 / 覆盖 Prompt editor 几何区域

用例：`TC-8.2/TC-8.3 analysis_ready layout has no text overlap or button overflow`

失败断言：

```text
expect(Math.round(overlapWidth * overlapHeight)).toBe(0)
Expected: 0
Received: 100354
```

归因：`analysis_ready` 状态下 `output-card` 与 `unified-prompt-editor` 的 bounding box 存在大面积重叠，说明 plan-08 视觉 QA checklist 中“Render Dock 不遮挡 Prompt”尚未通过。页面可正常进入 `analysis_ready`，mock 上传/分析链路有效；失败不是环境错误。

### 失败点 2：Style Memory 卡片缺少最终 QA selector

用例：`TC-8.4 populated Style Memory cards expose final QA selector and avoid legacy file-list copy`

失败断言：

```text
getByTestId('style-memory-card').first()
Expected: visible
Error: element(s) not found
```

归因：页面已渲染真实 `.style-memory-card`，并且 `Style tags`、`Reuse intent`、`No source preview` 等 Style Memory 内容可见；但 `src/components/workspace/template-card.tsx` 尚未提供 plan-08 视觉 QA 所需的稳定 `data-testid="style-memory-card"`。这是 plan-08 可观测性/视觉 QA gate 尚未实现，不是页面空白或测试 selector 写错。

## 命令 2：旧体系残留扫描

```bash
rg -n "Template Library|workspace-two-pane-layout|floating-generate-window|Ready to Generate|GenerateHistoryBar|No templates yet" src e2e docs/e2e
```

结果：命中 111 行。

归因摘要：

- 多个 legacy specs 仍断言旧 `Ready to Generate`、`floating-generate-window`、`workspace-two-pane-layout`：`e2e/happy-path.spec.ts`、`e2e/edge-cases.spec.ts`、`e2e/workspace-layout.spec.ts`、`e2e/workspace-generation-dialog.spec.ts`、`e2e/degradation.spec.ts`、`e2e/error-path.spec.ts`、`e2e/workspace-degradation.spec.ts`。
- `e2e/template.spec.ts`、`e2e/precision-glass-home.spec.ts`、`e2e/precision-glass-shell.spec.ts` 仍含旧 `Template Library` 语义。
- `e2e/helpers/workspace-actions.ts` 仍包含旧 two-pane / floating generate helper fallback，尚未完成 plan-08 legacy helper 迁移。
- `src/components/workspace/generate-history-bar.tsx`、`src/components/workspace/floating-generate-window.tsx`、`src/components/workspace/workspace-two-pane-layout.tsx` 等旧体系组件仍被扫描命中，需要在 plan-08 green 阶段判定是否迁移、隔离或标注非第 12 期 targeted gate。

说明：用例文档和历史 evidence 中用于描述禁止词/历史红点的命中属于可解释残留；legacy spec、helper 和旧主体验组件命中仍证明 plan-08 的迁移/隔离任务尚未完成。

## 结论

结论：预期失败，测试有效。

red 阶段已经建立 plan-08 视觉 QA spec 和用例追踪。当前失败均指向 plan-08 尚未实现的最终 QA gate、Style Memory QA selector、Render Dock / Prompt 视觉检查，以及 legacy spec 迁移/隔离与残留扫描，未发现环境错误或测试自身错误。
