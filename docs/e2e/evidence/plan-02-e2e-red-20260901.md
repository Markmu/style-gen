---
feat_id: plan-02
phase: red
date: 2026-09-01
---

# plan-02 E2E Red 证据（快速创作节奏与工作区状态）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.1/3.2/3.3、§6.1、ADR-2、§8.2
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-02-快速创作节奏与工作区状态.md`（status: ready-to-dev，Task 1）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（plan-02 分组，TC-2.1–TC-2.10）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（新建，workspace project，mock API 全量拦截，不触达 live Provider/R2）

## 场景覆盖（plan-02 实现规格 §4 / 验收 AC-01、AC-07）

| 用例 | 场景 | 类型 |
| --- | --- | --- |
| TC-2.1 | 深入路径默认 analyze_edit，分析完成后停在可编辑态且零 generation POST | Happy |
| TC-2.2 | 快速确认披露五类信息（intent/detail/画幅策略/生成设置/单张生成） | Happy |
| TC-2.3 | 快速路径分析 success 后恰好一次自动 POST，请求与确认快照一致（trigger/intent/detailLevel/params/promptText） | Happy |
| TC-2.4 | armed 期间生成设置（Aspect Ratio/Quality/Model）只读并说明退出方式 | Edge |
| TC-2.5 | 自动提交后刷新（page.reload）不重放，POST 总数保持 1 | Edge |
| TC-2.6 | 分析失败清除 armed 快照、说明原因、参考上下文保留、零 POST | Error |
| TC-2.7 | 取消确认零写入，焦点回触发器，默认节奏不变 | Edge |
| TC-2.8 | 分析期间退出快速路径：清授权、解锁设置、分析完成后不自动生成、上下文保留 | Edge |
| TC-2.9 | 快速路径与深入路径证据完整度一致（与 TC-2.1 共享同一断言集合） | Happy |
| TC-2.10 | 阻塞清除后条件恢复（Retry analysis 成功）不延迟自动提交、不复活 armed | Edge |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
```

## 结果与失败摘要

**10 failed / 10 passed 0（10 个用例全部失败）**，其中 10 处 `element(s) not found`。

全部 10 个用例的失败点一致且唯一：

```text
Error: expect(locator).toBeVisible() failed
Locator: getByTestId('creation-pace-selector')
Expected: visible
Timeout: 10000ms
Error: element(s) not found
```

- 入口组件 `src/components/workspace/creation-pace-selector.tsx`（plan-02 文件清单 create 项）尚不存在，`creation-pace-selector` 及其下游 `pace-option-*` / `quick-confirm-*` / `quick-authorization-*` / `exit-quick-recreate` 元素均无 —— TC-2.1/2.2/2.7 在选择器/入口断言处失败，TC-2.3/2.4/2.5/2.6/2.8/2.9/2.10 在 `chooseQuickRecreatePace(page)` 第一步失败（`e2e/helpers/workspace-actions.ts:145`）。
- 失败是功能未实现，不是测试/环境错误：失败截图（`test-results/*/test-failed-1.png`）显示三栏工作台完整渲染（Reference 上传空态、Style Intelligence、Prompt + Render、Recent Iterations），页面无错误、无空白，仅缺少节奏选择入口；workspace project 服务器（3001）与全部 mock（presign/analysis/generation/session/cdn）正常工作。
- 环境备注：本次与首次运行的终端日志中出现端口 3000 `EADDRINUSE`（既有 dev server 复用冲突的 webServer 提示），不影响 workspace project（3001）执行；两条运行（含首次全新起服务）结果完全一致（10 failed，同一 locator 失败），排除环境偶发。
- 报告备注：本环境 Playwright HTML reporter 只落盘 `index.html` 与附件，`data/report.json` 未生成（与本次改动无关的可复现行为）；失败事实以终端日志与 `test-results/` 截图为准。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`（见上方报告备注）
- 失败截图（10 张）: `test-results/workspace-evidence-guided--*-workspace/test-failed-1.png`
- 新增/扩展测试资产:
  - `e2e/workspace-evidence-guided-render-loop.spec.ts`（新建 spec）
  - `e2e/helpers/mock-api.ts`（新增 `mockCdnImages`、`mockAnalysisCreateSequence`）
  - `e2e/helpers/workspace-actions.ts`（新增 `chooseQuickRecreatePace`、`confirmQuickRecreate`、`exitQuickRecreate`）
  - fixture 复用 `analysis-v2-completed.json` / `generation-completed.json` / `test-image.png`，无新增 fixture
- 供 implementer 的 data-testid / mock 契约清单见用例文档「plan-02 > 需要新增的源码 data-testid」与「需要新增的 mock / fixture」小节

## 结论

**预期失败，测试有效。** 全部失败均由 plan-02 目标功能（创作节奏双入口与快速确认 UI）尚未实现导致，元素/入口不存在；测试代码 ESLint 0 error 0 warning，mock 与既有 helper 复用无重复实现，无 `page.waitForTimeout`。implementer 可以本 evidence 为开工条件，按 Task 2–6 实现后在 review 前运行同一 spec 取得 green 证据。
