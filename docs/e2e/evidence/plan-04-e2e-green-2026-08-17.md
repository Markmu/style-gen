---
feat_id: plan-04
phase: green
date: 2026-08-17
---

# plan-04 E2E Green 证据（继续此方向恢复与守卫）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §6.3（恢复链路、守卫三豁免、替换确认、恢复零写请求、恢复后主动生成形成新 Iteration、flush 后导航）、§6.2/§7.2（IterationDetail DTO 含 `sourceTemplateId`）、§8.6（守卫保守侧取舍）、ADR-4（恢复为纯客户端动作）
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-04-继续此方向恢复与守卫.md`（status: review，Task 9 = 实现至 E2E green）
- 计划 README: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/README.md`
- red 证据: `docs/e2e/evidence/plan-04-e2e-red-2026-08-17.md`（同一 spec，8 用例全部预期失败——动作区为 plan-03 空占位）
- spec 文件: `e2e/workspace-iteration-restore.spec.ts`（workspace project，mock 模式，无需 live 凭证）
- 用例列表: 见 red 证据同文件「用例清单」节（plan-03 先例：plan-task 用例表嵌入 evidence 而非独立用例文档）

## 用例清单（与 red 阶段同一 spec、同一映射）

| # | 场景 | 类型 | 覆盖 | 结果 |
|---|------|------|------|------|
| TC-4.1 | direct 恢复（空工作台，三豁免之"current 为空"）：不弹确认，URL 回 `/workspace`，快照全量恢复（提示/排除项/参数 16:9·hd/来源图/上一轮结果 previous-result-preview），零 POST /api/generation | Happy | AC-05 | 通过 |
| TC-4.2 | 当前工作台存在不同未完成内容：弹替换确认，展示当前方向与目标方向两侧提示摘要，取消/确认按钮可见，停留详情，确认前零 POST | Happy | AC-05 | 通过 |
| TC-4.3 | 取消替换确认：对话框关闭、仍停留详情且 `data-iteration-id` 不变、持久化通道 promptText 仍为当前未完成内容、自行回工作台仍是原内容、全程零 POST | Happy | AC-05 | 通过 |
| TC-4.4 | 确认替换：对话框关闭、URL 回 `/workspace`、切换为目标快照（TC-4.1 同款断言）且不再包含当前未完成提示、零自动 POST | Happy | AC-05 | 通过 |
| TC-4.5 | failed 详情"Fix and continue"走同一恢复链路：空工作台 direct 不弹确认、回工作台提示与来源恢复、无结果图、零自动提交 | Happy | AC-04 | 通过 |
| TC-4.6 | 恢复→修改提示→主动生成：恰好 1 次 POST，请求体 promptText=修改后提示、negativePromptText=快照排除项、analysisTaskId=目标迭代、params={16:9, hd}（新 Iteration，原记录不动） | Happy | AC-05 | 通过 |
| TC-4.7 | 恢复源自 Style Memory 的迭代后生成：POST 请求体 `sourceTemplateId === 'tpl-style-memory'` 且 promptText=恢复后快照提示（保障记录可按模板名搜索） | Happy | AC-02 | 通过 |
| TC-4.8 | 重复恢复当前 Iteration（三豁免之"已是同一 Iteration"）：不弹确认，幂等回工作台且提示仍为目标快照 | Happy | AC-05 | 通过 |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-iteration-restore.spec.ts --project=workspace
```

## 结果输出摘录

```text
Running 8 tests using 1 worker

[1/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:318:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.1 direct restore from an empty workspace skips the confirm dialog and restores the full snapshot
[2/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:346:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.2 restoring over different unfinished content opens the replace confirm with both prompt summaries
[3/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:378:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.3 cancelling the replace confirm keeps the detail open and the workspace untouched
[4/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:421:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.4 confirming the replace switches the workspace to the target snapshot without generating
[5/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:451:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.5 the failed detail Fix-and-continue action runs the same restore chain without submitting
[6/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:486:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.6 modifying the restored prompt and generating issues a single POST with the restored context
[7/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:525:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.7 generating after restoring a Style Memory sourced iteration carries sourceTemplateId (AC-02)
[8/8] [workspace] › e2e/workspace-iteration-restore.spec.ts:561:7 › plan-04 continue-this-direction restore and workspace guard › TC-4.8 restoring the iteration that is already current goes direct without the confirm dialog

8 passed (21.8s)
```

- 汇总: `8 passed`，0 failed，0 flaky，0 skipped
- HTML 报告: `playwright-report/index.html`
- 失败截图/trace: 无（全绿，未产生失败产物）

## 判定

- red → green 对照: 同一 spec、同一 8 用例。red 阶段 8/8 预期失败（primaryActions 空占位、无 Continue this direction / Fix and continue 按钮）；实现交付 `restore-guard.ts`、`use-iteration-restore.ts`、`replace-confirm-dialog.tsx`、详情动作与工作台接线、`flush()` 暴露与 `sourceTemplateId` 发送方后，8/8 通过，无任何行为断言放宽。
- 结论: **通过。**
