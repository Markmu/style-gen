---
feat_id: plan-07
phase: green
date: 2026-09-01
---

# plan-07 E2E Green 证据（Workspace 闭环集成与回归）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §2.1.6（成功结果不再通过阻断式弹层呈现）、§3.1–3.3（全流程/分支/状态机与焦点契约）、ADR-5/ADR-7、§6.4（含实现原则「移除成功 GenerationDialog，保留任务失败的内联恢复动作」）、§6.6/6.7、§8.2（L1～L5）、§8.5（三视口视觉验收）
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-07-Workspace闭环集成与回归.md`（status: review；Task 1–7 全部 done）
- Red 证据: `docs/e2e/evidence/plan-07-e2e-red-20260901.md`（65 用例 50 passed / 15 failed，预期失败，测试有效）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（plan-07 分组 TC-7.1–TC-7.9）
- spec 文件（与 red 阶段同一份，未做任何修改）:
  - `e2e/workspace-evidence-guided-render-loop.spec.ts`（plan-02/04/05/06/07 分组，TC-7.1–TC-7.8）
  - `e2e/workspace-generation-dialog.spec.ts`（「成功不弹层 + 内联」契约三条）
  - `e2e/ai-first-visual-regression.spec.ts`（TC-8.3 失败内联 + TC-7.9 三视口 rail/比较/降级与 1280×720 最小守卫）
- 运行 project: `workspace`（端口 3001，`AUTH_REQUIRED=false`，mock API 全量拦截，不触达 live Provider/R2）
- 运行日期: 2026-09-01（本地时钟显示 2026-09-02 凌晨，同一工作日会话）

## 执行命令与结果（四条全部通过，0 failed）

### 命令 1 — plan-07 主验证（三 spec，workspace project）

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts e2e/workspace-generation-dialog.spec.ts e2e/ai-first-visual-regression.spec.ts --project=workspace
```

**65 passed / 0 failed（1.9m，exit 0）**。red 阶段 15 条失败用例全部转绿：

| spec | 通过 | 失败 |
| --- | --- | --- |
| `workspace-evidence-guided-render-loop.spec.ts`（TC-2.1–2.10、TC-4.1–4.12、TC-5.1–5.15、TC-6.1–6.10、TC-7.1–7.8） | 55 | 0 |
| `workspace-generation-dialog.spec.ts`（进行中/成功/失败三条内联契约） | 3 | 0 |
| `ai-first-visual-regression.spec.ts`（TC-8.1–8.5 + TC-7.9） | 7 | 0 |

red 15 条失败点逐项核销：TC-2.3/TC-2.5/TC-7.1/TC-7.2/TC-7.3/TC-7.7 与 dialog 三条/TC-8.3（弹层退场，`generation-dialog` 全程 hidden，结果内联在 rail/active face）；TC-7.4（`workspace-live-region` polite 通知）；TC-7.5（1280×720 Prompt 卡控件可点击，`editor-mode-option-text` 不再被拦截）；TC-7.6（`direction-feed-open-iteration` 出口）；TC-7.8（`generation-submit-error` + `generation-submit-retry` 内联提交错误）；TC-7.9（三视口 + 1280×720 无 `scrollWidth > clientWidth` 横向溢出）。

### 命令 2 — pnpm verify:fast

```bash
pnpm verify:fast
```

**通过（exit 0）**：`workflow:check` ✓、`test:workflow` ✓、`type-check` ✓、`lint` ✓、Vitest 单元/组件 **118 文件 1211 passed / 0 failed**。

### 命令 3 — pnpm verify:full

```bash
pnpm verify:full
```

**通过（exit 0）**：verify:fast 全绿 + `next build` 生产构建成功 + `e2e:smoke` **18 passed / 0 failed（21.6s）**。

### 命令 4 — pnpm verify:acceptance（最终发布前验收门）

```bash
pnpm verify:acceptance
```

**通过（exit 0）**：verify:fast 全绿 + `next build` 成功 + `e2e:targeted`（14 个 targeted spec，含主 spec 与视觉回归）**148 passed / 0 failed（3.2m）**。TC-7.1–TC-7.8 在本门内再次通过（进度 [100/148]–[107/148]）。

## 存量 spec 对齐清单（plan-07「允许修改的额外文件」要求在 green evidence 列明）

implement 阶段（Task 6「targeted/visual/存量 spec 对齐」）按「成功不弹层 / 失败内联 / rail 收口」新契约对齐的 14 个存量 e2e 文件（除 plan-07 文件清单自有的 3 个 spec 与 red 阶段已扩展的 `e2e/helpers/mock-api.ts` 外的全部 e2e 改动，仅最小断言对齐、不放宽行为标准，diff 已逐文件核验含 generation-dialog / 内联语义改动）：

1. `e2e/ai-first-shell.spec.ts`
2. `e2e/degradation.spec.ts`
3. `e2e/edge-cases.spec.ts`
4. `e2e/error-path.spec.ts`（red 证据预告的两条「生成失败弹层」断言按新契约对齐为内联失败 + 恢复入口）
5. `e2e/happy-path.spec.ts`
6. `e2e/helpers/workspace-actions.ts`（helper 层断言适配）
7. `e2e/style-memory-reuse.spec.ts`
8. `e2e/workspace-ai-first-iteration-memory.spec.ts`
9. `e2e/workspace-ai-first-render-dock.spec.ts`
10. `e2e/workspace-degradation.spec.ts`
11. `e2e/workspace-history-strip.spec.ts`
12. `e2e/workspace-layout.spec.ts`
13. `e2e/workspace-prompt-generate.spec.ts`
14. `e2e/workspace-three-column-layout.spec.ts`

以上对齐文件全部被命令 3/4 的 smoke 与 targeted 门覆盖并保持通过。

## 验收对照

- AC-01～AC-07：主 spec 55 条（plan-02/04/05/06 分组回归 + TC-7.1–TC-7.8 收口）全部 green。
- US-01～US-11：US 覆盖矩阵由 TC-2.x/4.x/5.x/6.x/7.x 与 targeted 门（含 Iteration Memory/restore/save-memory 148 条）追踪，无孤立或弱化 AC。
- §8.2 降级回归：L1→TC-7.5、L2→TC-7.6（+TC-5.13/5.14）、L3→TC-7.3（+TC-5.4）、L4→TC-7.7（+TC-2.6/2.10 与 `degradation.spec.ts`）、L5→TC-7.8 全部 green。
- 焦点/键盘：TC-7.4 比较开关/取消/应用确定焦点 + polite live region 不夺编辑焦点，green。
- 视觉：TC-7.9 三视口（1440×900 / 1280×800 / 390×844）rail/比较/降级无横向溢出 + 1280×720 最小 DOM 守卫，green。
- 全局质量门：`verify:fast`、`verify:full`、`verify:acceptance` 全部通过，无环境 flake，未放宽任何断言。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`（当前内容为最后一次运行 `verify:acceptance` 的 e2e:targeted 148/148；命令 1 的 65/65 结果以本证据记录为准，报告目录按运行顺序被后续 run 覆盖）
- 失败截图/trace: 无（四条命令 0 failed，`test-results/` 无失败产物）
- 本阶段未修改任何产品代码、测试代码、README「## 5. 开发状态机」与 plan-07 frontmatter（status 保持 review）。

## 结论

**通过**。plan-07 验证命令四条全部通过：主验证 65/65、verify:fast（Vitest 1211/1211 + workflow/type/lint）、verify:full（smoke 18/18）、verify:acceptance（targeted 148/148）。red 阶段全部 15 条失败按逐项核销转绿，存量 14 个 e2e 文件最小对齐并保持 smoke/targeted 通过。plan-07 可携本证据进入 task-review。
