---
feat_id: plan-04
phase: red
date: 2026-08-26
---

# plan-04 E2E Red 证据（Style Memory 列表页）

- 关联架构: `docs/14-可验证Style-Memory/14-1-架构文档-可验证Style-Memory.md` §6.1（列表链路/搜索谓词/URL 条件持久化/空态双入口）、§4.2-③、ADR-8（导航术语）、§8.2（L4/L5）、§9 Phase B 点名 spec
- 功能文件: `docs/14-可验证Style-Memory/14-2-实现计划-可验证Style-Memory/plan-04-StyleMemory列表页.md`（status: draft，Task 1 red 先行）
- 依赖契约: plan-02 已交付的 `GET /api/templates` 新 DTO（`StyleMemoryListItem`：id/name/verificationStatus/retainedRulesPreview(≤2)/variableCount/sourceImageUrl/representativeImageUrl/lastUsedAt/updatedAt，支持 `search` + `status` 组合筛选），本 spec 以 `mockStyleMemoryList` mock 该契约
- spec 文件: `e2e/style-memory-list.spec.ts`（新建，14 用例，mocked 模式，workspace project 端口 3001）

## 执行命令与结果

```bash
pnpm e2e -- e2e/style-memory-list.spec.ts --project=workspace
```

- 结果: **14 failed / 0 passed（14 用例全部因 UI 未实现失败），exit 1**
- Playwright HTML 报告: `playwright-report/index.html`（最后一次运行为本 spec）
- 失败截图: `test-results/style-memory-list-plan-04--*/test-failed-1.png`（14 份，按用例目录）
- 环境健康性佐证: 同一实现在全量 workspace 套件中运行 217 用例 → **203 passed / 14 failed**，14 个失败全部为本 spec 新用例，既有 spec（template / ai-first-style-memory / iterations 等）零回归，登录 mock 与 webserver 正常。

## 用例清单（14 用例，覆盖 AC-01 / AC-02 / AC-08 / AC-10 + 导航术语）

| # | 场景 | 类型 | 断言要点 |
| --- | --- | --- | --- |
| TC-1.1 | 已验证卡 vs 待验证卡内容对比 | Happy | 已验证卡：徽标"用户已验证"+ 代表结果主预览 src（面积大于来源图小图）+ "参考图"标注 + 两条真实规则摘要 + "6 个变量" + 非"尚未使用"；待验证卡：徽标"待验证"、来源图预览且无代表结果图、真实规则摘要、"4 个变量"、"尚未使用"；无来源图卡："无预览"且零 `<img>`；全页无 Source-backed/Prompt-only/Variable structure 名称派生标签；卡片动作仅"查看详情/使用"，无更多操作/复制/删除 |
| TC-1.2 | 加载骨架出现后消失 | Happy | gate 挂起列表响应期间 `[data-testid="style-memory-card-skeleton"]` 可见且真实卡片数为 0（无虚假资产）；放行后骨架消失、卡片出现 |
| TC-2.1 | 按规则词搜索命中 | Happy | 请求携带 `search=低饱和暖灰`（拦截断言）、命中已验证卡可见、未命中卡隐藏、URL 持久化 `?search=` |
| TC-2.2 | 按变量名搜索命中 | Happy | 请求携带 `search=subject`、命中卡可见、URL 持久化（证明前端信任服务端跨字段谓词，不再客户端二次过滤） |
| TC-2.3 | 状态筛选 user_verified | Happy | 点击筛选"用户已验证"→ 请求携带 `status=user_verified`、只显示已验证卡、URL 持久化 `?status=` |
| TC-2.4 | search + status 组合 | Happy | 单请求同时携带 `search=编辑式构图` 与 `status=pending_verification`，结果与 URL 双参数一致 |
| TC-2.5 | 无结果条件保留与清除 | Edge | 规则词（仅命中已验证）+ 待验证筛选 → noResults 态保留输入值与 URL 双参数；清除动作清空输入、恢复全部卡片并移除 URL 参数 |
| TC-2.6 | 进详情返回后条件恢复 | Happy | 查看详情 → `/workspace/templates/{id}`；`goBack` 后 URL `search/status` 保持、输入值恢复、筛选结果恢复（待验证卡可见、已验证卡隐藏） |
| TC-2.7 | 搜索提示 aria 口径 | Edge | 搜索框 accessible name 含 名称/说明/风格规则/排除约束/变量（承诺范围 = 实际谓词，架构 §6.1 口径） |
| TC-8.1 | 清除搜索按钮可达性 | Edge | 输入框控件内清除按钮有可理解名称（/清除搜索\|clear search/i）且 boundingBox ≥ 44×44px（PRD 规则 29） |
| TC-10.1 | 空列表双入口 | Happy | 空态 `link "打开工作区" href=/workspace` 与 `link "查看 Iterations" href=/workspace/iterations`（href 断言，修正现有两按钮均指向工作区） |
| TC-10.2 | 401 未登录保留查询 | Error | authRequired 态可见；URL `search/status` 不被重置；登录入口 callbackUrl 携带原 `search`/`status` 返回原入口 |
| TC-10.3 | 503 错误态重试恢复 | Error | 首次 GET 503 → failedRecoverable 态 + 重试按钮，搜索框与条件仍可见（工具栏不被整体隐藏）；重试（`route.fallback()` 放行成功响应）后原条件与内容恢复 |
| TC-11 | 导航术语（ADR-8） | Happy | 侧栏导航存在 accessible name 与可见文本均为 exact "Style Memory" 的链接；"Library" 不再出现 |

## 失败摘要（按用例，均为功能未实现所致）

| # | 失败断言 | 缺失功能 |
| --- | --- | --- |
| TC-1.1 | `getByText('用户已验证')` 在已验证卡内 not found | 卡片未接新 DTO：无状态徽标/代表结果预览/规则摘要（视图模型仍为名称派生 + Source-backed 标签，卡片仍有 More actions/复制/删除） |
| TC-1.2 | `getByTestId('style-memory-card-skeleton')` not found | 骨架缺稳定 testid（见下方"需新增的源码 data-testid"） |
| TC-2.1 | `getByText('Editorial Soft Daylight')` not found（`search=低饱和暖灰` 请求已发出且 mock 已返回命中卡） | 现有前端在客户端按 name/sourceAssetId/sourceImageUrl/"N variables" 二次过滤，把服务端规则命中卡隐藏——搜索与可见信息不一致（plan-04 需删除客户端二次过滤，信任服务端谓词） |
| TC-2.2 | 同 TC-2.1（`search=subject` 请求已发出） | 同上：变量名等服务端谓词命中的结果被客户端过滤隐藏 |
| TC-2.3 | `getByRole('button', { name: '用户已验证' })` click 超时（元素不存在） | 三态验证筛选未实现（现有筛选仍为 All/Source-backed/Prompt-only） |
| TC-2.4 | `expect(page).toHaveURL(/search=/)` 失败（URL 保持 `/workspace/templates`） | URL 条件持久化未实现（"待验证"筛选按钮亦不存在） |
| TC-2.5 | 同上（URL 无 `search=`） | URL 条件持久化 + 无结果条件保留未实现 |
| TC-2.6 | 同上（URL 无 `search=`） | URL 条件持久化 + 查看详情入口未实现 |
| TC-2.7 | `toHaveAccessibleName(/名称/)` 失败，实际 accessible name 为 "Search Style Memory" | 搜索提示 aria 口径未承载全量谓词范围 |
| TC-8.1 | `boundingBox` 宽 24 < 44（aria-label "Clear Search" 存在但为 `h-6 w-6` 24px 图标钮） | 清除搜索按钮命中面积 ≥ 44×44px 未实现 |
| TC-10.1 | `section[data-status="empty"]` 内 `link "打开工作区"` not found | 空态双入口未实现（现为两个 button 且均指向工作区） |
| TC-10.2 | 登录入口 callbackUrl 为 `/workspace/templates`，不含 `search=`/`status=` | 401 登录入口未携带原查询条件返回 |
| TC-10.3 | `getByRole('textbox')` not found（503 时工具栏整体隐藏） | 服务异常态保留搜索/筛选可见性未实现 |
| TC-11 | 侧栏无 exact "Style Memory" 链接（现 label "Library"、aria-label "Style Memory Library"） | 导航术语未改名（left-sidebar） |

## 新增测试资产

- `e2e/helpers/mock-api.ts` 新增（纯增量，未改既有 helper）:
  - `mockStyleMemoryList(page, items, options)` — 拦截 `GET /api/templates?**`，按 `status`（all/user_verified/pending_verification）与 `search`（名称 + retainedRulesPreview + mockSearchText 跨字段）过滤，返回 `{ items, hasMore, nextCursor }` 新 DTO；`onRequest` 暴露 search/status/cursor 供参数透传断言
  - `styleMemoryListItemDto(item)` — 剥离 mock-only `mockSearchText` 后的响应条目
  - 类型 `MockStyleMemoryListItem` / `StyleMemoryListRequestQuery` / `MockStyleMemoryListOptions`
- 场景化时序控制在 spec 内本地实现（不重复入库）：TC-1.2 的 gate 挂起路由、TC-10.3 的首请求 503 后 `route.fallback()` 放行、TC-10.2 的 `**/api/auth/signin**` 拦截捕获 callbackUrl

### 需新增的源码 data-testid

- `src/app/workspace/templates/page.tsx` `SkeletonCard` → `data-testid="style-memory-card-skeleton"`（TC-1.2 骨架断言的稳定锚点；实现阶段随 Task 4 添加）

### 中途修正记录（测试自身问题，非功能失败）

- TC-8.1 首跑 strict mode violation：`/clear search/i` 同时命中工具栏图标钮与 noResults 态同名主操作。已改为 `page.getByRole('textbox').locator('..')` 容器内定位，修正后失败原因收敛为 24px < 44px（正确 red）。

## 判定

- 14 个失败全部可归因到 plan-04 未实现的用户可观察功能：卡片视图模型未接新 DTO、三态筛选缺失、URL 条件持久化缺失、客户端二次过滤残留、搜索 aria 口径缺失、清除按钮命中面积不足、空态双入口缺失、401 callbackUrl 丢查询、503 错误态隐藏工具栏、导航仍为 "Library"；无环境错误（webserver/登录 mock 正常）、无语法错误（`pnpm exec eslint` 与 `tsc` 对新文件 0 error 0 warning）、无选择器歧义（TC-8.1 已修正）。
- 用例内容逐条对应 plan-04「实现规格 5」与「验收标准」（AC-01/02/08/10 + 导航术语），实现落地后同一命令转 green。
- 结论: **预期失败，测试有效。**
