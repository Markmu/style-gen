---
feat_id: plan-01
phase: red
date: 2026-08-17
---

# plan-01 E2E Red 证据（相邻 Vitest 承载）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §6/§7/§8
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-01-迭代数据层与API扩展.md`
- **E2E 不适用（纯后端，理由见 plan-01 §8"风险与边界>E2E 不适用说明"），red 证据由相邻 Vitest 承载**：无直接用户界面，接口行为以相邻路由/仓库 Vitest 为直接质量门，用户可观察行为由 plan-02/03 的页面 E2E 通过同一接口间接承接（README §6 全局护栏亦明确 plan-01 以相邻路由/仓库测试为直接质量门）。
- 覆盖测试文件（plan-01 文件清单内相邻测试）:
  - `src/lib/repositories/__tests__/generation-task-repository.test.ts`（listIterations / findIterationDetail / createGenerationTask 快照 / linkTemplateToGenerationTask）
  - `src/app/api/generation/__tests__/route.test.ts`（GET q/status 参数扩展与默认 completed 兼容；POST 快照固化与 sourceTemplateId 校验）
  - `src/app/api/generation/[id]/__tests__/route.test.ts`（全状态详情 DTO、401/404/500）
  - `src/app/api/templates/__tests__/route.test.ts`（sourceGenerationTaskId 校验与落库）
- 执行命令:
  - `pnpm vitest --run src/lib/repositories/__tests__/generation-task-repository.test.ts src/app/api/generation/__tests__/route.test.ts "src/app/api/generation/[id]/__tests__/route.test.ts" src/app/api/templates/__tests__/route.test.ts`
- 结果: 失败，**47 failed / 58 passed (105)**，4 个测试文件全部出现失败

## 关键失败输出摘要

- 仓库层（功能未实现——目标函数不存在）:
  - `TypeError: listIterations is not a function`（status 过滤、q ILIKE 双字段、游标、promptSummary 截断、pageSize clamp 等 10 个用例）
  - `TypeError: findIterationDetail is not a function`（快照优先/回退/缺失三来源、pending 归并、已保存关联等 9 个用例）
  - `linkTemplateToGenerationTask is not a function`（templates 反向关联落库）
  - `createGenerationTask`：`expected "vi.fn()" to be called with arguments: [ ObjectContaining{recipeSnapshot…} ]`——insert values 尚未包含快照列
- `GET /api/generation`（路由仍走 completed-only `listCompleted`，未解析 q/status）:
  - `Cannot read properties of undefined (reading 'items')`（路由调用 listCompleted，而相邻 mock 只提供 listIterations）
  - `expected 500 to be 400`——非法 `status`、超长 `q` 尚无白名单/长度校验
  - `expected "vi.fn()" call … [ ObjectContaining {"status": "all"} ], but called only 0 times`——listIterations 未被调用
  - `iteration_list_queried` 结构化日志：`Number of calls: 0`
- `POST /api/generation`:
  - `createGenerationTask` 未收到 `recipeSnapshot` / `variablesSnapshot` / `sourceTemplateId`
  - 非法 `sourceTemplateId`：`expected 500/404 to be 400`——服务端归属校验未实现
- `GET /api/generation/[id]`（仍为 completed-only `findByIdWithRecipe` 分支 + 回退分支）:
  - `expected 500 to be 200`——findIterationDetail 分支不存在；`recipeSource`/`variablesSource`/`sourceTemplateId`/`savedTemplate` 为 `undefined`
  - `expected 500 to be 404`（跨用户/不存在用例走旧链路报 500）
  - `iteration_detail_queried` 结构化日志：`Number of calls: 0`
- `POST /api/templates`:
  - `expected "vi.fn()" to be called with arguments: [ 'gen-1', 'user-1' ]`——sourceGenerationTaskId 归属校验未实现
  - 非 completed / 无结果资产 / 非字符串类型：期望 400，实际 201/500——校验分支未实现

## 判定

- 失败原因是 plan-01 目标功能（schema 快照列、listIterations/findIterationDetail、三个路由扩展）尚未实现，不是测试代码、mock、fixture 或环境错误：所有既有（改造前已存在）用例在本次 red 运行中保持通过（58 passed），失败集中在新增断言点。
- 结论: **预期失败，测试有效。**
