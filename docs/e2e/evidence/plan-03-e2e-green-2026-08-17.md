---
feat_id: plan-03
phase: green
date: 2026-08-17
---

# plan-03 E2E Green 证据（迭代详情三态与轮询）

- 关联架构: `docs/13-Iteration-Memory闭环补全/13-1-架构文档-Iteration-Memory闭环补全.md` §3.3（详情状态机 DLoading→DCompleted/DProcessing/DFailed/DError、DProcessing 轮询迁移、关闭不重置列表态）、§6.2（打开详情链路与上下文组装标记）、§6.5（进行中 5s 轮询更新）、§7.2（IterationDetail DTO 含 source 标记）、§8.2（降级策略 L1/L2、详情 5xx）、ADR-7（轮询复用单任务端点）
- 功能文件: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/plan-03-迭代详情三态与轮询.md`（status: review，Task 9 = 实现至 E2E green）
- 计划 README: `docs/13-Iteration-Memory闭环补全/13-2-实现计划-Iteration-Memory闭环补全/README.md`
- Red 证据: `docs/e2e/evidence/plan-03-e2e-red-2026-08-17.md`（9 failed / 0 passed，预期失败，测试有效；用例清单 TC-3.1 ~ TC-3.9 与选择器契约见该文件）
- spec 文件: `e2e/workspace-iteration-detail.spec.ts`（workspace project，mock 模式，无需 live 凭证）
- 用例清单: 嵌入于 red 证据文件（任务边界约束不改 plan-03 文件清单之外的文件），本 green 运行覆盖同一批用例、同一 spec

## 用例清单（与 red 证据同表，9 用例）

| # | 场景 | 类型 | 结果 |
|---|------|------|------|
| TC-3.1 | completed 详情并排展示参考图与结果图，右区分区块（证据与不变量 data-source=snapshot + facet 值、提示内容、变量与排除项、生成设置 16:9/hd/modelName）、头部返回/上一条/下一条、底部动作区插槽占位 | Happy（AC-03） | 通过 |
| TC-3.2 | processing 详情展示阶段与"可离开"安抚文案 + 已保留上下文，无结果图、无动作区、无 generate/regenerate/resubmit 入口（count 0） | Happy（AC-04） | 通过 |
| TC-3.3 | processing 5s 轮询序列 [processing, processing, completed]：同一 `iteration-detail-panel` 容器 `data-status` 原地切换为 completed，结果图出现，详情端点请求 ≥ 3 次 | Happy（AC-04） | 通过 |
| TC-3.4 | processing 轮询序列 [processing, failed]：面板原地切换为 failed，失败说明区可见且含业务文案 | Happy（DProcessing→DFailed 分支） | 通过 |
| TC-3.5 | failed 详情失败说明 + 保留的参考图/提示/变量/排除项/设置，无结果图，底部动作区插槽占位可见（plan-04 填充） | Happy（AC-04） | 通过 |
| TC-3.6 | 详情 5xx：`iteration-detail-error` 错误位（说明 + Retry + Close），列表搜索值与条数不动；解除 500 重试后 completed 详情渲染、列表仍 2 条 | Error（AC-07） | 通过 |
| TC-3.7 | 上一条/下一条连续切换：iter-newest→iter-mid→iter-old→iter-mid→iter-newest，首条 Previous disabled、末条 Next disabled，列表 `data-selected` 高亮恰好 1 条且同步移动 | Happy | 通过 |
| TC-3.8 | 打开/返回保位：搜索 `neon` + 筛选 Completed + 加载较早至 24 条 + scrollTop=600 后打开详情再返回，搜索值、筛选选中、条数、scrollTop（>400）全部保持 | Happy（AC-03/07） | 通过 |
| TC-3.9 | 旧记录缺失标记：证据区块 `data-source=fallback` 内容仍可读 + 回退提示；变量区块 `data-source=missing` + 缺失提示；来源图占位（`iteration-reference-missing`）且无参考 img；结果图/提示/设置照常渲染 | 降级（L1/L2） | 通过 |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-iteration-detail.spec.ts --project=workspace
```

## 结果输出摘录

```text
Running 9 tests using 1 worker

[1/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.1 completed detail shows reference and result side by side with full context blocks
[2/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.2 processing detail shows stage and preserved context without generate or resubmit entries
[3/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.3 processing detail polls the sequence and switches in place to completed
[4/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.4 processing detail polling can switch in place to failed
[5/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.5 failed detail keeps the preserved context and reserves the actions slot
[6/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.6 detail 5xx keeps the list untouched and offers retry and close
[7/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.7 previous and next walk the list order and keep the row highlight in sync
[8/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.8 opening and closing the detail never resets search, filter, depth, or scroll
[9/9] [workspace] › plan-03 Iteration Memory detail three states and polling › TC-3.9 legacy record missing sources are marked without blocking the rest of the detail

9 passed (37.3s)
```

- 通过/失败数量: **9 passed / 0 failed / 0 skipped**
- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图/trace: 无（全部通过，未产生失败工件）

## 与 red 证据的对照说明

- 同一 spec、同一命令、同一 mock helper（`e2e/helpers/mock-api.ts` 的 `mockIterationDetail` / `mockIterationDetailSequence` / `mockIterationList` / `mockApiError` / `mockAuthSession`），red 阶段 9 用例因详情面板未实现（`iteration-detail-panel` 选择器不存在）全部失败；plan-03 Task 2-8 实现完成后同一 spec 全绿，红绿对照成立。
- red 证据中实现方选择器契约全部落地：`iteration-detail-panel[data-status][data-iteration-id]`、`iteration-detail-error`（Retry/Close）、`iteration-reference-image` / `iteration-reference-missing` / `iteration-result-image`、`iteration-context-{evidence,prompt,variables,settings}[data-source]`、`iteration-failure-reason`、`iteration-detail-actions`（processing 不渲染）及面板头部 Back to list / Previous / Next 边界禁用。
- 本 green 运行未修改 spec 文件与任何源码文件（仅写入本证据文档）。

## 结论

**通过。** `pnpm e2e -- e2e/workspace-iteration-detail.spec.ts --project=workspace` 9/9 全绿（37.3s），覆盖 plan-03 §5 E2E 验收全部场景（completed 并排与分区块、processing 无生成入口、5s 轮询原地切换 completed/failed、failed 上下文保留与动作占位、详情 5xx 保留列表可重试/关闭、上一条/下一条与高亮同步、打开/返回保位、旧记录缺失标记 L1/L2 降级）。plan-03 green-e2e 步骤完成，可进入 review。
