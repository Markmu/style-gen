---
feat_id: plan-04
phase: red
date: 2026-09-01
---

# plan-04 E2E Red 证据（Prompt 控制与保留改变摘要）

- 关联架构: `docs/15-Workspace证据引导生成闭环/15-1-架构文档-Workspace证据引导生成闭环.md` §3.2（手动全文/armed/旧任务分支）、§6.2（Prompt 意图、表达与手动全文）、§6.3（参考画幅推荐）、§7.2（PromptControlSnapshot / AspectRatioSource / PromptEditorMode）、§8.6（三档规则恒等、未知画幅请求前拒绝）
- 实现计划 README: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/README.md`
- 功能文件: `docs/15-Workspace证据引导生成闭环/15-2-实现计划-Workspace证据引导生成闭环/plan-04-Prompt控制与保留改变摘要.md`（status: ready-to-dev，Task 1：red 补 Prompt/画幅 E2E 场景）
- 用例文档: `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（本次增量新增 plan-04 分组 TC-4.1–TC-4.12，plan-02 分组未改动）
- spec 文件: `e2e/workspace-evidence-guided-render-loop.spec.ts`（在既有主 spec 末尾追加 plan-04 describe，workspace project，mock API 全量拦截，不触达 live Provider/R2）

## 场景覆盖（plan-04 实现规格 §1–§5 / 验收 AC-02、AC-03、AC-05）

| 用例 | 场景 | 类型 |
| --- | --- | --- |
| TC-4.1 | 分析完成后两轴与编辑方式默认态：same_style / standard（新分析默认）/ variables，两轴顶层、三入口次级 | Happy |
| TC-4.2 | 切换 intent 即时重编译（无确认）；reconstruction 使用原内容并显示「同时参考原内容与风格」说明；invariant 恒等 | Happy |
| TC-4.3 | 三档 detail 切换 invariant 集合恒等；concise 不加未覆盖 observation、standard/professional 按阈值补充 | Happy |
| TC-4.4 | 变量值在 intent/detail 切换后保持并进入编译结果（AC-02 变量保持） | Happy |
| TC-4.5 | 三编辑入口（variables/text/structured）可达；structured 只读（无可编辑输入，仅查看/复制）；返回后最终 Prompt 来源不变 | Edge |
| TC-4.6 | 手动全文后切换先确认：cancel 逐字保留零写入且焦点回原控件；accept 替换为新编译并清 dirty（后续切换即时生效） | Edge |
| TC-4.7 | 保留/改变摘要从真实规则与变量派生；点击保留项定位 RecipeCard 真实 invariant，点击改变项聚焦变量编辑器（AC-05） | Happy |
| TC-4.8 | Recipe 无 invariants 时摘要显示可恢复空态、不伪造项 | 降级 |
| TC-4.9 | 画幅 reference 推荐标注（data-recommended）；user 选择不被图片重载（reload）与 Prompt 编辑覆盖 | Happy |
| TC-4.10 | Iteration 恢复：restore 来源优先于参考推荐；旧快照降级 text 模式、全文取 promptSnapshot | Edge |
| TC-4.11 | 未知画幅请求前拒绝：清洗回 1:1、fallback 不标推荐、POST 仅携带白名单画幅 | Edge |
| TC-4.12 | armed 期间 intent/detail 只读并说明「自动任务将使用已确认设置」 | Edge |

## 执行命令

```bash
pnpm e2e -- e2e/workspace-evidence-guided-render-loop.spec.ts --project=workspace
```

## 结果与失败摘要

**22 个用例：10 passed / 12 failed**（plan-02 TC-2.1–TC-2.10 全部通过保持绿；plan-04 TC-4.1–TC-4.12 全部失败，12 处 `element(s) not found`）。

失败点分布（每条均为对应功能的入口元素不存在，即 plan-04 组件未实现）：

| 用例 | 失败 locator（Timeout 10000ms, element(s) not found） |
| --- | --- |
| TC-4.1 / TC-4.2 / TC-4.3 / TC-4.4 / TC-4.5 / TC-4.6 / TC-4.10 / TC-4.12 | `getByTestId('prompt-intent-controls')` |
| TC-4.7 / TC-4.8 | `getByTestId('keep-change-summary')` |
| TC-4.9 / TC-4.11 | `getByTestId('output-card').getByTestId('aspect-ratio-source')` |

典型失败输出：

```text
Error: expect(locator).toBeVisible() failed
Locator: getByTestId('prompt-intent-controls')
Expected: visible
Timeout: 10000ms
Error: element(s) not found
```

- 失败是功能未实现，不是测试/环境错误：`src/components/workspace/prompt-intent-controls.tsx`、`keep-change-summary.tsx`（plan-04 文件清单 create 项）尚不存在，`prompt-card.tsx` / `output-card.tsx` / `workspace/page.tsx` 尚未挂载两轴控制、摘要与画幅来源（modify 项），故三组入口元素及其下游断言均不可达；plan-02 既有用例在同一运行中全部通过，证明 dev server、mock（presign/analysis/generation/session/cdn）与测试环境本身正常。
- 失败截图: `test-results/workspace-evidence-guided--*-workspace/test-failed-1.png`（三栏工作台正常渲染，仅缺少 plan-04 控件区/摘要/来源徽标）。
- spec/fixture lint 通过（`pnpm exec eslint e2e/workspace-evidence-guided-render-loop.spec.ts` 0 errors）。

## 测试基建披露

- 新增 fixture: `e2e/fixtures/api-responses/analysis-v2-completed-no-invariants.json`（复制 `analysis-v2-completed.json` 并置 `recipe.styleInvariants=[]`，驱动 TC-4.8 摘要空态）。
- 未改动 `e2e/helpers/mock-api.ts`：画幅/恢复场景通过 spec 内新增 `seedWorkspaceV5State`（在不消费工作台快照的 `/workspace/iterations` 页面注入 v5 sessionStorage 快照后导航回 `/workspace`）与 `completeDeepAnalysis` 共享 helper 实现，沿用 `style-memory-reuse.spec.ts` 既有 seed 模式；plan-02 review 已确认共享测试基建扩展为允许惯例，本次仅新增 fixture 文件。

## 产物

- Playwright HTML 报告: `playwright-report/index.html`
- 失败截图（12 张）: `test-results/workspace-evidence-guided--*-workspace/test-failed-1.png`
- 本次修改/新增:
  - `e2e/workspace-evidence-guided-render-loop.spec.ts`（追加 plan-04 describe：TC-4.1–TC-4.12 + 3 个共享 helper）
  - `e2e/fixtures/api-responses/analysis-v2-completed-no-invariants.json`（新增）
  - `docs/e2e/15-e2e-用例-Workspace证据引导生成闭环.md`（增量 plan-04 分组 + 汇总更新）

## 结论

预期失败，测试有效。plan-04 的 12 条用例全部因 plan-04 功能（两轴控制、三编辑方式、手动全文 dirty confirm、保留/改变摘要、画幅来源展示）未实现而失败；plan-02 的 10 条既有用例保持通过。implementer 可以本证据为开始条件，按用例文档中的 data-testid 契约实现至 green。
