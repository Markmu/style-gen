# plan-05 Review - 20260706

## 基本信息

- 功能 ID：`plan-05`
- 标题：Iteration Memory 与保存记忆入口
- 功能文件：`docs/12-全站AI优先界面风格复刻/12-2-实现计划-全站AI优先界面风格复刻/plan-05-Iteration-Memory与保存记忆入口.md`
- Failed review report：本文件上一版，结论为未通过，blocker 为真实 `GET /api/generation/[id]` completed detail response 缺少 restored source context
- Red evidence：`docs/e2e/evidence/plan-05-e2e-red-20260706.md`
- Green evidence：`docs/e2e/evidence/plan-05-e2e-green-20260706.md`
- Review agent：independent task-review second pass after remediation

## 验收结论

**通过。**

remediation 后，第一次 review 的 blocker 已关闭：真实 `GET /api/generation/[id]` completed detail response 现在通过 repository 从 `generation_tasks.analysis_task_id -> analysis_tasks.source_asset_id -> assets.file_url` 派生并返回 `sourceAssetId`、`sourceImageUrl`，同时返回 `variables` 与 `analysisTemplateVariables`。route 层已转发这些字段，`useHistoryRestore` 与 workspace 保存入口会消费 restored source context，targeted E2E 的 mock detail 字段也与真实 contract 对齐。

本次按 9 个验收维度复核，功能文件验证命令全部通过，并额外运行 route/repository contract 单测。功能文件 frontmatter 已从 `status: review` 改为 `status: done`。按用户要求，未修改 README 的“## 5. 开发状态机”表。

## 9 个验收维度

| 维度 | 结果 | 说明 |
| --- | --- | --- |
| 1. 文件交付完整性 | 通过 | 功能文件原清单和 remediation 补充清单中的必需文件均存在且非空。`e2e/helpers/mock-api.ts` 有 plan-05 E2E helper 相关修改但未列入清单，已记录为非阻塞范围建议。 |
| 2. Task 列表完成度 | 通过 | 7 个 Task 均为 `done`，无 `todo`、无非法状态、无未说明的 `waived`。 |
| 3. 验收标准完成度 | 通过 | Iteration Memory、性能、降级回归验收 checklist 均已勾选；AC-02、AC-05、AC-08 均有实现与测试证据。 |
| 4. 实现规格符合度 | 通过 | HistoryStrip 空态/列表/Compare、HistoryDetailDialog prompt/params/action、workspace restore/save/continue、history error/auth 状态均符合 plan-05；未新增 history API、compare API、后端表或后端模板字段。 |
| 5. 验证命令执行 | 通过 | 功能文件列出的 4 条命令均退出码 0；另补跑 route/repository contract 单测，退出码 0。 |
| 6. E2E 质量证据 | 通过 | red 证据结论为“预期失败，测试有效”；green 证据结论为“通过”。本次复跑 `workspace-ai-first-iteration-memory.spec.ts`，6 个用例全部通过。 |
| 7. 边界与风险 | 通过 | history 空态、401、API error、restore 失败、restored prompt variables、保存 restored style memory 均为 `done`；remediation 已按用户授权做最小 completed detail response 扩展，未触达暂停条件。 |
| 8. 契约对齐 | 通过 | `findByIdWithRecipe`、`GET /api/generation/[id]`、`useHistoryRestore`、workspace `TemplateSaveDialog` 入参已对齐 restored source context contract；route/repository 测试覆盖 `sourceAssetId/sourceImageUrl/variables/analysisTemplateVariables`。 |
| 9. 代码审查 | 通过 | 未发现正确性、安全、数据损坏或下游 contract blocker。 |

## 验证命令结果

| 命令 | 退出码 | 关键输出 |
| --- | --- | --- |
| `pnpm vitest --run src/components/workspace/__tests__/history-strip.test.tsx src/components/workspace/__tests__/history-detail-dialog.test.tsx src/hooks/__tests__/use-history-restore.test.tsx` | 0 | `Test Files 3 passed (3)`；`Tests 12 passed (12)` |
| `pnpm vitest --run src/lib/repositories/__tests__/generation-task-repository.test.ts 'src/app/api/generation/[id]/__tests__/route.test.ts'` | 0 | `Test Files 2 passed (2)`；`Tests 29 passed (29)`；额外覆盖 remediation contract |
| `pnpm e2e -- e2e/workspace-ai-first-iteration-memory.spec.ts` | 0 | `6 passed (27.8s)`；输出包含 `NO_COLOR` 被 `FORCE_COLOR` 覆盖的 Node warning，不阻塞 |
| `pnpm type-check` | 0 | `tsc --noEmit` 通过 |
| `pnpm build` | 0 | Next.js production build 成功；存在既有 ESLint/font/img warning，不阻塞 |

## Blocker

无。

### B-01 remediation 复核：已关闭

- repository：`findByIdWithRecipe` 已 select `analysisTasks.sourceAssetId`、source asset `fileUrl`、`analysisTasks.analysisTemplateVariables`，并返回 `sourceAssetId/sourceImageUrl/variables/analysisTemplateVariables`。
- route：completed detail response 已转发 `sourceAssetId/sourceImageUrl/variables/analysisTemplateVariables`，并有 route 单测断言字段存在。
- 前端：`useHistoryRestore` 消费真实 detail response 字段；workspace restored save path 将 source context 与 variables 传入 `TemplateSaveDialog`。
- E2E：TC-5.5 的 mock detail payload 与真实 completed detail response 字段同名，复跑通过。

## 建议项

1. 将 `e2e/helpers/mock-api.ts` 补录到 plan-05 文件清单或“允许修改的额外文件”，因为本功能的 targeted E2E 新增了 history/detail/template helper。
2. 后续可收紧一个 legacy 边界：当 restored detail 明确返回 `sourceAssetId/sourceImageUrl: null` 时，workspace 目前会回退当前 workspace reference；若需要避免旧历史记录误关联当前 reference，可改为保留 `null` 并在保存入口显示来源未知。

## 状态变更记录

- 功能文件 frontmatter：`status: review` -> `status: done`。
- README 开发状态机：按用户要求未修改。
- Review 报告：已重写当前文件，记录 remediation 后通过结论。
