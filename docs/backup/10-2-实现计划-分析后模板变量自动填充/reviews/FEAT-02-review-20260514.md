# FEAT-02 Review: 工作台自动模板编辑与生成

- **验收日期**: 2026-05-14
- **功能文件**: `docs/10-2-实现计划-分析后模板变量自动填充/FEAT-02-工作台自动模板编辑与生成.md`
- **验收结论**: 未通过
- **状态变更**: 无，保持 `status: review`

## 环境备注

- 按 skill 要求尝试读取 `.Codex/contracts/workflow-schema.json`，当前仓库不存在 `.Codex/` 目录，未能读取 workflow schema。
- 本地 `localhost:3000` 已被 PID 73255 的 Next 进程占用，且该进程返回 500；Playwright 不能复用，也不能启动新的 3000 webServer。

## 9 个验收维度

| 维度 | 结果 | 说明 |
| --- | --- | --- |
| 1. 文件交付完整性 | 通过 | 文件清单中的 workspace state、页面接线、编辑器、变量面板、生成面板、测试和 E2E spec 均存在。 |
| 2. Task 列表完成度 | 通过 | 8 个 Task 均为 `done`。 |
| 3. 验收标准完成度 | 未通过 | 功能文件仍有 16 个 `- [ ]` 未勾选验收项，未写豁免原因。 |
| 4. 实现规格符合度 | 未通过 | 自动模板主链路基本实现，但持久化和文本/fallback 保存路径存在阻塞问题。 |
| 5. 验证命令执行 | 未通过 | Vitest、type-check、build 通过；E2E 命令因 3000 占用失败，未执行到用例层。 |
| 6. E2E 质量证据 | 部分通过 | red/green evidence 文件存在且 green 记录 5 passed；但本次 review 无法复跑通过。 |
| 7. 边界与风险 | 通过 | 边界场景表均为 `done`。 |
| 8. 契约对齐 | 部分通过 | FEAT-01 自动模板字段已接入；但 workspace 持久化契约未实际生效。 |
| 9. 代码审查 | 未通过 | 发现 2 个阻塞级代码问题。 |

## 验证命令摘要

| 命令 | 退出码 | 摘要 |
| --- | --- | --- |
| `pnpm e2e -- e2e/analysis-template-autofill.spec.ts` | 1 | Playwright webServer 启动失败：`EADDRINUSE :::3000` |
| `pnpm vitest --run src/hooks/__tests__/use-workspace-state.test.tsx src/components/workspace/__tests__/unified-prompt-editor.test.tsx src/components/workspace/__tests__/template-variable-panel.test.tsx src/components/workspace/__tests__/light-generate-panel.test.tsx` | 0 | 4 files / 40 tests passed |
| `pnpm type-check` | 0 | TypeScript check passed |
| `pnpm build` | 0 | Build passed；存在既有 lint warning，不阻塞 |

## Blocker

1. 验收标准 checklist 未关闭：`FEAT-02-工作台自动模板编辑与生成.md` 仍有 16 个 `- [ ]`。
2. 当前 E2E 验证命令未通过：`localhost:3000` 被占用且返回 500，Playwright 无法启动 webServer。
3. `src/hooks/use-workspace-state.ts:260` / `src/hooks/use-workspace-state.ts:471`：新会话下 `isRestoredRef.current` 为 `false` 且从未被置为 `true`，持久化 effect 永远提前返回，导致 FEAT-02 要求的 workspace/template 字段持久化不会写入 sessionStorage。
4. `src/components/workspace/unified-prompt-editor.tsx:208`：保存按钮始终提交 `templateSource`。用户在文本模式或 fallback 文本模式编辑后，`textPrompt` 不会被传给保存弹窗，存在保存旧内容的问题，影响“fallback 仍可编辑/保存”和文本模式保存路径。

## 建议项

- 修复持久化 effect 的首次写入逻辑，并补一个从 fresh session 完成分析后写入 `analysisTemplate*` 的测试。
- 保存按钮按当前模式提交内容：模板模式提交 `templateSource`，文本模式提交 `textPrompt`。
- 释放或修复本地 3000 进程后复跑 FEAT-02 E2E。
