# FEAT-03 Review: 模板保存默认值承接

- **验收日期**: 2026-05-14
- **功能文件**: `docs/10-2-实现计划-分析后模板变量自动填充/FEAT-03-模板保存默认值承接.md`
- **验收结论**: 未通过
- **状态变更**: 无，保持 `status: review`

## 环境备注

- 按 skill 要求尝试读取 `.Codex/contracts/workflow-schema.json`，当前仓库不存在 `.Codex/` 目录，未能读取 workflow schema。
- 本地 `localhost:3000` 已被 PID 73255 的 Next 进程占用，且该进程返回 500；Playwright 不能复用，也不能启动新的 3000 webServer。

## 9 个验收维度

| 维度 | 结果 | 说明 |
| --- | --- | --- |
| 1. 文件交付完整性 | 通过 | 文件清单中的 parser、repository、API、编辑器、保存弹窗、组件测试和 E2E spec 均存在。 |
| 2. Task 列表完成度 | 通过 | 8 个 Task 均为 `done`。 |
| 3. 验收标准完成度 | 未通过 | 功能文件仍有 14 个 `- [ ]` 未勾选验收项，未写豁免原因。 |
| 4. 实现规格符合度 | 未通过 | 变量默认值保存主链路有实现；但 `sourceAnalysisTaskId` 持久化违反 FEAT-03 明确规格，文本保存路径也存在旧内容风险。 |
| 5. 验证命令执行 | 未通过 | Parser/repository/dialog tests、type-check、build 通过；E2E 命令因 3000 占用失败。 |
| 6. E2E 质量证据 | 部分通过 | red/green evidence 文件存在且 green 记录 1 passed；但本次 review 无法复跑通过。 |
| 7. 边界与风险 | 通过 | 边界场景表均为 `done`。 |
| 8. 契约对齐 | 未通过 | `sourceAnalysisTaskId` 规格要求只用于日志/排查，不写入 `templates` 表；当前 schema/repository/type 已持久化并返回该字段。 |
| 9. 代码审查 | 未通过 | 发现 2 个阻塞级代码问题。 |

## 验证命令摘要

| 命令 | 退出码 | 摘要 |
| --- | --- | --- |
| `pnpm e2e -- e2e/template-default-values.spec.ts` | 1 | Playwright webServer 启动失败：`EADDRINUSE :::3000` |
| `pnpm vitest --run src/lib/__tests__/template-parser.test.ts src/lib/repositories/__tests__/template-repository.test.ts` | 0 | 2 files / 42 tests passed |
| `pnpm vitest --run src/components/workspace/__tests__/template-save-dialog.test.tsx` | 0 | 1 file / 1 test passed |
| `pnpm type-check` | 0 | TypeScript check passed |
| `pnpm build` | 0 | Build passed；存在既有 lint warning，不阻塞 |

## Blocker

1. 验收标准 checklist 未关闭：`FEAT-03-模板保存默认值承接.md` 仍有 14 个 `- [ ]`。
2. 当前 E2E 验证命令未通过：`localhost:3000` 被占用且返回 500，Playwright 无法启动 webServer。
3. `src/lib/db/schema.ts:176`、`src/lib/repositories/template-repository.ts:16`、`src/lib/repositories/template-repository.ts:43`：`sourceAnalysisTaskId` 被写入、读取并暴露为模板字段，违反 FEAT-03 规格“首版不写入 `templates` 表，仅作为日志/排查来源标记”。现有 repository 测试还在断言该持久化行为。
4. `src/components/workspace/unified-prompt-editor.tsx:208`：文本模式保存仍提交 `templateSource` 而不是当前 `textPrompt`，会让普通文本/fallback 文本保存为旧内容，不满足“只有普通文本没有模板变量时保存为普通模板”的边界场景。

## 建议项

- 移除 `templates.source_analysis_task_id` 的新增 schema/repository/type/API 响应持久化路径，保留 API 入参校验和日志字段即可。
- 调整 repository 测试，不再断言 `sourceAnalysisTaskId` 被写入 `templates`。
- 修复文本模式保存内容来源后，补覆盖普通文本/fallback 文本保存的组件或 E2E 断言。
- 释放或修复本地 3000 进程后复跑 FEAT-03 E2E。
