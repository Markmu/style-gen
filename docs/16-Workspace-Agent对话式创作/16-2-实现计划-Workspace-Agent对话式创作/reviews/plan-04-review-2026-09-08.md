# plan-04 独立验收报告

- 日期：2026-09-08
- 实现者：implement_plan04 agent；审查者：root agent，未参与本轮代码或测试实现。
- 结论：通过；0 未解决 blocker。

| 维度 | 结果 | 依据 |
| --- | --- | --- |
| 文件交付 | 通过 | 核对服务、输出校验、幂等资产仓储、回调、路由、人工脚本与规定测试完整；邻近变更已记录 |
| Task 列表 | 通过 | 全部 done |
| AC | 通过 | AC-09/19/20/21 与性能边界有实际断言 |
| 规格 | 通过 | 已知输出恢复、签名任务绑定、唯一结果事务、未知隔离、显式 prepared 首次发送 |
| 验证 | 通过 | DB 29、API 4、聚焦 79；完整门 1234 单元测试、构建、18 smoke 通过 |
| E2E 质量 | 通过 | red 为实际 outputStored 读取未完成；green 为真实 Next/数据库及有效/失效签名回调 |
| 边界 | 通过 | 真数据库 trigger 注入事务/CAS/ID/R2 后写入故障；并发、乱序与所有权均验证 |
| 契约 | 通过 | 四种公开状态保持；unknown 不释放或重发；人工材料严格绑定；原绑定失效拒绝 |
| 代码审查 | 通过 | 独立阅读状态机、存储/媒体处理、脚本、Provider/回调与测试，无剩余阻断问题 |

## 修补与证据

审阅发现并由实现者修复：截断图片被文件头检查误接受、内联图片从方向读取泄露大量内部数据、无查询 Provider 缺少人工支持材料恢复、原绑定不可用缺测试、模型配置动态 mock 反复竞争。实现者另补签名正文 webhook 地址对应任务校验，root 核对同样约束人工 Replicate 查询绑定。

复用 [red](evidence-plan-04/red-api.log)、[API](evidence-plan-04/green-api.log)、[DB](evidence-plan-04/green-db.log)、[聚焦](evidence-plan-04/green-focused.log)、[完整门](evidence-plan-04/verify-full.log)、[文档门](evidence-plan-04/verify-fast-final.log)。root 校验 29 份源码/测试 SHA256 均一致。最终新增原绑定不可用用例只修改隔离 integration 测试与证据，DB 29 项重跑通过；生产代码未变，完整门及 API 结果有效复用。

关键断言：真实 PostgreSQL trigger 在 R2 成功后阻止 outputStored 写入，移除故障后仅 HEAD 恢复，PUT/下载不重复；并发显式 prepared 只有一次外呼；120s/300s deadline 和 4999ms/5000ms 查询节流；有效签名且正确任务才能绑定早到回调；成功结果不被迟到失败覆盖；原模型移除时无新任务/外呼。

模型配置校验改为实际模块初始化共享的 loadModelConfig，保留全部非法配置断言及真实目录解析测试；未通过删断言掩盖问题。既有 26 lint warnings 不阻塞。

## 运行边界

人工脚本默认只读。apply 必须精确 Provider 查询或带哈希与操作者确认的支持材料，审计保留白名单字段；仅 not_accepted 且确认未收费释放预约。无证据继续隔离。测试使用可丢弃数据库和外部边界 fixture，无真实 apply、收费调用或部署。

代码请求时限声明 240s，实际宿主/代理时限仍待部署验证。下一任务负责分析管线和前端本机恢复；本次不宣称完整 UI 已交付。

## 状态

plan-04 review → done，绑定本报告；计划保持 in_execution，继续 plan-05。
