# Impeccable Design Context

## Design Context

### Users
混合用户群——专业设计师、产品运营人员、AI 爱好者都可能使用。典型场景：上传参考图 → AI 提取风格配方 → 调整并生成新图。用户在创作流程中使用，需要快速、可信的结果，界面不应干扰内容本身。

### Brand Personality
简洁·现代·克制。工具应该像一把好手术刀——精确、安静、值得信赖。不过度表达自我，而是让用户的创作成果成为焦点。

### Aesthetic Direction
- **主题**：深色为主，精致克制。参考 Linear、Vercel、Raycast 的深色 UI 气质。
- **当前状态**：极简白底 + Tailwind 默认色 + 系统字体，需要向更精致的深色方向演进。
- **色彩基调**：深色背景 + 高对比前景，用语义色（indigo/蓝系）做功能性强调，避免彩虹色板。
- **排版**：现代无衬线字体栈，清晰层级，正文可读性优先。
- **留白**：宽敞但不空洞，用空间引导视线而非装饰元素。
- **交互**：微妙的过渡和反馈，不要花哨动画。
- **图片内容优先**：界面是画框，不是画作——让用户上传和生成的图片始终是视觉中心。

### Anti-References
- **不要信息过载**：避免满屏文字、密集表单、过多选项。
- **不要模板感**：避免看起来像 generic SaaS 后台或 WordPress 主题。
- **不要过度装饰**：避免无意义的渐变、粒子效果、装饰性插画。

### Design Principles

1. **内容即界面** —— 用户上传的参考图和生成的图片是页面的绝对主角，UI 元素应当退居背景，只在需要交互时显现。

2. **克制表达** —— 每个视觉元素都要有存在的理由。如果移除某个元素不影响使用，就不应该存在。用空间和层级代替装饰。

3. **可信反馈** —— AI 处理过程中，用清晰、平静的状态指示（而非焦虑的动画）传达进度。成功与失败都要有明确、不突兀的表达。

4. **高效直达** —— 混合用户群意味着界面要同时满足新手（直觉操作）和专家（快速路径）。减少步骤，消除不必要的确认和跳转。

5. **深色精致** —— 深色主题不是简单的黑底白字，而是精心调校的灰度层级、恰好的对比度、有呼吸感的暗色调。参考高端摄影编辑器的界面气质。

### Current Design System Notes
- **Tech**: Tailwind CSS 4, 系统字体栈, 无 UI 组件库
- **Colors**: Tailwind 默认色板（blue-500 主色, indigo 生成态, gray 灰度层级, red/orange/yellow 语义态）
- **Spacing**: Tailwind 默认间距，卡片 rounded-lg/xl/2xl
- **Icons**: 当前使用 emoji，需考虑引入图标库（如 Lucide）
- **Layout**: 2-column workspace, responsive md: breakpoint
- **State management**: 多层级降级系统（L1-L4），覆盖各种服务不可用场景
